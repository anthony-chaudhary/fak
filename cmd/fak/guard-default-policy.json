{
  "version": "fak-policy/v1",

  "allow": [
    "Bash",
    "BashOutput",
    "KillShell",
    "PowerShell",
    "Read",
    "Edit",
    "Write",
    "NotebookEdit",
    "Glob",
    "Grep",
    "LS",
    "TodoWrite",
    "Task",
    "WebFetch",
    "WebSearch",
    "ExitPlanMode",
    "Skill",
    "SlashCommand",

    "Agent",
    "AskUserQuestion",
    "DeferredToolPlaceholder",
    "EnterPlanMode",
    "Monitor",
    "ReportFindings",
    "ScheduleWakeup",
    "SendMessage",
    "StructuredOutput",
    "TaskCreate",
    "TaskGet",
    "TaskList",
    "TaskOutput",
    "TaskStop",
    "TaskUpdate",
    "ToolSearch",
    "shell_command",
    "functions.shell_command",
    "update_plan",
    "functions.update_plan",
    "request_user_input",
    "functions.request_user_input",
    "view_image",
    "functions.view_image",
    "get_goal",
    "functions.get_goal",
    "create_goal",
    "functions.create_goal",
    "update_goal",
    "functions.update_goal",
    "emit_context_tip",
    "list_mcp_resources",
    "functions.list_mcp_resources",
    "list_mcp_resource_templates",
    "functions.list_mcp_resource_templates",
    "read_mcp_resource",
    "functions.read_mcp_resource",
    "tool_search_tool",
    "tool_search.tool_search_tool",
    "parallel",
    "multi_tool_use.parallel",
    "web.run",
    "imagegen",
    "image_gen.imagegen",
    "ListMcpResourcesTool",
    "ReadMcpResourceTool",
    "ReadMcpResourceDirTool",

    "Workflow",
    "EnterWorktree",
    "ExitWorktree",
    "CronCreate",
    "CronList",
    "CronDelete",
    "PushNotification",
    "RemoteTrigger",
    "DesignSync",

    "mcp__dos__dos_verify",
    "mcp__dos__dos_arbitrate",
    "dos_dos_arbitrate",
    "mcp__dos__dos_recall",
    "mcp__dos__dos_review",
    "mcp__dos__dos_status",
    "mcp__dos__dos_doctor",
    "mcp__dos__dos_answer",
    "mcp__dos__dos_check_reason",
    "mcp__dos__dos_refuse_reasons",
    "mcp__dos__dos_commit_audit",
    "mcp__dos__dos_citation_resolve",
    "mcp__dos__acme_lane_hint",
    "mcp__fak__fak_adjudicate",
    "mcp__fak__fak_admit",
    "mcp__fak__fak_syscall",
    "mcp__fak__fak_read",
    "mcp__fak__fak_changes",
    "mcp__fak__fak_memory_drivers",
    "mcp__fak__fak_memory_explain",
    "mcp__fak__fak_trajquery",
    "mcp__fak__fak_tools_search",
    "mcp__fak__fak_index_lane",
    "mcp__fak__fak_index_leaves",
    "mcp__fak__fak_index_docs",
    "mcp__fak__fak_index_claims",
    "mcp__fak__fak_index_verbs",
    "mcp__fak__fak_index_work",
    "mcp__fak__fak_index_freshness",
    "mcp__fak__fak_feature_query",
    "mcp__fak__fak_capabilities",
    "mcp__fak__fak_context_value",
    "mcp__fak__fak_context_spans",
    "mcp__fak__fak_context_restore",
    "mcp__fak__fak_resume_history",

    "mcp__playwright__browser_snapshot",
    "mcp__playwright__browser_navigate",
    "mcp__playwright__browser_take_screenshot",
    "mcp__playwright__browser_tabs",
    "mcp__playwright__browser_click",
    "mcp__playwright__browser_type",
    "mcp__playwright__browser_fill_form",
    "mcp__playwright__browser_select_option",
    "mcp__playwright__browser_file_upload",
    "mcp__playwright__browser_press_key",

    "bash",
    "read",
    "write",
    "edit",
    "multiedit",
    "patch",
    "apply_patch",
    "functions.apply_patch",
    "grep",
    "glob",
    "list",
    "ls",
    "webfetch",
    "todowrite",
    "todoread",
    "task",
    "skill",
    "lsp",

    "execute_code",
    "write_file",
    "edit_file"
  ],

  "allow_prefix": [
    "read_",
    "get_",
    "search_",
    "list_",
    "lookup_",
    "find_",
    "web_",
    "file_"
  ],

  "deny": {
    "delete_file": "POLICY_BLOCK",
    "mcp__atlassian__confluence_delete_page": "POLICY_BLOCK",
    "mcp__atlassian__confluence_delete_attachment": "POLICY_BLOCK"
  },

  "arg_rules": [
    { "tool": "execute_code", "arg": "code", "deny_regex": "rm\\s+-rf|sudo|os\\.system\\(|subprocess\\.|:(){:|:&};:", "reason": "POLICY_BLOCK",
      "fix": "run the programmatic step without a shell escape: this arg is for pure computation, so recursive forced deletes, local privilege escalation, the process-spawning standard-library entry points and fork-bomb shapes are all refused here whatever the intent. Use the file/edit tools for writes and the shell tools — which carry their own argument rules, including the MENTION carve-outs this rule does not have — for real commands. Worded without the literal tokens so that quoting this refusal does not re-trip the rule that produced it" },

    { "tool": "Bash", "arg": "command", "deny_regex": "\\brm\\s+-[A-Za-z]*[rRfF]", "reason": "POLICY_BLOCK",
      "fix": "delete exact files without -r/-f (rm <file>), or move them aside (mv <path> <trash-dir>). Two routes are ALREADY admitted with no operator: (1) a recursive delete whose targets all sit strictly BELOW a declared harness scratchpad root (FAK_GUARD_SCRATCHPAD_ROOTS) — cleaning up your own session scratch is routine work, not a fatal action; (2) a force-only delete (no -r) of ONE explicit literal path, which falls through to the reversibility preview-confirm gate. Still refused because the blast radius is not provably bounded: the scratchpad root itself, a glob, a $VAR or command-substitution target, several paths at once, and any recursive delete of real work — that last one is operator-only" },
    { "tool": "Bash", "arg": "command", "deny_regex": "\\bsudo\\b", "reason": "POLICY_BLOCK",
      "fix": "run the command without sudo — only a LOCAL escalation is refused (a remote one like `ssh host 'sudo …'` is allowed; the floor governs this host only); if local elevation is genuinely required, print the exact command and ask the operator to run it" },
    { "tool": "Bash", "arg": "command", "deny_regex": "\\bmkfs\\b|\\bdd\\s+if=|>\\s*/dev/sd", "reason": "POLICY_BLOCK",
      "fix": "disk/device operations are operator-only; print the exact command and what it is for, and ask the operator" },
    { "tool": "Bash", "arg": "command", "deny_regex": ":\\(\\)\\s*\\{", "reason": "POLICY_BLOCK",
      "fix": "a fork-bomb shaped function definition is refused: it forks until the host stops responding, and nothing running on it is recoverable afterwards. There is no sanctioned way to RUN one. Unlike the download-pipe and delete rules this one has no inert-MENTION carve-out yet, so quoting the literal in a commit message, a grep pattern or a doc trips it too — describe the shape in words, or break up the token, when you only need to TALK about it. This text deliberately omits the literal so that quoting the refusal does not re-trip the rule" },
    { "tool": "Bash", "arg": "command", "deny_regex": "\\b(curl|wget)\\b[^|]*\\|\\s*(sudo\\s+)?(ba)?sh\\b", "reason": "POLICY_BLOCK",
      "fix": "download to a file first, inspect it, then run the inspected file: curl -o <file> <url>; cat <file>; sh <file>. Only a real download piped into an interpreter at a command boundary is refused: an inert MENTION of the pattern is already admitted with no operator — a quoted argument (echo, a grep pattern, a commit message, a here-doc), a URL that is merely fetched to a file, and a pipe into a non-interpreter (jq, tee, grep) all pass" },
    { "tool": "Bash", "arg": "command", "deny_regex": "(?i)\\b(?:Select-Object|Get-ChildItem|Get-Content|Get-Item|Get-ItemProperty|Get-Process|Get-Command|Where-Object|ForEach-Object|Measure-Object|Sort-Object|Group-Object|Format-Table|Format-List|Out-File|Set-Content|Add-Content|New-Item|Remove-Item|Copy-Item|Move-Item|Rename-Item|Test-Path|Set-Location|Get-Location|Select-String|Write-Output|Write-Host|Invoke-WebRequest|Invoke-RestMethod)\\b", "reason": "SHELL_DIALECT",
      "fix": "PowerShell cmdlet submitted to the Bash tool — it fails `command not found` (exit 127). Use the PowerShell tool for cmdlet pipelines, or the POSIX equivalent in Bash (Select-Object->head/sed, Where-Object->grep, Measure-Object->wc, Get-Content->cat, Get-ChildItem->ls)." },
    { "tool": "Bash", "arg": "command", "deny_regex": "\\bterraform(?:\\.exe)?\\b[^|;&\\n]*\\bdestroy\\b", "reason": "POLICY_BLOCK",
      "fix": "produce the destroy plan for review instead: terraform plan -destroy — that plan is read-only and IS admitted, as are naming, grepping or documenting the command and every read-only subcommand (show/state/output/validate). Only a real teardown is refused: terraform destroy, or its exact equivalent terraform apply -destroy. Applying one is operator-only" },
    { "tool": "Bash", "arg": "command", "deny_regex": "-o\\s+\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "write inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "Bash", "arg": "command", "deny_regex": "--output[= ]\\s*\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "write inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "Bash", "arg": "command", "deny_regex": ">>?\\s*\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "redirect to a path inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "Bash", "arg": "command", "deny_regex": "\\b(cp|mv|install|tee|rsync|ln)\\b[^|;&]*\\s\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "copy/move to a destination inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },

    { "tool": "PowerShell", "arg": "command", "deny_regex": "(?i)\\bRemove-Item\\b[^|;\\n]*-(Recurse|Force)\\b", "reason": "POLICY_BLOCK",
      "fix": "remove exact items without -Recurse/-Force (Remove-Item <file>), or move them aside. A recursive removal is ALREADY admitted when every target sits strictly BELOW a declared harness scratchpad root (FAK_GUARD_SCRATCHPAD_ROOTS), Windows backslash paths included — cleaning up your own session scratch is routine work, not a fatal action. Still refused because the blast radius is not provably bounded: the scratchpad root itself, a wildcard, a comma-separated -Path list, an unterminated quote, any target outside the declared roots, and any recursive/forced removal of real work — that last one is operator-only" },
    { "tool": "PowerShell", "arg": "command", "deny_regex": "(?i)\\b(Format-Volume|Clear-Disk|Initialize-Disk)\\b", "reason": "POLICY_BLOCK",
      "fix": "disk/volume operations are operator-only: they destroy a filesystem outright and nothing that was on it is recoverable. Print the exact command and what it is for, and ask the operator" },
    { "tool": "PowerShell", "arg": "command", "deny_regex": "(?i)\\bClear-Content\\b", "reason": "POLICY_BLOCK",
      "fix": "emptying a file in place is refused: the previous contents are gone with no preview and no undo. This is a FILE operation, not a disk/volume one — it used to be bundled with Format-Volume, so the refusal told you to fetch an operator for something no operator needs to approve. Three routes are ALREADY admitted with no operator: rewrite the file with the content you want (Set-Content -Path <file> -Value ''), write it with the harness's own file tools, or move the old file aside (Move-Item) and create a fresh one. Ask the operator only if you genuinely need the in-place truncation itself" },
    { "tool": "PowerShell", "arg": "command", "deny_regex": "(?i)\\b(Invoke-WebRequest|iwr|curl|wget|Invoke-RestMethod|irm)\\b[^|]*\\|[^|]*\\b(iex|Invoke-Expression)\\b", "reason": "POLICY_BLOCK",
      "fix": "download to a file first, inspect it, then run the inspected file: Invoke-WebRequest -OutFile <file> <url>; Get-Content <file>; & <file>. Fetching to a file and reading it back are both already admitted with no operator, and so is an inert MENTION of the pattern — a quoted argument, a grep pattern, a commit message. What is refused is only the single step that executes bytes nobody has read" },
    { "tool": "PowerShell", "arg": "command", "deny_regex": "(?i)\\bStart-Process\\b[^|;\\n]*-Verb\\s+RunAs\\b", "reason": "POLICY_BLOCK",
      "fix": "elevation is operator-only BY DESIGN: the floor has no interactive-vs-headless seam to approve it in-band (internal/policy TestToolCallEnvelopeHasNoBypassSeam, #2921). Print the exact command and ask the operator to run it in an elevated shell — printing, grepping or documenting the command is admitted; only a real Start-Process invocation is refused (#2343)" },
    { "tool": "PowerShell", "arg": "command", "deny_regex": "(?i)\\bterraform(?:\\.exe)?\\b[^|;&\\n]*\\bdestroy\\b", "reason": "POLICY_BLOCK",
      "fix": "produce the destroy plan for review instead: terraform plan -destroy — that plan is read-only and IS admitted, as are naming, grepping or documenting the command and every read-only subcommand (show/state/output/validate). Only a real teardown is refused: terraform destroy, or its exact equivalent terraform apply -destroy. Applying one is operator-only" },

    { "tool": "shell_command", "arg": "command", "deny_regex": "\\brm\\s+-[A-Za-z]*[rRfF]", "reason": "POLICY_BLOCK",
      "fix": "delete exact files without -r/-f (rm <file>), or move them aside (mv <path> <trash-dir>). Two routes are ALREADY admitted with no operator: (1) a recursive delete whose targets all sit strictly BELOW a declared harness scratchpad root (FAK_GUARD_SCRATCHPAD_ROOTS) — cleaning up your own session scratch is routine work, not a fatal action; (2) a force-only delete (no -r) of ONE explicit literal path, which falls through to the reversibility preview-confirm gate. Still refused because the blast radius is not provably bounded: the scratchpad root itself, a glob, a $VAR or command-substitution target, several paths at once, and any recursive delete of real work — that last one is operator-only" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "\\bsudo\\b", "reason": "POLICY_BLOCK",
      "fix": "run the command without sudo — only a LOCAL escalation is refused (a remote one like `ssh host 'sudo …'` is allowed; the floor governs this host only); if local elevation is genuinely required, print the exact command and ask the operator to run it" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "\\bmkfs\\b|\\bdd\\s+if=|>\\s*/dev/sd", "reason": "POLICY_BLOCK",
      "fix": "disk/device operations are operator-only; print the exact command and what it is for, and ask the operator" },
    { "tool": "shell_command", "arg": "command", "deny_regex": ":\\(\\)\\s*\\{", "reason": "POLICY_BLOCK",
      "fix": "a fork-bomb shaped function definition is refused: it forks until the host stops responding, and nothing running on it is recoverable afterwards. There is no sanctioned way to RUN one. Unlike the download-pipe and delete rules this one has no inert-MENTION carve-out yet, so quoting the literal in a commit message, a grep pattern or a doc trips it too — describe the shape in words, or break up the token, when you only need to TALK about it. This text deliberately omits the literal so that quoting the refusal does not re-trip the rule" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "\\b(curl|wget)\\b[^|]*\\|\\s*(sudo\\s+)?(ba)?sh\\b", "reason": "POLICY_BLOCK",
      "fix": "download to a file first, inspect it, then run the inspected file: curl -o <file> <url>; cat <file>; sh <file>. Only a real download piped into an interpreter at a command boundary is refused: an inert MENTION of the pattern is already admitted with no operator — a quoted argument (echo, a grep pattern, a commit message, a here-doc), a URL that is merely fetched to a file, and a pipe into a non-interpreter (jq, tee, grep) all pass" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "-o\\s+\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "write inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "--output[= ]\\s*\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "write inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "shell_command", "arg": "command", "deny_regex": ">>?\\s*\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "redirect to a path inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "\\b(cp|mv|install|tee|rsync|ln)\\b[^|;&]*\\s\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "copy/move to a destination inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "(?i)\\bRemove-Item\\b[^|;\\n]*-(Recurse|Force)\\b", "reason": "POLICY_BLOCK",
      "fix": "remove exact items without -Recurse/-Force (Remove-Item <file>), or move them aside. A recursive removal is ALREADY admitted when every target sits strictly BELOW a declared harness scratchpad root (FAK_GUARD_SCRATCHPAD_ROOTS), Windows backslash paths included — cleaning up your own session scratch is routine work, not a fatal action. Still refused because the blast radius is not provably bounded: the scratchpad root itself, a wildcard, a comma-separated -Path list, an unterminated quote, any target outside the declared roots, and any recursive/forced removal of real work — that last one is operator-only" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "(?i)\\b(Format-Volume|Clear-Disk|Initialize-Disk)\\b", "reason": "POLICY_BLOCK",
      "fix": "disk/volume operations are operator-only: they destroy a filesystem outright and nothing that was on it is recoverable. Print the exact command and what it is for, and ask the operator" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "(?i)\\bClear-Content\\b", "reason": "POLICY_BLOCK",
      "fix": "emptying a file in place is refused: the previous contents are gone with no preview and no undo. This is a FILE operation, not a disk/volume one — it used to be bundled with Format-Volume, so the refusal told you to fetch an operator for something no operator needs to approve. Three routes are ALREADY admitted with no operator: rewrite the file with the content you want (Set-Content -Path <file> -Value ''), write it with the harness's own file tools, or move the old file aside (Move-Item) and create a fresh one. Ask the operator only if you genuinely need the in-place truncation itself" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "(?i)\\b(Invoke-WebRequest|iwr|curl|wget|Invoke-RestMethod|irm)\\b[^|]*\\|[^|]*\\b(iex|Invoke-Expression)\\b", "reason": "POLICY_BLOCK",
      "fix": "download to a file first, inspect it, then run the inspected file: Invoke-WebRequest -OutFile <file> <url>; Get-Content <file>; & <file>. Fetching to a file and reading it back are both already admitted with no operator, and so is an inert MENTION of the pattern — a quoted argument, a grep pattern, a commit message. What is refused is only the single step that executes bytes nobody has read" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "(?i)\\bStart-Process\\b[^|;\\n]*-Verb\\s+RunAs\\b", "reason": "POLICY_BLOCK",
      "fix": "elevation is operator-only BY DESIGN: the floor has no interactive-vs-headless seam to approve it in-band (internal/policy TestToolCallEnvelopeHasNoBypassSeam, #2921). Print the exact command and ask the operator to run it in an elevated shell — printing, grepping or documenting the command is admitted; only a real Start-Process invocation is refused (#2343)" },
    { "tool": "shell_command", "arg": "command", "deny_regex": "(?i)\\bterraform(?:\\.exe)?\\b[^|;&\\n]*\\bdestroy\\b", "reason": "POLICY_BLOCK",
      "fix": "produce the destroy plan for review instead: terraform plan -destroy — that plan is read-only and IS admitted, as are naming, grepping or documenting the command and every read-only subcommand (show/state/output/validate). Only a real teardown is refused: terraform destroy, or its exact equivalent terraform apply -destroy. Applying one is operator-only" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "\\brm\\s+-[A-Za-z]*[rRfF]", "reason": "POLICY_BLOCK",
      "fix": "delete exact files without -r/-f (rm <file>), or move them aside (mv <path> <trash-dir>). Two routes are ALREADY admitted with no operator: (1) a recursive delete whose targets all sit strictly BELOW a declared harness scratchpad root (FAK_GUARD_SCRATCHPAD_ROOTS) — cleaning up your own session scratch is routine work, not a fatal action; (2) a force-only delete (no -r) of ONE explicit literal path, which falls through to the reversibility preview-confirm gate. Still refused because the blast radius is not provably bounded: the scratchpad root itself, a glob, a $VAR or command-substitution target, several paths at once, and any recursive delete of real work — that last one is operator-only" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "\\bsudo\\b", "reason": "POLICY_BLOCK",
      "fix": "run the command without sudo — only a LOCAL escalation is refused (a remote one like `ssh host 'sudo …'` is allowed; the floor governs this host only); if local elevation is genuinely required, print the exact command and ask the operator to run it" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "\\bmkfs\\b|\\bdd\\s+if=|>\\s*/dev/sd", "reason": "POLICY_BLOCK",
      "fix": "disk/device operations are operator-only; print the exact command and what it is for, and ask the operator" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": ":\\(\\)\\s*\\{", "reason": "POLICY_BLOCK",
      "fix": "a fork-bomb shaped function definition is refused: it forks until the host stops responding, and nothing running on it is recoverable afterwards. There is no sanctioned way to RUN one. Unlike the download-pipe and delete rules this one has no inert-MENTION carve-out yet, so quoting the literal in a commit message, a grep pattern or a doc trips it too — describe the shape in words, or break up the token, when you only need to TALK about it. This text deliberately omits the literal so that quoting the refusal does not re-trip the rule" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "\\b(curl|wget)\\b[^|]*\\|\\s*(sudo\\s+)?(ba)?sh\\b", "reason": "POLICY_BLOCK",
      "fix": "download to a file first, inspect it, then run the inspected file: curl -o <file> <url>; cat <file>; sh <file>. Only a real download piped into an interpreter at a command boundary is refused: an inert MENTION of the pattern is already admitted with no operator — a quoted argument (echo, a grep pattern, a commit message, a here-doc), a URL that is merely fetched to a file, and a pipe into a non-interpreter (jq, tee, grep) all pass" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "-o\\s+\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "write inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "--output[= ]\\s*\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "write inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": ">>?\\s*\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "redirect to a path inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "\\b(cp|mv|install|tee|rsync|ln)\\b[^|;&]*\\s\\.\\.[\\\\/]", "reason": "POLICY_BLOCK",
      "fix": "copy/move to a destination inside the working tree (or the harness scratchpad); out-of-tree writes are refused by policy" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "(?i)\\bRemove-Item\\b[^|;\\n]*-(Recurse|Force)\\b", "reason": "POLICY_BLOCK",
      "fix": "remove exact items without -Recurse/-Force (Remove-Item <file>), or move them aside. A recursive removal is ALREADY admitted when every target sits strictly BELOW a declared harness scratchpad root (FAK_GUARD_SCRATCHPAD_ROOTS), Windows backslash paths included — cleaning up your own session scratch is routine work, not a fatal action. Still refused because the blast radius is not provably bounded: the scratchpad root itself, a wildcard, a comma-separated -Path list, an unterminated quote, any target outside the declared roots, and any recursive/forced removal of real work — that last one is operator-only" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "(?i)\\b(Format-Volume|Clear-Disk|Initialize-Disk)\\b", "reason": "POLICY_BLOCK",
      "fix": "disk/volume operations are operator-only: they destroy a filesystem outright and nothing that was on it is recoverable. Print the exact command and what it is for, and ask the operator" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "(?i)\\bClear-Content\\b", "reason": "POLICY_BLOCK",
      "fix": "emptying a file in place is refused: the previous contents are gone with no preview and no undo. This is a FILE operation, not a disk/volume one — it used to be bundled with Format-Volume, so the refusal told you to fetch an operator for something no operator needs to approve. Three routes are ALREADY admitted with no operator: rewrite the file with the content you want (Set-Content -Path <file> -Value ''), write it with the harness's own file tools, or move the old file aside (Move-Item) and create a fresh one. Ask the operator only if you genuinely need the in-place truncation itself" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "(?i)\\b(Invoke-WebRequest|iwr|curl|wget|Invoke-RestMethod|irm)\\b[^|]*\\|[^|]*\\b(iex|Invoke-Expression)\\b", "reason": "POLICY_BLOCK",
      "fix": "download to a file first, inspect it, then run the inspected file: Invoke-WebRequest -OutFile <file> <url>; Get-Content <file>; & <file>. Fetching to a file and reading it back are both already admitted with no operator, and so is an inert MENTION of the pattern — a quoted argument, a grep pattern, a commit message. What is refused is only the single step that executes bytes nobody has read" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "(?i)\\bStart-Process\\b[^|;\\n]*-Verb\\s+RunAs\\b", "reason": "POLICY_BLOCK",
      "fix": "elevation is operator-only BY DESIGN: the floor has no interactive-vs-headless seam to approve it in-band (internal/policy TestToolCallEnvelopeHasNoBypassSeam, #2921). Print the exact command and ask the operator to run it in an elevated shell — printing, grepping or documenting the command is admitted; only a real Start-Process invocation is refused (#2343)" },
    { "tool": "functions.shell_command", "arg": "command", "deny_regex": "(?i)\\bterraform(?:\\.exe)?\\b[^|;&\\n]*\\bdestroy\\b", "reason": "POLICY_BLOCK",
      "fix": "produce the destroy plan for review instead: terraform plan -destroy — that plan is read-only and IS admitted, as are naming, grepping or documenting the command and every read-only subcommand (show/state/output/validate). Only a real teardown is refused: terraform destroy, or its exact equivalent terraform apply -destroy. Applying one is operator-only" }
  ],

  "egress": {
    "research_allow_hosts": [
      "github.com",
      "raw.githubusercontent.com",
      "api.github.com"
    ]
  },

  "self_modify_globs": [
    ".git/",
    ".fak/guard/",
    ".hermes/",
    ".env",
    ".aws/",
    ".npmrc",
    ".netrc",
    "id_rsa",
    "id_ed25519",
    "id_ecdsa",
    "id_dsa",
    "/etc/",
    ".credentials.json",
    ".gitconfig",
    ".bashrc",
    ".bash_profile",
    ".zshrc",
    ".profile",
    "gh/hosts.yml",
    ".pypirc",
    ".docker/config.json",
    ".kube/config",
    ".terraformrc"
  ],

  "redact_fields": [
    "password",
    "secret",
    "api_key",
    "token",
    "authorization"
  ]
}
