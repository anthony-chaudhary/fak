{
  "rows": [
    {
      "id": "promptcachekey",
      "canonical": "PromptCacheKey",
      "family": "cache",
      "kind": "symbol",
      "definition": "The OpenAI Responses-wire cross-shard cache-routing hint (prompt_cache_key): a stable key derived from the cacheable HEAD that pins requests sharing a prefix onto one upstream automatic-prefix-cache node (#5186).",
      "distinction": "A routing/affinity hint on the auto prefix cache (miss only costs latency), distinct from the Anthropic managed-cache TTL upgrade (c4681-cachettlupgrade) which extends explicit cache_control breakpoint lifetime.",
      "distinct_from": [
        "c4681-cachettlupgrade"
      ],
      "aliases": [],
      "grounding": "PromptCacheKey",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ,
    {
      "id": "loopconstraint",
      "canonical": "loop_constraint.go (agent constraint seam)",
      "family": "loop",
      "kind": "symbol",
      "definition": "loop_constraint.go is the agent package's loop-side consumer of the out-of-band add-constraint op (#2756): applyConstraints drains the sessionctl constraint mailbox at the turn boundary and carries the tightened floor's standing directive as a system notice, and constraintDenied denies a floor-forbidden tool call before dispatch with a typed receipt carrying the closed CONSTRAINT_* reason.",
      "distinction": "Not loop_redirect.go (which folds an operator redirect into the live OBJECTIVE - what the session pursues): loop_constraint.go folds operator constraints into the live FLOOR - what the session MAY DO - and enforces it per tool call; and not the trajctl metaloop, which walks intents, not turns.",
      "distinct_from": [
        "loop-metaloop"
      ],
      "aliases": [],
      "grounding": "loop_constraint",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ,
    {
      "id": "looppark",
      "canonical": "loop_park.go (agent park seam)",
      "family": "loop",
      "kind": "symbol",
      "definition": "loop_park.go is the agent package's loop-side consumer of the out-of-band operator approve/deny inbox (#2757): parkEscalatedDeny intercepts an ESCALATE-gated deny at the dispatch site when the session's inbox is open, parks it on the sessionctl pending-action queue, and honors the external verdict — approve re-proposes the call through the normal syscall boundary (byte-identical plus the gate's confirm echo, or operator-modified args freshly adjudicated), deny/timeout abort with a typed receipt carrying the closed PARK_* reason.",
      "distinction": "Not loop_constraint.go (which folds operator constraints into the live FLOOR before dispatch): loop_park.go resolves a call the gate ALREADY refused with an ESCALATE disposition, parking it for an EXTERNAL operator verdict instead of returning the deny straight to the model; and not the trajctl metaloop, which walks intents, not turns.",
      "distinct_from": [
        "loopconstraint"
      ],
      "aliases": [],
      "grounding": "loop_park",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ,
    {
      "id": "loopobserve",
      "canonical": "loop_observe.go (agent progress-observer seam)",
      "family": "loop",
      "kind": "symbol",
      "definition": "loop_observe.go is the agent package's OUTBOUND loop-state seam (#5148): WithProgressObserver installs a typed, nil-safe ProgressObserver that runArm emits closed-set ProgressEvent lifecycle transitions through (turn_started, tool_started, call_adjudicated, result_admitted, turn_done), which the native gateway stream renders as structured SSE. Unset, every emit is a no-op and the loop is byte-for-byte the historical loop.",
      "distinction": "Not loop_constraint.go / loop_park.go, which are INBOUND seams that drain operator ops at the turn boundary and CHANGE what the loop does (the floor it enforces, the park it takes): loop_observe.go is purely OUTBOUND and read-only — it publishes witnessed transitions the loop already made and can never gate, deny, or alter a turn.",
      "distinct_from": [
        "loopconstraint",
        "looppark"
      ],
      "aliases": [],
      "grounding": "loop_observe",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ,
    {
      "id": "calladjudicated",
      "canonical": "call_adjudicated (loop-progress SSE event)",
      "family": "policy-capability",
      "kind": "symbol",
      "definition": "call_adjudicated (Go const ProgressCallAdjudicated) is the loop-progress event KIND that publishes one tool call's already-decided kernel verdict on the native owned-loop SSE stream (#5148): it carries Turn, CallID, Tool, Verdict (ALLOW/DENY/TRANSFORM/QUARANTINE) and, on a deny, the closed refusal reason a client gates on.",
      "distinction": "Not 'adjudicating' (the decision act itself, performed by the kernel's adjudicator rungs): call_adjudicated is the after-the-fact OBSERVATION of that act, emitted on the stream once the verdict is final. It decides nothing and can change no verdict; suppressing it would leave the adjudication outcome identical.",
      "distinct_from": [
        "x2-policy-capability-adjudicating"
      ],
      "aliases": [
        "ProgressCallAdjudicated"
      ],
      "grounding": "call_adjudicated",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ,
    {
      "id": "loopmidflight",
      "canonical": "loop_midflight",
      "family": "loop",
      "kind": "symbol",
      "definition": "loop_midflight.go is the agent package's owned per-run MID-FLIGHT verb mailbox and tamper-evident hash-chained journal (#5158, the write half of #2403 / epic #2388): a transport enqueues interrupt / drop-pending-call / set-budget verbs that the owned loop consumes at its next CLEAN turn boundary (never mid-tool, never mid-adjudication) to stop the arm, skip one named queued call, or write a fresh budget through to the session table; nil-safe, so with no verb issued the loop is byte-for-byte the historical loop.",
      "distinction": "Not loop_observe.go, the OUTBOUND read-only half of the same owned-turn program that only PUBLISHES witnessed lifecycle transitions and can alter no turn: loop_midflight is INBOUND and MUTATING, stopping or steering the live arm at the boundary and sealing at return. And not loop_constraint.go / loop_park.go, which drain sessionctl OPERATOR ops (floor tightening / escalated-deny parking): loop_midflight is the per-run SESSION-verb mailbox (interrupt/drop/set-budget) whose journal is hash-chained tamper-evidence.",
      "distinct_from": [
        "loopobserve",
        "loopconstraint",
        "looppark"
      ],
      "aliases": [],
      "grounding": "loop_midflight",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ,
    {
      "id": "kvprefixreusesupported",
      "canonical": "KVPrefixReuseSupported",
      "family": "support-maturity",
      "kind": "symbol",
      "definition": "Config predicate reporting whether a *KVCache is a COMPLETE session prefix for this architecture — i.e. whether cloning the cache carries the whole of what the session already ingested. True for cached architectures whose per-layer K/V rows are the entire state; false for the gemma4 recompute bridge, whose state is the token history and whose cache stays empty.",
      "distinction": "ExactSpanSupported asks whether an engine can EVICT an exact span from a cache it already holds; this asks whether the cache IS the state at all. A recompute architecture answers yes to neither, but for different reasons: eviction is inert because there are no rows, while prefix reuse is unsound because the rows were never where the prefix lived.",
      "distinct_from": [
        "x2-support-maturity-exactspansupported"
      ],
      "aliases": [],
      "grounding": "KVPrefixReuseSupported",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ]
}
