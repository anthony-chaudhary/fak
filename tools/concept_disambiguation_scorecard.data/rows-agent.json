{
  "rows": [
    {
      "id": "promptcachekey",
      "canonical": "PromptCacheKey",
      "family": "cache",
      "kind": "symbol",
      "definition": "The OpenAI Responses-wire cross-shard cache-routing hint (prompt_cache_key): a stable key derived from the cacheable HEAD that pins requests sharing a prefix onto one upstream automatic-prefix-cache node (#5186).",
      "distinction": "A routing/affinity hint on the auto prefix cache (miss only costs latency), distinct from the Anthropic managed-cache TTL upgrade (c4681-cachettlupgrade) which extends explicit cache_control breakpoint lifetime.",
      "distinct_from": [
        "c4681-cachettlupgrade"
      ],
      "aliases": [],
      "grounding": "PromptCacheKey",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ,
    {
      "id": "loopconstraint",
      "canonical": "loop_constraint.go (agent constraint seam)",
      "family": "loop",
      "kind": "symbol",
      "definition": "loop_constraint.go is the agent package's loop-side consumer of the out-of-band add-constraint op (#2756): applyConstraints drains the sessionctl constraint mailbox at the turn boundary and carries the tightened floor's standing directive as a system notice, and constraintDenied denies a floor-forbidden tool call before dispatch with a typed receipt carrying the closed CONSTRAINT_* reason.",
      "distinction": "Not loop_redirect.go (which folds an operator redirect into the live OBJECTIVE - what the session pursues): loop_constraint.go folds operator constraints into the live FLOOR - what the session MAY DO - and enforces it per tool call; and not the trajctl metaloop, which walks intents, not turns.",
      "distinct_from": [
        "loop-metaloop"
      ],
      "aliases": [],
      "grounding": "loop_constraint",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ,
    {
      "id": "looppark",
      "canonical": "loop_park.go (agent park seam)",
      "family": "loop",
      "kind": "symbol",
      "definition": "loop_park.go is the agent package's loop-side consumer of the out-of-band operator approve/deny inbox (#2757): parkEscalatedDeny intercepts an ESCALATE-gated deny at the dispatch site when the session's inbox is open, parks it on the sessionctl pending-action queue, and honors the external verdict — approve re-proposes the call through the normal syscall boundary (byte-identical plus the gate's confirm echo, or operator-modified args freshly adjudicated), deny/timeout abort with a typed receipt carrying the closed PARK_* reason.",
      "distinction": "Not loop_constraint.go (which folds operator constraints into the live FLOOR before dispatch): loop_park.go resolves a call the gate ALREADY refused with an ESCALATE disposition, parking it for an EXTERNAL operator verdict instead of returning the deny straight to the model; and not the trajctl metaloop, which walks intents, not turns.",
      "distinct_from": [
        "loopconstraint"
      ],
      "aliases": [],
      "grounding": "loop_park",
      "grounding_kind": "symbol",
      "glossary_anchor": "docs/fak/concept-glossary.md",
      "parent": "",
      "verdict": "crystal",
      "gaps": []
    }
  ]
}
