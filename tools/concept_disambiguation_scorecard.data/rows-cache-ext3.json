{
  "_note": "Cache-family coverage retirement, batch 3. Positions the genuinely distinct cache concepts the discovery sweep surfaced (engine cache-capability seam, ablation attribution, compaction uncached-trim, Go build cache, managed-cache posture knob, canonical price-constant pin, KV-admission replay policy). Pure metric-field/inflection compounds (cachereads, cachedtok, prompt-cache hit/miss counters, TTL-upgrade inflections, ...) are retired via the cache family `ignore` list in _meta.json instead, matching the existing cachettl/cacheevent/cachecreation convention. Aliases carry the token variants that denote the same concept.",
  "rows": [
    {
      "id": "x3-cache-cachecapability",
      "canonical": "CacheCapability (wire-neutral engine cache contract)",
      "family": "cache",
      "kind": "symbol",
      "definition": "engine.CacheCapability is the wire-neutral value contract for what an upstream inference engine can expose about its cache: an opaque Engine label plus a Verdict (closed CacheVerdict class), a Provenance plane, an Evidence anchor, and a ColdPathCorrect bit. The gateway consumes it through the CacheCapabilityProducer seam so core never imports an engine-specific adapter package (cache-frontier item 32, epic #1490); gateway.ReportEngineCacheCapability is the reporter that fail-closes an unset verdict to CacheUnknown rather than reading a fabricated positive.",
      "distinction": "It is the wire-neutral capability SEAM describing what an engine's cache CAN do, not the CacheVerdict label vocabulary it carries, not the CacheEventRecorder that streams live residency events, and not the ProviderCacheVerdict the gateway derives for a provider's realized cache.",
      "distinct_from": ["engine.CacheVerdict (engine cache-capability vocabulary)", "CacheEventRecorder (engine event seam)", "ProviderCacheVerdict"],
      "aliases": ["cachecapabilityproducer", "reportenginecachecapability"],
      "grounding": "cachecapability",
      "grounding_kind": "symbol",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    },
    {
      "id": "x3-cache-cacheverdict",
      "canonical": "engine.CacheVerdict (engine cache-capability vocabulary)",
      "family": "cache",
      "kind": "symbol",
      "definition": "engine.CacheVerdict is the CLOSED vocabulary of wire-neutral engine cache-capability labels — unknown, passive-observe, active-warm, exact-evict, prefix-clone, paged-kv — the only classes a CacheCapability report may use. CacheUnknown is the zero value ('capability not established'), never inferred as a positive; CachePassiveObserve marks an engine that exposes cache symptoms (usage counters) but no control surface to act on. A new engine cannot add a class outside this list.",
      "distinction": "It is the closed LABEL vocabulary a CacheCapability carries, not the CacheCapability contract that wraps it, and not gateway's ProviderCacheVerdict which classifies a provider's realized cache rather than an engine's declared capability.",
      "distinct_from": ["CacheCapability (wire-neutral engine cache contract)", "ProviderCacheVerdict"],
      "aliases": ["cacheunknown", "cachepassiveobserve"],
      "grounding": "cacheverdict",
      "grounding_kind": "symbol",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    },
    {
      "id": "x3-cache-cacheeffect",
      "canonical": "ablate.CacheEffect (per-feature ablation attribution card)",
      "family": "cache",
      "kind": "symbol",
      "definition": "ablate.CacheEffect is the per-feature attribution card the ablation report carries: for one cache feature it records the Owner (fak vs external) and the Plane (kernel_tool_cache | local_kv | provider_prompt_cache | context_compression | context_view) so a reader can tell 'fak's own KV/tool-cache effect' apart from 'a provider/external component was merely observed'. The report's CacheEffects slice holds one card per witnessed feature.",
      "distinction": "It is the ablation-report attribution CARD that splits fak-authored cache effects from observed external ones, not the CacheEventRecorder live event stream it may draw on, nor the cachevalue P&L roll-up that totals priced savings across a session.",
      "distinct_from": ["CacheEventRecorder (engine event seam)", "cachevalue (cache P&L rollup)"],
      "aliases": ["cacheeffects"],
      "grounding": "cacheeffect",
      "grounding_kind": "symbol",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    },
    {
      "id": "x3-cache-cacheattribution",
      "canonical": "cache attribution (owner/plane per-mechanism line)",
      "family": "cache",
      "kind": "concept",
      "definition": "Cache attribution is the owner/plane labeling that tags every reuse saving with WHO authored it (Owner: fak vs provider/external) and WHICH plane produced it (kernel_tool_cache, local_kv, provider_prompt_cache, provider_prompt_cache_control, context_compression, context_view). It surfaces as the guard-info cache-attribution line (guardInfoCacheAttributionText) and the cachevalue-status plane rows, so a provider's observed cache_read is never read as fak's own witnessed reuse (epic #1490 honest per-mechanism attribution).",
      "distinction": "It is the owner/plane ATTRIBUTION labeling shared by guard-info and cachevalue-status, not the cachevalue P&L totals it annotates, nor the raw provider-cache read/savings metrics it attributes to a plane.",
      "distinct_from": ["cachevalue (cache P&L rollup)", "provider-cache read/savings metrics"],
      "aliases": ["guardinfocacheattributiontext", "kerneltoolcache", "providerpromptcachecontrol", "providercachebehavior"],
      "grounding": "cacheattribution",
      "grounding_kind": "symbol",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    },
    {
      "id": "x3-cache-uncachedtrim",
      "canonical": "uncached_trim (compaction shed of uncached spans)",
      "family": "cache",
      "kind": "subsystem",
      "definition": "uncached_trim (ablate.FeatureUncachedTrim, env FAK_ABLATE_UNCACHED_TRIM) is the compaction mechanism that sheds history spans NOT protected by a provider cache_control breakpoint — it trims only uncached tokens so the cached prefix the provider is keyed on survives intact. The ablation harness toggles it to measure its isolated savings (the UncachedTrimResults / UncachedTrimShed accounting).",
      "distinction": "It is the compaction shed that removes only UNCACHED spans to protect the keyed prefix, not a guard that refuses to drop a cached span, and not the in-kernel KV evictor that reclaims tensor spans under memory pressure.",
      "distinct_from": ["managed-cache restart plan", "KV cache", "cov5304-feature-uncached-trim"],
      "aliases": ["uncachedtrimresults", "uncachedtrimshed"],
      "grounding": "uncachedtrim",
      "grounding_kind": "symbol",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    },
    {
      "id": "x3-cache-gocache",
      "canonical": "GOCACHE (Go build cache)",
      "family": "cache",
      "kind": "config",
      "definition": "GOCACHE is the Go toolchain's on-disk build-artifact cache. internal/nightrun's preflightGoCache provisions a per-run GOCACHE under the build temp dir so a night run's compiles don't fail with 'build cache is required, but could not be located' on a HOME-less runner; export HOME or GOCACHE to keep a persistent cache across runs.",
      "distinction": "It is the Go COMPILER's build-artifact cache the night-run harness provisions, wholly unrelated to fak's runtime KV cache, the provider prompt-cache, or the cachevalue P&L — it caches object files, not tokens.",
      "distinct_from": ["KV cache", "Provider cache", "vcache"],
      "aliases": [],
      "grounding": "gocache",
      "grounding_kind": "symbol",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    },
    {
      "id": "x3-cache-managedcacheposture",
      "canonical": "managed-cache posture flag (auto|on|off)",
      "family": "cache",
      "kind": "config",
      "definition": "The managed-cache posture is the operator knob (--managed-cache / $FAK_MANAGED_CACHE, named by the fleetManagedCacheEnv const) that normalizeManagedCacheMode resolves to auto|on|off, arming ONLY the 1h-TTL member of the managed-cache family: auto stays PASSIVE on a subscription-OAuth seat and resolves ACTIVE on an API-key-billed seat; on REQUESTS the stable-prefix 1h-TTL upgrade but DEGRADES to passive on a subscription-OAuth seat (guard_managed_cache.go, since 43cbdb14a4da) so it only actually arms on an API-key-billed seat; off disables it. The rest of the managed-cache family (the 5m prompt cache, tool-prune) runs regardless of this knob. The accounts and codex launchers splice the resolved flag into `fak guard`.",
      "distinction": "It is the operator POSTURE INPUT (auto|on|off) that arms only the 1h-TTL family MEMBER - NOT the managed cache family (the umbrella, effective by default via the 5m prompt cache and tool-prune independent of this knob), NOT the managed-cache mechanism itself, and NOT guardManagedCachePosture (the resolved posture the guard session then enforces).",
      "distinct_from": ["managed cache", "guardManagedCachePosture"],
      "aliases": ["fakmanagedcache", "guardmanagedcacheauto", "normalizemanagedcachemode"],
      "grounding": "fleetmanagedcacheenv",
      "grounding_kind": "symbol",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    },
    {
      "id": "x3-cache-cacheprice",
      "canonical": "internal/cacheprice (canonical price-constant pin)",
      "family": "cache",
      "kind": "subsystem",
      "definition": "internal/cacheprice is the single source of the canonical cache price multipliers and constants that the gateway pricing model, the resume planner, and the compaction fire-gate all pin to (the #2798 acceptance), so no consumer hard-codes a divergent cache_read/cache_write coefficient and drifts the value accounting.",
      "distinction": "It is the canonical price-CONSTANTS package every consumer imports, not gateway.CachePricing (the per-turn USD cost model that applies the constants), and not CacheReadMultiplier (a single one of the coefficients it pins).",
      "distinct_from": ["CachePricing (USD cost model)", "CacheReadMultiplier (price coeff)"],
      "aliases": [],
      "grounding": "cacheprice",
      "grounding_kind": "path",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    },
    {
      "id": "x3-cache-replaykvcache",
      "canonical": "ReplayKVCache (KV-admission replay policy)",
      "family": "cache",
      "kind": "symbol",
      "definition": "ReplayKVCache is the victim-ranking KV eviction policy that internal/compute's KV-admission cost model simulates on every miss (issue #2672, epic #2236 matrix row M5) to score whether re-admitting an evicted span pays off, feeding the KVEvictPolicy victim ranking.",
      "distinction": "It is a SIMULATED victim-ranking eviction policy inside the KV-admission cost model, not the live KV cache tensor store it reasons about, and not the CacheEventRecorder that streams real residency transitions.",
      "distinct_from": ["KV cache", "CacheEventRecorder (engine event seam)"],
      "aliases": [],
      "grounding": "replaykvcache",
      "grounding_kind": "symbol",
      "glossary_anchor": "",
      "parent": "",
      "verdict": "defined",
      "gaps": []
    }
  ]
}
