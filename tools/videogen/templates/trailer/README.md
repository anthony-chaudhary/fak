# Trailer template

Keep one conversion story: problem → intervention → proof → command. Run:

```sh
go run ./tools/videogen -trailer -config tools/videogen/templates/trailer/trailer.json -verify
```

Copy the template into a project, replace its copy, and render. Review the
contact sheet at 680 px and 360 px with someone who has not read the manifest.
