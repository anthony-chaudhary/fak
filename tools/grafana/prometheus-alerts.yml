# Alert rules for the Fleet Bottleneck engine (metrics emitted by
# fleet_bottleneck.py /metrics, namespace fleet_). Mirrors the metrics-service
# alert structure: grouped by layer, severity label + summary/description.
# The generic Top-10 rules at the bottom fire for ANY class crossing a severity
# band (the title/layer travel in $labels), so new bottleneck classes are covered
# without adding a rule.
groups:
  - name: fleet_availability
    rules:
      - alert: FleetBottleneckEngineDown
        expr: up{job="fleet_bottleneck"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Fleet bottleneck engine is not scrapeable"
          description: "Prometheus cannot scrape fleet_bottleneck.py at :9095/metrics for 1m — you are flying blind. Is `python tools/fleet_bottleneck.py serve` running?"

      - alert: FleetHealthCritical
        expr: fleet_health_state >= 2
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Fleet health is CRITICAL"
          description: "fleet_health_state = {{ $value }} (2 critical / 3 down) for 1m — at least one CRITICAL bottleneck is binding throughput."

      - alert: FleetAllAccountsThrottled
        expr: fleet_accounts_throttled >= fleet_accounts and fleet_accounts > 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Every account is rate-limited"
          description: "All accounts throttled (fleet_accounts_throttled = fleet_accounts) — the whole fleet has stalled on its external ceiling. Wait for reset or add account capacity."

  - name: fleet_account_ceiling
    rules:
      - alert: FleetAuthBlocked
        expr: fleet_workers_auth_blocked > 0
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "Workers blocked on authentication"
          description: "{{ $value }} worker(s) need /login for 2m — a human must re-auth the owning account (run /login under its CLAUDE_CONFIG_DIR). That account contributes zero until then."

      - alert: FleetThrottleSaturation
        expr: fleet_bottleneck_score{id="throttle_saturation"} >= 55
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Rate-limit / throttle saturation"
          description: "throttle_saturation score {{ $value }} (layer {{ $labels.layer }}) — most in-flight workers sit on a rate-limited account. Spread work across accounts or wait for reset."

  - name: fleet_recovery
    rules:
      - alert: FleetResumeBacklog
        expr: fleet_resume_queue > 0
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "Crash-resume backlog not draining"
          description: "{{ $value }} worker(s) queued AUTO_RESUME but not recovered for 10m — confirm the resume watchdog is scheduled and in LIVE mode (DRY-RUN never resumes)."

      - alert: FleetSurfaceBacklog
        expr: fleet_surface_backlog > 5
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "Dead-crash surfacing backlog"
          description: "{{ $value }} crashed/stopped interactive worker(s) flagged for human review (action=SURFACE) for 15m — triage them (resume the keepers, close the rest)."

      - alert: FleetRecoveryStale
        expr: fleet_bottleneck_score{id="recovery_stale"} >= 55
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Recovery plumbing is stale"
          description: "recovery_stale score {{ $value }} — the supervisor/resume watchdog looks down while workers need recovery. Nothing self-heals until you restart it."

  - name: fleet_provider
    rules:
      - alert: FleetApiErrorStalls
        expr: fleet_api_error_stalls > 3
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Workers stalling on API errors"
          description: "{{ $value }} worker(s) stopped on a transient API/transport error (STOPPED_APIERR) for 5m — if climbing, the upstream API is degraded. Check provider status before adding load."

      # Companion to FleetApiErrorStalls: that rule fires on a COUNT of stalled
      # workers; this fires on the RATE (per-min) of new STOPPED_APIERR sessions. A
      # self-inflicted crash-loop — workers relaunching straight back into the same
      # transient error faster than they finish — shows as a sustained high rate even
      # while a plain 'active workers' gauge still looks busy. This is the storm a
      # bloated registry can otherwise hide (0/24 usable reading as 'saturated').
      - alert: FleetApiErrorStorm
        expr: fleet_apierr_per_min > 1
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "API-error crash-loop storm"
          description: "STOPPED_APIERR sessions are being created at {{ $value | printf \"%.1f\" }}/min for 5m — workers are dying on API errors faster than they finish (a crash-loop, not progress). Check fleet_account_apierr_recent for the storming account(s) and pause/replace them before they burn more capacity."

  - name: fleet_cost
    rules:
      - alert: FleetTokenWaste
        expr: fleet_cache_hit_ratio_median < 0.8
        for: 30m
        labels:
          severity: warning
        annotations:
          summary: "Low prompt-cache reuse (token waste)"
          description: "Median per-session cache-hit {{ $value | printf \"%.2f\" }} below 0.80 for 30m — workers are re-reading context instead of reusing the prompt cache. Inspect the worst-cache sessions."

      # Companion to FleetTokenWaste: fleet_cache_hit_ratio_median is only emitted
      # when the session audit returns non-empty. When the audit is empty (no
      # transcripts, no matching namespace, or a session_audit import failure) the
      # metric is ABSENT, not zero — so `... < 0.8` matches no series and
      # FleetTokenWaste goes silently dead in exactly the case that is hardest to
      # notice. This fires on that absence so a broken audit pass is itself an alert.
      - alert: FleetAuditEmpty
        expr: absent(fleet_cache_hit_ratio_median)
        for: 30m
        labels:
          severity: warning
        annotations:
          summary: "Fleet audit pass is producing no metrics"
          description: "fleet_cache_hit_ratio_median has been absent for 30m — the session-audit pass returned empty (no transcripts, no matching namespace, or a session_audit import failure), so FleetTokenWaste and every other audit-derived cost signal is silently dead. Confirm the audit runs and emits (python tools/session_audit.py)."

  - name: fleet_observability
    rules:
      - alert: FleetTelemetryStale
        expr: fleet_registry_age_minutes > 15
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Fleet telemetry is stale"
          description: "Session registry is {{ $value | printf \"%.0f\" }} min old — every signal reflects a fleet that may have changed. Re-run `python tools/fleet_sessions.py registry`."

      - alert: FleetSnapshotStale
        expr: (time() - fleet_snapshot_timestamp_seconds) > 600
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "Bottleneck snapshot not refreshing"
          description: "The engine last produced a snapshot {{ $value | printf \"%.0f\" }}s ago — the serve loop or cron refresh has stopped. Metrics are frozen."

  - name: fleet_top10_generic
    rules:
      - alert: FleetBottleneckCritical
        expr: fleet_bottleneck_score >= 80
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "CRITICAL bottleneck: {{ $labels.title }}"
          description: "{{ $labels.title }} ({{ $labels.layer }}) scored {{ $value }} — it is the most likely actual limiter right now. See the Grafana dashboard / `python tools/fleet_bottleneck.py report` for the fix."

      - alert: FleetBottleneckHigh
        expr: fleet_bottleneck_score >= 55
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "HIGH bottleneck: {{ $labels.title }}"
          description: "{{ $labels.title }} ({{ $labels.layer }}) scored {{ $value }} for 5m. Check the ranked card for the recommended fix."

  - name: fak_gateway_availability
    rules:
      - alert: FakGatewayDown
        # `up == 0` is the authoritative "process gone" signal — critical/2m to match
        # FleetBottleneckEngineDown. (A self-report `fak_gateway_up != 1` rule was removed:
        # the gateway emits fak_gateway_up as a constant 1, so when it is alive the metric
        # is 1 and when it is dead the series is absent — `!= 1` can never fire. This `up`
        # scrape check is the real down-detector; do not replace it with a self-report.)
        expr: up{job="fak_gateway"} == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "fak gateway metrics are not scrapeable"
          description: "Prometheus cannot scrape fak serve at :8080/metrics for 2m. If the gateway is expected to be live, start `fak serve` or check bearer-auth scrape credentials."

  - name: fak_gateway_request_path
    rules:
      - alert: FakGateway5xx
        expr: sum(rate(fak_gateway_http_requests_total{status=~"5.."}[5m])) > 0
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "fak gateway is returning 5xx responses"
          description: "Gateway 5xx response rate is {{ $value | printf \"%.3f\" }}/s for 2m. Check upstream model errors and gateway logs."

      - alert: FakGatewayHighHTTPP95
        expr: histogram_quantile(0.95, sum by (le) (rate(fak_gateway_http_request_duration_seconds_bucket[5m]))) > 2
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "fak gateway HTTP p95 latency is high"
          description: "Gateway HTTP p95 latency is {{ $value | printf \"%.2f\" }}s for 5m. Split by route in the FAK Gateway Observability dashboard."

      - alert: FakGatewayHighInflight
        expr: fak_gateway_inflight_requests > 50
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "fak gateway has high inflight request count"
          description: "{{ $value }} gateway requests have been inflight for 5m. Check upstream model latency, clients, or stuck connections."

      # Fires WHILE a request is still running, not after it completes. The HTTP
      # p95/duration histograms only observe a request once it returns, so a single
      # request wedged on a slow or hung upstream is invisible to them (and to the
      # count-based HighInflight rule) until it finally finishes -- or never, if it
      # hangs. fak_gateway_inflight_max_age_seconds is computed at scrape time from
      # the set of in-flight requests, so this is the live "a request is stuck right
      # now" signal. 120s is well past a healthy upstream round-trip.
      - alert: FakGatewayRequestStuck
        expr: fak_gateway_inflight_max_age_seconds > 120
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "fak gateway has a request stuck in flight"
          description: "The oldest in-flight gateway request has been running for {{ $value | printf \"%.0f\" }}s (still executing). Likely a slow or hung upstream model call or a stuck client connection -- inspect gateway logs and the upstream provider."

  - name: fak_gateway_kernel_path
    rules:
      - alert: FakGatewayDenyRatioHigh
        expr: sum(rate(fak_gateway_operations_total{verdict="DENY"}[5m])) / clamp_min(sum(rate(fak_gateway_operations_total[5m])), 0.001) > 0.5
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "fak gateway deny ratio is high"
          description: "More than half of gateway kernel operations are DENY verdicts for 10m. This may be expected during an attack test; otherwise inspect client/tool drift."

      - alert: FakGatewayQuarantineObserved
        expr: increase(fak_kernel_quarantines_total[5m]) > 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "fak gateway quarantined a result"
          description: "{{ $value }} result quarantine(s) observed in 5m. Treat this as a trust-floor event and inspect the trace/client that supplied the result."

      - alert: FakGatewayHighOperationP95
        expr: histogram_quantile(0.95, sum by (le) (rate(fak_gateway_operation_duration_seconds_bucket[5m]))) > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "fak gateway kernel operation p95 latency is high"
          description: "Gateway syscall/adjudicate/admit p95 latency is {{ $value | printf \"%.2f\" }}s for 5m. Split by operation in the dashboard."

  # ---------------------------------------------------------------------------
  # Fleet sessions (`fak fleet metrics`, scrape job fak_fleet). These watch the
  # per-SESSION tier no other job carries: the gateway rules above describe one
  # process, and the fleet_* rules describe aggregate scores. A wedged session is
  # invisible to both.
  - name: fak_fleet_sessions
    rules:
      - alert: FakFleetExporterDown
        expr: up{job="fak_fleet"} == 0
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "fak fleet exporter is not scrapeable"
          description: "The fak_fleet job has been down for 5m, so every fleet-session panel is blank. Start it with `fak fleet metrics --serve --addr 127.0.0.1:9098`. This is an OBSERVABILITY outage, not a fleet outage -- the fleet may be perfectly healthy and unwatched."

      # The load-bearing fleet alert. A STALLED session claims a running state while
      # its durable heartbeat has stopped advancing: it holds a seat, a lane lease and
      # an account slot while producing nothing. `for: 15m` is well past the 5m stale
      # window the exporter classifies on, so a session merely between turns at the
      # moment of one scrape cannot trip it.
      - alert: FakFleetSessionsStalled
        expr: fak_fleet_sessions_by_liveness{liveness="stalled"} > 0
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "fak fleet has stalled sessions"
          description: "{{ $value }} session(s) claim a running state but their heartbeat has not advanced for 15m+. Open the fleet overview (uid fak-fleet-overview), find them in the live-session table, and drill down. `fak fleet monitor` classifies them from process evidence; `fak fleet replace --session S` renders a replacement."

      # Every session stalled at once is a different failure from some sessions
      # stalling: it points at the harness, the account ceiling or the host, not at
      # one wedged agent.
      - alert: FakFleetAllSessionsStalled
        expr: fak_fleet_sessions > 0 and fak_fleet_sessions_by_liveness{liveness="stalled"} == fak_fleet_sessions
        for: 10m
        labels:
          severity: critical
        annotations:
          summary: "every fak fleet session is stalled"
          description: "All {{ $value }} session(s) in the durable inventory are stalled for 10m+. Suspect a fleet-wide cause (account ceiling, harness wall, host wedge) rather than a per-session one; check `fak fleet capacity` and the account chips first."

      # The registry read failing is NOT the same as an empty fleet, and the two look
      # identical on every other panel -- which is the whole reason the exporter emits
      # this gauge separately.
      - alert: FakFleetRegistryUnreadable
        expr: fak_fleet_registry_readable == 0
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "fak fleet exporter cannot read the durable session registry"
          description: "The exporter is up but could not read the session registry for 10m, so the live panels show zero sessions while knowing nothing. Do NOT read the fleet as idle. Check the registry path and its permissions."

      # A truncated per-session tier renders a table that LOOKS complete. Alerting on
      # the confession is the only way an operator learns the view is partial.
      - alert: FakFleetSessionSeriesTruncated
        expr: fak_fleet_sessions_truncated > 0
        for: 30m
        labels:
          severity: info
        annotations:
          summary: "fak fleet per-session panels are truncated"
          description: "{{ $value }} session(s) are outside the exporter's --max-sessions cardinality bound, so the live-session table is INCOMPLETE. Either the fleet outgrew the default bound (raise --max-sessions) or the durable registry is accumulating dead rows and needs collecting."

      # The historical drill-down is only as good as its join key. Sustained zero
      # coverage means every per-session cost panel is empty while the fleet totals
      # look fine -- a silent hole this makes loud.
      - alert: FakFleetDrilldownCoverageZero
        expr: fak_fleet_usage_sessions_identified == 0 and fak_fleet_usage_rows > 0
        for: 1h
        labels:
          severity: info
        annotations:
          summary: "no fak fleet usage row carries a session id"
          description: "The gateway-usage ledger has {{ $value }} folded rows but none carries a session_id, so every per-session cost/cache panel is blank while the fleet roll-ups are populated. `fak serve` rows are unidentified by design (one process, many sessions); a fleet of `fak guard` sessions should be producing identified rows."

  # ---------------------------------------------------------------------------
  # Fak Ops plane (exporter `fak-ops-dashboard`, scrape job fak_ops, :9096; and
  # `fak-sync ops throughput-metrics`, scrape job fak_ops_workers, :9094).
  # These enforce the repo's own fail-closed doctrine for the autonomous
  # factory: a skipped plane, an unreadable state file, a stale routine and a
  # degraded stuck-join are all DANGEROUS WHEN SILENT, because every ops panel
  # renders a healthy-looking number (often zero) in exactly those cases. Each
  # rule below therefore fires on the CONFESSION metric (up, *_partial,
  # *_present, truncated, *_plane_up), never on a coincidence of zeros.
  #
  # Metric-name note: the two exporters are deliberately name-disjoint. The ops
  # dashboard owns fak_ops_schedule_enabled/stale; the workers exporter emits
  # fak_ops_workers_routines_enabled/stale instead (see
  # cmd/fak-sync/ops_throughput_metrics.go) so a cross-job selector can never
  # silently sum both. The rules below always name the job explicitly.
  - name: fak_ops_availability
    rules:
      # `up == 0` is the authoritative "process gone" signal, matching
      # FleetBottleneckEngineDown / FakGatewayDown. A dead ops exporter is an
      # OBSERVABILITY outage: the factory may be running perfectly and be
      # completely unwatched.
      - alert: FakOpsExporterDown
        expr: up{job="fak_ops"} == 0
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "fak ops dashboard exporter is not scrapeable"
          description: "Prometheus cannot scrape fak-ops-dashboard at :9096/metrics for 5m, so every Ops Plane Statuses panel is blank. This is an observability outage, not necessarily an ops outage. Start it with `fak-ops-dashboard --addr 0.0.0.0:9096 --repo-root .` or `fak-obs-stack check`."

      - alert: FakOpsWorkersExporterDown
        expr: up{job="fak_ops_workers"} == 0
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "fak ops workers/throughput exporter is not scrapeable"
          description: "Prometheus cannot scrape `fak-sync ops throughput-metrics` at :9094/metrics for 5m, so workers, queue depth and issue-throughput panels are blank. Start it with `fak-sync ops throughput-metrics --serve --addr 0.0.0.0:9094`."

      # The exporter folds per scrape; a served snapshot older than the refresh
      # interval means the content routes show a stale fold while `up` stays 1.
      # Without this rule a frozen fold is invisible.
      - alert: FakOpsSnapshotStale
        expr: fak_ops_up == 1 and fak_ops_snapshot_age_seconds > 300
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "fak ops dashboard is serving a stale snapshot"
          description: "The ops exporter responds but its cached snapshot is {{ $value | printf \"%.0f\" }}s old for 5m (the refresh interval is far shorter). The background fold is wedged, so every panel is a frozen past. Check for a stuck stuck-work probe and restart the exporter."

  - name: fak_ops_degradation
    rules:
      # fak_ops_partial / fak_ops_stuck_partial are the exporter's own
      # confession that a plane FAILED. A failed plane is not a zero, and the
      # dashboard cannot tell the two apart on its own -- this makes it loud.
      - alert: FakOpsPlaneDegraded
        expr: fak_ops_partial == 1 or fak_ops_stuck_partial == 1
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "fak ops is folding a degraded/partial snapshot"
          description: "The ops exporter reports partial=true (or stuck_partial=true) for 10m: at least one plane failed or was deliberately skipped, so the counts understate work. Read the named diagnostics on the exporter `/status` page and `skipped_planes` -- do NOT read the numbers as zero."

      # A failed stuck plane means an entire class of work is UNCOUNTED while the
      # Stuck total looks calm.
      - alert: FakOpsStuckPlaneFailed
        expr: fak_ops_stuck_planes_failed > 0
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "fak ops stuck-work join has a failed plane"
          description: "{{ $value }} stuck-work plane(s) failed for 10m, so stuck work in those planes is invisible and the stuck total is a LOWER BOUND. Inspect `fak-sync ops stuck` and the exporter diagnostics."

      # An unreadable state file and an idle queue look identical on every other
      # panel (both render zero items), which is exactly why the exporter emits
      # this presence gauge separately.
      - alert: FakOpsStateUnreadable
        expr: fak_ops_up == 1 and fak_ops_state_present == 0
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "fak ops cannot read its own state file"
          description: "The ops exporter is up but `.fak/ops.json` has been unreadable for 15m, so routines/queue/intervals read as empty while knowing nothing. Do NOT read the factory as idle. Check the state path and its permissions."

      - alert: FakOpsReportsLedgerMissing
        expr: fak_ops_up == 1 and fak_ops_reports_ledger_present == 0
        for: 1h
        labels:
          severity: info
        annotations:
          summary: "fak ops report ledger is absent"
          description: "The ops exporter has been up for 1h but `.fak/ops-reports.jsonl` is absent, so every reports-window panel is empty by construction rather than by inactivity. Confirm the ops runs are writing their report ledger."

      - alert: FakOpsGoalRegistryMissing
        expr: fak_ops_up == 1 and fak_ops_canonical_goal_registry_present == 0
        for: 1h
        labels:
          severity: info
        annotations:
          summary: "fak canonical goal registry is unreadable"
          description: "The ops exporter cannot read the canonical fak goal registry for 1h, so goal-lifecycle and goal-closure panels are blank while the registry may simply be empty. A missing registry is not a closed goal."

      # The failure-rate series is deliberately OMITTED when the window holds no
      # terminal result, so a plain `> X` rule cannot see "no data at all".
      # This uses the same absent() idiom as FleetAuditEmpty to catch the silent
      # case.
      - alert: FakOpsReportsWindowEmpty
        expr: fak_ops_up == 1 and absent(fak_ops_reports_failure_rate)
        for: 1h
        labels:
          severity: info
        annotations:
          summary: "fak ops has no terminal report results in the window"
          description: "The ops report window has contained no ok/failed interval for 1h (the exporter omits the failure rate rather than emitting zero). Either ops is not running or its reports are not landing -- check `fak-sync ops status` and the scheduler cadence."

  - name: fak_ops_health
    rules:
      # A stale routine is enabled but not firing: the scheduler believes it is
      # live while it produces nothing.
      - alert: FakOpsRoutinesStale
        expr: fak_ops_schedule_stale > 0
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "fak ops has stale enabled routines"
          description: "{{ $value }} enabled ops routine(s) are past the stale threshold for 15m -- they are scheduled but not firing. Re-arm them with `fak-sync ops routine-rearm` or inspect `fak-sync ops status`."

      - alert: FakOpsWorkersRoutinesStale
        expr: fak_ops_workers_routines_stale > 0
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "fak ops workers view reports stale routines"
          description: "The workers exporter counts {{ $value }} stale enabled routine(s) for 15m. See `fak-sync ops status`; re-arm with `fak-sync ops routine-rearm`."

      # A persistently failing factory is the ops-plane equivalent of a red
      # build: work is being admitted and thrown away.
      - alert: FakOpsHighFailureRate
        expr: fak_ops_reports_failure_rate > 0.25
        for: 30m
        labels:
          severity: warning
        annotations:
          summary: "fak ops report failure rate is high"
          description: "{{ $value | printf \"%.2f\" }} of terminal ops intervals in the window failed for 30m. Inspect `fak-sync ops failure-catalog` and the failing routine's recent intervals."

      # The goal-closure SLO is the factory's throughput truth; it had zero
      # alerting before this group.
      - alert: FakOpsGoalClosureLow
        expr: fak_ops_canonical_goal_health{status="LOW"} == 1
        for: 1h
        labels:
          severity: warning
        annotations:
          summary: "fak canonical goal closure rate is below floor"
          description: "Independently witnessed goal closures have been below the configured floor for 1h. The factory is producing work but not closing goals -- check for work accumulating in-flight: `fak-flow wip` and `fak-flow cycle`."

      # Backlog growth is the slowest-moving and most important ops signal:
      # closed-per-hour below opened-per-hour means the factory is losing.
      - alert: FakOpsIssueBacklogGrowing
        expr: fak_ops_issues_net_per_hour{window="12h"} > 0
        for: 2h
        labels:
          severity: info
        annotations:
          summary: "fak issue backlog is growing"
          description: "Issues are being opened faster than closed over the trailing 12h window (net {{ $value | printf \"%.1f\" }}/h) for 2h. This is a throughput trend, not an incident; see the Workers & Issue Throughput dashboard (uid fak-ops-workers-queue)."

      # A truncated census renders a table that LOOKS complete. Alerting on the
      # confession is the only way an operator learns the throughput view is a
      # lower bound.
      - alert: FakOpsThroughputCensusTruncated
        expr: fak_ops_throughput_census_truncated > 0
        for: 30m
        labels:
          severity: info
        annotations:
          summary: "fak ops issue-throughput census is truncated"
          description: "The GitHub issue census is truncated ({{ $value }} items outside the bound) for 30m, so closure counts are a LOWER BOUND. Raise the census bound or narrow the window."

      # A plane-readiness gauge at 0 means the worktree/session census could not
      # be read, which is different from zero live workers.
      - alert: FakOpsWorktreePlaneDown
        expr: fak_ops_worktree_plane_up == 0 or fak_ops_sessions_plane_up == 0
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "fak ops worktree/session plane is unreadable"
          description: "The live worktree or session census plane has been down for 15m, so live-worker and at-risk-worktree counts are unknown, not zero. Check the worktree root and git availability on the exporting host."
