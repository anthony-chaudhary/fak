<?xml version="1.0" encoding="UTF-8"?>
<Task version="1.2" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <Date>2026-07-14T20:36:53</Date>
    <Author>fak-fleet</Author>
    <URI>\FakBenchmarkFleetLoop</URI>
  </RegistrationInfo>
  <Principals>
    <Principal id="Author">
      <UserId>%FLEET_TASK_USER_SID%</UserId>
      <LogonType>InteractiveToken</LogonType>
    </Principal>
  </Principals>
  <Settings>
    <DisallowStartIfOnBatteries>true</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>true</StopIfGoingOnBatteries>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <!-- Disabled until one real node yields a witnessed numeric benchmark (#6503).
         `fak bench-loop install` refuses to arm this schedule until the durable
         queue holds such a measurement, so re-enabling by hand skips that gate. -->
    <Enabled>false</Enabled>
    <IdleSettings>
      <Duration>PT10M</Duration>
      <WaitTimeout>PT1H</WaitTimeout>
      <StopOnIdleEnd>true</StopOnIdleEnd>
      <RestartOnIdle>false</RestartOnIdle>
    </IdleSettings>
  </Settings>
  <Triggers>
    <TimeTrigger>
      <StartBoundary>2026-07-14T20:36:00</StartBoundary>
      <Repetition>
        <Interval>PT15M</Interval>
      </Repetition>
    </TimeTrigger>
  </Triggers>
  <!-- The action runs the committed tick payload directly. conhost.exe --headless
       returned its own status and hid the dispatcher's, which is how a loop with zero
       successful witnesses reported Last Result 0 for weeks (#6503). -->
  <Actions Context="Author">
    <Exec>
      <Command>cmd.exe</Command>
      <Arguments>/d /s /c ""%FAK_WORKSPACE%\tools\scheduled-tasks\fak-bench-fleet-tick.cmd" "%FAK_BIN%" "%FAK_WORKSPACE%""</Arguments>
    </Exec>
  </Actions>
</Task>
