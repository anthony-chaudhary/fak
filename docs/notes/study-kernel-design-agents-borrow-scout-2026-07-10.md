---
title: "Borrow scout: kernel-design-agents (KDA orchestration + ncu-report-skill) → fak — 2 filed, rest present/deduped"
description: >-
  A /study-repo pass over mit-han-lab/kernel-design-agents @ dda6be3c — the
  orchestration WORKFLOW (not the KernelWiki KB, already mined as epic #3946).
  Witnessed the evidence-discipline axes of the KDA loop + its vendored
  ncu-report-skill against fak's dispatch/RSI/witness machinery. Net: 2
  ship-alone borrows filed (#4100 parent-linked rejected-candidate genealogy;
  #4101 instrument-before-optimize perf-claim witness rung); the rest witnessed
  PRESENT (shipgate metric-and-suite-and-truth; plan-mode + dos-plan-price;
  reference/workspace split) or already filed (epic #3946 family, #2550, #3925).
---

# Borrow scout: kernel-design-agents → fak (2026-07-10)

## What was studied

`mit-han-lab/kernel-design-agents` @ `dda6be3c` — the KDA **orchestration
workflow** (a global task-list orchestrator driving stateless sub-agent workers over
shared files), which vendors two submodules:

- `mit-han-lab/KernelWiki` @ `2777d18f` — the curated GPU-kernel knowledge base.
- `DongyunZou/ncu-report-skill` @ `1cf238d6` — the profiling-report skill.

Borrow level throughout: **inspire** (Python/markdown methodology → Go
clean-room). No bytes vendored. The primary `WebFetch` of the repo's raw doc
files was guard-refused (`TRUST_VIOLATION`, already filed as **#3936**); the
landing page + skill READMEs were readable and sufficient for the axes below.

## The load-bearing dedup

The **KernelWiki KB** half of this repo was already mined by the 2026-07-10
`/study-repo` pass in [`docs/notes/2026-07-10-kernelwiki-study.md`](2026-07-10-kernelwiki-study.md),
which filed **epic #3946** with children #3947–#3951 and deduped alias-retrieval
into #3925. So every KB-hygiene axis is **already covered** and was NOT re-filed:

| KDA/KernelWiki axis | Already filed |
|---|---|
| Confidence-tier VERIFIED gate + reproducibility floor | #3949 |
| Version-pinning / cutoff SoT / tool-version time-decay | #3950 |
| Immutable source layer + typed provenance taxonomy + byte re-check | #3951 |
| Committed decision-ledger + skip-audit (inbound triage) | #3947 |
| Frontmatter-derived indices + citation-graph gate | #3948 |
| Alias/synonym-aware retrieval (kills false-ABSENT) | #3925 |
| Six-field numeric perf schema on the **CLAIMS.md** surface | #3949 (rung) |

This pass studied only what #3946 did **not**: the **orchestration loop** and
the **ncu evidence arm**.

## Witness ledger (orchestration + ncu axes)

Each candidate dogfooded `fak_feature_query` + a raw grep of the named seam.

| # | Axis | Verdict | Evidence |
|---|---|---|---|
| B-A | Rejected attempt = first-class artifact with structured reason **+ parent/derived-from lineage** (queryable attempt genealogy) | **PARTIAL → filed #4100** | `internal/knownbad` ledger is flat (`{reason,trees,ttl,by}`, no parent edge) + manually recorded; `rsiloop`/`shipgate` *counts* non-keeps (escalation breaker) but doesn't persist the rejected variant+reason+parent. The "parent" symbols in `rsiloop` are KV-cache **fork** lineage (`cacheparity.go`), not attempt lineage. |
| B-D | Perf claim inadmissible without a collected measurement witness naming metric values ("specificity is the deliverable") | **ABSENT on the commit seam → filed #4101** | `dos_commit_audit` grades claim-kind vs diff **shape**, explicitly not perf; `benchauthority.Claim` is free-text `Headline`, `Validate()` unimplemented. Distinct surface from #3949 (which schematizes perf on the **CLAIMS.md** ledger, not on commits). |
| B-B | Two-gate task contract: correctness oracle ⟂ target-metric oracle, both pre-registered; parent accepts witnessed score not self-report | **PRESENT** | Shipgate keep-bit = **strict metric gain AND suite-green AND truth-clean** (three pre-defined gates); `fak knownbad resolve` is witness-gated (`go test`/`dos verify`); the dispatch objective+scorer contract + witnessed acceptance + scorer-less refusal is **already filed as #2550** (epic #2533). |
| B-C | No implementation until a draft/plan with ranked candidate directions exists; baseline validated first | **PRESENT** | fak plan-mode (`EnterPlanMode`) + `dos-plan-price` (price a fan-out before launch) + `plan-audit`. |
| B-F | Reusable method kept physically separate from mutable per-run artifacts (method can't overfit one run; generated bytes never pollute the reference) | **PRESENT** | `.claude/skills` (method) vs gitignored scratchpad + DOS lane artifacts (per-run); CLAUDE.md task-agnostic rules. |
| — | ROI-ranked candidate selection (Est. Speedup × effort) | folded as a **rung of #4101** | Not ship-alone for fak; also overlaps `#3952` (cache-budget ROI knee) / score-signal machinery. |

## Filed this pass

- **#4100** — `feat(knownbad)`: parent-linked rejected-candidate genealogy for
  the RSI/dispatch attempt loop (add `derived_from` to the `knownbad` record;
  auto-record shipgate reverts as structured rejected rows; `match` surfaces the
  lineage). Milestone: 10x agentic loop with witnessed self-correction.
- **#4101** — `feat(commit-audit)`: instrument-before-optimize — a perf-claim
  witness rung in the `dos_commit_audit`/`dos_review` family, backed by finally
  implementing `benchauthority.Validate()`. Milestone: Win the benchmarks.

## Completeness-critic disclosure

- **Not exhaustively read:** KDA's per-agent prompt templates and its watchdog /
  mid-run-kill logic (fak's watchdog is out of scope for #2550 by its own
  statement, and mid-run kill is tracked elsewhere) — no borrow claimed there.
- **Guard-refused surface:** the raw markdown of the KDA docs (#3936); axes were
  witnessed from the landing page + skill READMEs, so a deeper agent-flow rung
  could exist unseen. If #3936 is fixed, a re-read is cheap and worth one pass.
- **Deliberately not filed:** B-B/B-C/B-F (PRESENT); ROI-ranking as a standalone
  (folded into #4101) — re-filing a witnessed-PRESENT axis is the exact failure
  the witness step exists to prevent.

Sibling scouts: [KernelWiki](2026-07-10-kernelwiki-study.md) · [SDPO](BORROW-SDPO-OFFPOLICY-STALENESS-2026-07-10.md) · [tair-kvcache](tair-kvcache-borrow-study-2026-07-10.md) · [Puppetmaster](study-puppetmaster-borrow-scout-2026-07-10.md).
