---
title: "fak implicit-explicit scorecard - is every relied-on concept directly named"
description: "Inward naming scorecard: detects concepts that are assumed or hinted at but never directly named (hedged prose, magic literals, code-only identifiers, doc-only headings) and drives naming them. Two driven numbers: coverage of the discovered implicit-signal space and implicitness-debt."
---

# Implicit-explicit scorecard - naming what the system only assumes

<!-- implicit-explicit-scorecard: 2026-07-05 positioning sweep - process: tools/implicit_explicit_scorecard.py - data: tools/implicit_explicit_scorecard.data/ -->

The sibling [concept-disambiguation scorecard](../concept-disambiguation-scorecard/) grades the names that EXIST - of the similar-sounding names, is each distinct concept crystal-clear? This card asks the question that comes BEFORE it: **does each concept the system relies on have a direct name at all, or is it merely implicit - assumed by convention, hedged in prose ("the so-called warm window"), encoded as a magic literal repeated across files, named for the compiler but invisible in the docs, or named in prose with no code symbol behind it?** Every number below is re-derived by `tools/implicit_explicit_scorecard.py` and cross-checked against the real tree (the evidence must appear; a claimed named_symbol must resolve; a doc_anchor must exist). No verdict is hand-typed.

> Regenerate: `python tools/implicit_explicit_scorecard.py --markdown-dir docs/implicit-explicit-scorecard`.

## Headline

| Metric | Value |
|---|---|
| **Score** | **100.0/100** (grade A) = 10.0/10 |
| **Coverage** | **100.0%** (204/204 implicit-concept signals positioned) |
| **Implicitness-debt** | **0** (naming 0 + coverage 0) |
| Explicit concepts | 52 of 247 positioned |
| As of | 2026-07-05 (fak dev) |

> **Read this right.** The score is deliberately LOW at birth: it grades the WHOLE implicit-signal space discovered in the tree, not the few concepts already catalogued. A low coverage number is the honest statement that most assumed/hinted concepts are not yet named - which is exactly the debt this scorecard exists to retire.

## The explicitness ladder

| Verdict | Means |
|---|---|
| * explicit | a code identifier resolves AND a definition is written at a doc anchor that exists - named in both worlds |
| c named-code | a real identifier exists, but no written definition anywhere |
| d named-doc | defined in prose at a real anchor, but no code symbol behind it |
| ~ hinted | only hedges/patterns refer to it, but a proposed_name exists - naming planned |
| . latent | pure pattern; no name anywhere, no plan yet |

## Standing at a glance

```text
implicit-explicit chart - 247 concepts - score 100.0/100 (grade A) - implicitness-debt 0

explicitness ladder (count of concepts, named -> fog):
  * explicit     #########................... 52
  c named-code   ##.......................... 11
  d named-doc    ###......................... 18
  ~ hinted       ############################ 166
  . latent       ............................ 0

explicitness mix by signal (each cell = one concept):
  code-only       *************************************************** (51 concept(s); 51 explicit)
  doc-only        ddddddddddddddddd~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ (152 concept(s); 0 explicit)
  hinted          d                    (1 concept(s); 0 explicit)
  latent-literal  *ccccccccccc~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ (43 concept(s); 1 explicit)

coverage by signal kind (positioned / discovered):
  code-only       ############################ 2/2
  doc-only        ############################ 157/157
  hinted          ############################ 1/1
  latent-literal  ############################ 44/44

signal coverage  [################################] 100.0%  (204/204 implicit signals positioned)

legend: * explicit   c named-code   d named-doc   ~ hinted   . latent
```

## The concepts (most explicit first)

| | Verdict | Signal | Canonical - definition | Proposed / named |
|---|---|---|---|---|
| * | explicit | code-only | **.env.slack.local key resolver wrappers** - Ten per-package one-line wrappers (benchpost, blockerpost, cachevaluepost, dispatchpost, dojopost, marketing, milestonepost, nodeusagepost, ...) all delegating to slackenv.FileValue to read a key from .env.slack.local walked up from the cwd - the byte-identical per-package walk-up they replaced is gone; 12 production files, no doc mentions it. | `envFileValue` |
| * | explicit | code-only | **HTTP method guard** - internal/gateway/http.go helper enforcing a single allowed HTTP method on a handler entry: on mismatch it writes the standard 405 ('use <METHOD>') and returns false so the caller returns immediately; used across 19 gateway files; no doc mentions it. | `requireMethod` |
| * | explicit | code-only | **MoE expert tensor name builder** - internal/model/moe.go: builds the GGUF tensor name for one MoE expert ('mlp.experts.<i>.<suffix>' through layerName); referenced in 12 internal/model files plus a ggufload comment; no doc mentions it. | `expertName` |
| * | explicit | code-only | **Q8_0 activation vector quantizer** - Q8_0 activation-vector quantizer declared three times: internal/compute/cpuref.go (scalar reference form returning q/d slices) and internal/model (quant.go function plus a (*Session) method in quant_forward.go, both filling a reusable q8Vec via quantizeVecQ8Into); 15 production files, no doc mentions it. | `quantizeVecQ8` |
| * | explicit | code-only | **RealRunner test seam** - the repo-wide convention of a RealRunner function - the production process-runner injected where tests substitute a fake - independently re-declared in mergepreview, modver, releasestale and more (19 files), never documented as the convention it is | `RealRunner` |
| * | explicit | code-only | **abi verdict-kind name mapper** - A switch mapping abi.VerdictKind to its ALLOW/DENY/TRANSFORM/QUARANTINE/... name, declared five times unexported (internal/agent, internal/journal, internal/trajectory, cmd/ctxbench, cmd/fak) and twice exported as VerdictName (agentdemo, browseraction); 13 production files, no doc mentions it. | `verdictName` |
| * | explicit | code-only | **any-substring matcher** - internal/strmatch.ContainsAny is the one shared variadic any-substring matcher that replaced six per-package containsAny copies (slop de-dup #776); of the 41 production files matching the name, 35 call stdlib strings.ContainsAny and only 6 call strmatch; no doc mentions it. | `ContainsAny` |
| * | explicit | code-only | **background-command window gate** - the internal/windowgate helper every spawned subprocess passes through so background commands never flash a console window on Windows (no-op elsewhere) - called from 77 production files, invisible in the docs | `ConfigureBackgroundCommand` |
| * | explicit | code-only | **best-effort JSONL ledger reader** - Convention declared in five internal packages (cachevalueledger, experiments, gatewayusageledger, nightrun, trajctl): read a JSONL ledger file, returning nil rows when absent, and parse via the package's tolerant parser; cmd/fak/ledgerio.go adds the generic readLedgerFile[T] seam (#1437); 18 production files, no doc mentions it. | `ReadLedgerFile` |
| * | explicit | code-only | **byte-count total field convention** - Recurring struct-field name for an accumulated byte total, re-declared across internal/agent, internal/compute, internal/gateway (three structs), internal/memgate and internal/model profiles, plus the unexported (StageCost).totalBytes() sum method and gateway metrics counters; 15 production files, no doc mentions it. | `TotalBytes` |
| * | explicit | code-only | **cache admission verdict** - the cachemeta verdict string recording whether an entry was admitted to the cache and why (AdmissionFromVerdict maps abi verdict kinds onto it) - in 12 production files, never mentioned in the cache docs | `AdmissionVerdict` |
| * | explicit | code-only | **canonical bench artifact encoding** - internal/turnbench/helpers.go: the canonical indented-JSON-plus-trailing-newline artifact encoding shared by every report/sweep JSON() method in the package (the MarshalIndent error is deliberately dropped - the reports are JSON-encodable by construction); 16 turnbench files, no doc mentions it. | `marshalArtifact` |
| * | explicit | code-only | **closed-vocabulary json.Marshaler impls** - Repo-declared json.Marshaler implementations: six MarshalJSON methods rendering closed-vocabulary or wire-shaped types as strings/objects (codelint.Severity, policy.Outcome, eveparity.CheckKind, fleetaccounts.Account, gardenbundle, ablate.AblationRun) plus the memorystability.MarshalJSON function; 18 production files, no doc mentions the pattern. | `MarshalJSON` |
| * | explicit | code-only | **cmd JSON-output exit helper** - One shared cmd/fak helper (cmd/fak/jsonout.go): writes v as indented JSON to stdout returning exit code 0, or prints '<label>: encode json: <err>' to stderr and returns 1; used across 55 cmd/fak files, mentioned in no doc. | `encodeJSONOrFail` |
| * | explicit | code-only | **cmd root-dir resolution** - One unexported cmd/fak helper (cmd/fak/hooks.go): returns the explicit --root, else the `git rev-parse --show-toplevel` output, else empty; used in 23 files of the cmd/fak package; no doc mentions it. | `resolveRoot` |
| * | explicit | code-only | **durability-class normalizer** - Declared twice (internal/ctxplan/store.go and internal/memq/memq.go): maps any durability-class string into the canonical vocabulary, failing closed to the turn class for missing or unknown values; 14 production files, no doc mentions it. | `NormDurability` |
| * | explicit | code-only | **evidence ref** - the struct that points a witness at an artifact it can read back (a file path, a git object) - the unit of witnessable evidence in taskmgr/loopmgr, in 17 production files and zero docs despite the witness pipeline being a headline concept | `EvidenceRef` |
| * | explicit | code-only | **fak group-verb subcommand router** - cmd/fak/subdispatch.go: the shared `fak <group> <verb>` router - a missing or unknown verb prints the human hint list and exits 2, otherwise it os.Exits with the matching handler's return code; used in 15 cmd/fak files; no doc mentions it. | `dispatchSubcommands` |
| * | explicit | code-only | **first non-empty string pick** - Per-package helper convention: func firstNonEmpty(...) string returning the first non-blank argument, declared 31 separate times (e.g. internal/benchscore/benchscore.go, cmd/fak/claude_mac_fak.go) and present in 57 production files; no doc mentions it. | `firstNonEmpty` |
| * | explicit | code-only | **first-usable-string pick** - Two unrelated helpers share the name: cmd/fak/dispatch_tick.go (first non-blank of variadic strings, with sibling firstInt) and internal/fleetmon/transcript.go (first string value among named keys of a raw JSON object); 12 production files, no doc mentions it. | `firstString` |
| * | explicit | code-only | **gateway synchronous bind-and-serve** - The repo declares ListenAndServe once: (*gateway.Server).ListenAndServe (internal/gateway/http.go) binds addr synchronously - measured as the listener-bind boot phase and failing before MarkReady - then serves, warning when a no-auth gateway binds beyond loopback; other files in the 12-file count call the stdlib method; no doc mentions it. | `ListenAndServe` |
| * | explicit | code-only | **gh existing-issue fetcher convention** - Convention declared in five packages (dogfoodissues, guardcomplaint, issuecatalog, learningdebt, maturity) plus ideascout's LiveFetcher method: shell out to `gh issue list --json ...` to fetch existing issues so a sync can classify create-vs-update; 13 production files, no doc mentions it. | `FetchExistingIssues` |
| * | explicit | code-only | **git-tracked tree snapshot for gates** - internal/hooks/tree.go: the whole `git ls-files -z` tracked-path set read once and shared across every hygiene gate, with a lazy per-file read cache - the tree-mode counterpart of StagedDiff (gate logic is identical, only the input set differs); 12 production files, no doc mentions it. | `TrackedTree` |
| * | explicit | code-only | **in-place RoPE rotation of q/k rows** - internal/model/kv.go: applies RoPE rotation in place to each of the nH query head rows and nKV key head rows for one position, given cos/sin tables and head dim hd (the pre-RoPE Kraw stash is deliberately the caller's job); referenced in 15 internal/model files; no doc mentions it. | `ropeRowQKInto` |
| * | explicit | code-only | **indented-JSON emitter** - the helper (declared per package, 59 production files) that emits the canonical two-space-indented JSON for --json verb output, with a no-escape variant - the de-facto JSON output contract of the CLI, stated in no doc | `writeIndentedJSON` |
| * | explicit | code-only | **integral-aware float render** - internal/fleetcap.formatFloat renders an integral float as a bare integer (no trailing fraction), else with the caller's fallback verb - shared by trim (%g) and trimFixed (%.2f); the 39-file presence count is mostly stdlib strconv.FormatFloat calls; no doc mentions it. | `formatFloat` |
| * | explicit | code-only | **ledger file-path variable convention** - A naming convention, not one symbol: a local/flag variable holding a JSONL ledger file path, re-declared across 37 production files (cmd/fak subcommands, internal/benchloop, cmd/devexmeter), plus the (*nightrunFlags).ledgerPath method and exported memorycotravel.LedgerPath(); no doc mentions it. | `ledgerPath` |
| * | explicit | code-only | **lineage-stamped bench report marshal** - internal/benchcli.MarshalReport marshals a bench report and splices the lineage stamp and artifact block into the top-level JSON object - the wiring point the lineage gate (tools/check_bench_lineage.py) requires of every cmd/*bench emitter; a second copy lives in internal/apihostprobe; 21 production files, no doc mentions it. | `MarshalReport` |
| * | explicit | code-only | **loop ledger path default** - cmd/fak/loop.go: the default loop-ledger path - FAK_LOOP_LEDGER env var else .fak/loops.jsonl - used by 13 cmd/fak files alongside its siblings defaultLoopPolicy and defaultLoopRegistry; no doc mentions it. | `defaultLoopLedger` |
| * | explicit | code-only | **matmul worker-count budget** - internal/model/parallel.go package var capping matmul parallelism (GOMAXPROCS by default; FAK_WORKERS pins an absolute count, FAK_BUDGET a portable core fraction) with exported accessor NumWorkers(); referenced in 31 production files, nearly all inside internal/model; no doc mentions it. | `numWorkers` |
| * | explicit | code-only | **per-OS dispatch helper process setup** - cmd/fak per-OS build-tag pair: dispatch_helper_windows.go delegates to windowgate.ConfigureBackgroundCommand (suppress console windows), dispatch_helper_unix.go is a no-op; applied to every background dispatch-helper exec.Cmd across 19 production files; no doc mentions it. | `configureDispatchHelperCommand` |
| * | explicit | code-only | **per-package Slack channel resolver** - Convention declared in 11 internal posting packages: resolve that surface's Slack channel id from its env var then a .env.slack.local line, returning empty so the caller can require an explicit --channel; also the resolveChannel func field on cmd/fak's slackPostSpec; 29 production files, no doc mentions it. | `ResolveChannel` |
| * | explicit | code-only | **per-package Slack token resolver** - Convention declared in 11 internal posting packages (benchpost, blockerpost, cachevaluepost, chatrelay, dispatchpost, dojopost, grafanapost, marketing, ...): resolve that surface's Slack bot token from its env vars, then .env.slack.local, then scoreboard.ResolveToken; plus unexported hfhub.resolveToken for the HF token; 35 production files, no doc mentions it. | `ResolveToken` |
| * | explicit | code-only | **per-verb ParseFlags convention** - the convention that every CLI verb declares its own ParseFlags/parseFlags helper (139 production files) with reject-args and or-help variants - the single most repeated declared name in the tree, described nowhere | `ParseFlags` |
| * | explicit | code-only | **post source label default** - cmd/fak/scoreboard.go: the default 'source' label stamped on posted reports - FAK_SCOREBOARD_SOURCE env var, else the hostname, else empty; the single declaration is shared by about 14 cmd/fak post subcommand files (bench, blockers, cachevalue, dojo, marketing, news, nodeusage, ...); no doc mentions it. | `defaultSource` |
| * | explicit | latent-literal | **provider overload status 529** - the non-standard HTTP 529 the Anthropic API returns when overloaded, load-bearing in stream-retry and account-rotation paths - internal/agent/retry.go names it (const statusOverloaded = 529) but ~8 other production files (gateway, attemptbudget, agent siblings) still use the bare literal, and no doc mentions either form | `statusOverloaded` |
| * | explicit | code-only | **pure JSONL row renderer** - Convention declared in eight internal packages (cachevalueledger, cadencereport, dojo, experiments, fleetmon, milestonereport, nightrun, programreport): json.Marshal one ledger row to its JSONL line, kept pure (no disk write) so the writer is testable; 16 production files, no doc mentions it. | `AppendLedgerLine` |
| * | explicit | code-only | **registered region-backend resolver accessor** - internal/abi.ActiveResolver() (registry.go) returns the Resolver from the registered RegionBackend snapshot, or nil when no backend is registered; called from 38 production files across roughly 30 packages, mentioned in no doc. | `ActiveResolver` |
| * | explicit | code-only | **registry file loader convention** - Same-named loader declared over four different registry types: internal/accounts (read plus validate, returns error), fleetaccounts (best-effort sessions.json, silent empty on failure), grafanapost, and loopmgr; 18 production files, no doc mentions it. | `LoadRegistry` |
| * | explicit | code-only | **registry file-path variable convention** - A naming convention: a local/flag variable holding a registry JSON path (loop registry, accounts sessions.json) re-declared across 18 production files in cmd/fak and internal/fleetaccounts, plus the (*experimentsFlags).registryPath method and RegistryPath struct fields; no doc mentions it. | `registryPath` |
| * | explicit | code-only | **report file writer convention** - Same-named writers with different contracts: benchcli.WriteReport (lineage-stamped via MarshalReport, creates parent dirs - the single-call form for bench emitters) and fleet.WriteReport (validated fleet status report behind a file-safe key guard), plus lowercase writeReport copies in cmd/fleetserve and cmd/modelbench; 13 production files, no doc mentions it. | `WriteReport` |
| * | explicit | code-only | **scorecard axis struct** - A struct type re-declared in four packages (docfreshrsi, rsiloop, rulesynth, supportmaturity): one named axis of a scorecard - Name, Value, and a short free-form Unit ('ratio', 'tokens', 'turns'); 12 production files, no doc mentions it. | `ScoreComponent` |
| * | explicit | code-only | **short HEAD commit stamp** - Convention in five packages (auditpane, gardenbundle, milestonereport, programreport, plus cadencereport delegating to gardenbundle): run `git rev-parse --short HEAD` under a root and return the SHA, or 'unknown' on any failure; 13 production files, no doc mentions it. | `HeadCommit` |
| * | explicit | code-only | **sorted map-keys helper** - func sortedKeys returning the lexicographically sorted keys of a string-keyed map, still declared 15 times per package even though the shared generic maputil.SortedKeys exists expressly to replace the copies (its package doc says so); 32 production files, no doc mentions it. | `sortedKeys` |
| * | explicit | code-only | **status-recording WriteHeader** - The repo declares WriteHeader once: (*statusRecorder).WriteHeader in internal/gateway/metrics.go records the first status code written (later calls ignored) and forwards it, so the metrics middleware labels requests by the status actually sent; the rest of the 14-file presence count is stdlib http.ResponseWriter calls; no doc mentions it. | `WriteHeader` |
| * | explicit | code-only | **suffix-tolerant float parse** - The repo declares parseFloat once (cmd/dojorsi/main.go), an fmt.Sscanf %g parse that tolerates a trailing format suffix; the 50-file presence count is dominated by stdlib strconv.ParseFloat calls, and no doc mentions either. | `parseFloat` |
| * | explicit | code-only | **surfaced upstream HTTP error** - internal/agent/chat.go error type carrying a non-retried non-2xx upstream status plus a truncated body (operator log only), the verbatim Retry-After header, and sanitized 429 limit metadata, so the gateway surfaces the real upstream status downstream instead of a misleading 200/502 (#82); 12 production files across internal/agent and internal/gateway; no doc mentions it. | `UpstreamStatusError` |
| * | explicit | code-only | **tilde expansion helper** - the internal/pathutil helper that expands a leading ~ in user-supplied paths (54 production files) - the reason ~/foo works in every fak flag, a behavior users rely on that no doc promises | `ExpandTilde` |
| * | explicit | code-only | **user config-dir resolution** - internal/guardrsi.configHome resolves the user config dir (XDG_CONFIG_HOME, then APPDATA, then HOME/.config, then os.UserHomeDir fallback); the same resolution recurs as a local in fleetaccounts/paths.go feeding the exported Paths.ConfigHome field; 12 production files, no doc mentions it. | `configHome` |
| * | explicit | code-only | **verb deep-help flag usage hook** - cmd/fak/help.go helper that sets fs.Usage so `fak <verb> --help` prints the verb's catalog synopsis and usage-wall block above the flag defaults instead of the bare flag-package dump; called right after flag.NewFlagSet in 23 cmd/fak files; no doc mentions it. | `verbFlagUsage` |
| * | explicit | code-only | **walk-up-to-.git root finder** - Stat-walk from a start dir up to the nearest directory containing .git, falling back to start: repoguard.findRepoRoot (no subprocess, mirrors Python repo_guard.find_repo_root) with exported FindRepoRoot wrapper, plus separate copies in cmd/fak/guard_support.go and fleetpane.FindRepoRoot (error-returning); 16 production files, no doc mentions it. | `findRepoRoot` |
| * | explicit | code-only | **weight-bearing engine** - the engine-interface predicate saying whether an engine actually carries model weights (real inference: DynamoEngine true) or is a stub/proxy (readEngine, localEngine false) - a load-bearing trust distinction that appears in 13 production files and zero docs | `WeightBearing` |
| c | named-code | latent-literal | **Anthropic 5m cache-write multiplier** - a bare 1.25 on non-const lines in ~6 production files with one dominant meaning: the Anthropic 5-minute-TTL prompt-cache WRITE price multiplier (1.25x base input) - named THREE times in production (CacheWrite5mMultiplier in both internal/gateway/cache_pricing.go and internal/resume/resume.go, defaultCacheWriteMult in internal/agent/anthropic_compact.go) while fanbench's --cache-write flag default, bench relayvscompaction's CacheWriteMult fixture and turnbench fanout field docs re-inline it; conflated with the small-tier $1.25/Mtok output price in the modelroute price book and a rand.NewZipf s=1.25 parameter in kvreplay | `CacheWrite5mMultiplier` |
| c | named-code | latent-literal | **default gateway port** - a bare 8080 on non-const lines in ~10 production files (and dozens of doc-comment/flag-help occurrences) with one dominant meaning: the default fak serve/gateway listen port - "127.0.0.1:8080" addr and base-URL defaults inlined across serve/guard/node/ps/session/tui/info in cmd/fak, chatrelay, macbench and the bench commands, plus llama.cpp probe order entries; named in production only at the edges: internal/frontierswe DefaultGatewayAddr / DefaultGatewayBaseURL (a leaf consumer) and cmd/dropindemo exampleGateway, while the actual front doors (cmd/fak/serve.go addr flag, node.go port flag) re-inline it | `DefaultGatewayAddr` |
| c | named-code | latent-literal | **the 15% memory headroom** - a bare 0.15 on non-const lines in ~6 production files, dominated by a 15% memory-headroom fraction declared at least five separate times (agent inKernelKVMemoryHeadroom and inKernelRequestDeviceHeadroom, cmd/fak serveGGUFDeviceHeadroom and serveGGUFHostHeadroom, cmd/modelbench modelbenchDeviceHeadroom); conflated with headroom-adjacent but distinct 0.15s (headroom minSavedRatio env default, bench CompactionLossFrac, sessionobs minHeadlineSep), turnbench PPrivate profile shares, a nightrun weight and a CSS rgba alpha in report HTML | `serveGGUFDeviceHeadroom` |
| c | named-code | latent-literal | **the 160-char one-line clip** - a bare 160 on non-const lines in ~11 production files with one dominant meaning: a 160-char/byte one-line clip (tail[:160] in cadencereport, gardenbundle and scorecardpane, s[:160] in hooks gate_provenance, oneLine(e.RawArgs, 160) in agent/loop.go, headLine(body, 160) in memq) - named once as taskmgr's maxEchoChars while every other site inlines it; conflated with IQ3 sign-table byte values (ggufload/model tables), ctxdemo token sizes (MinTok/MaxTok 160), the RVC duration grid {40,80,160,320} and a TUI score increment | `maxEchoChars` |
| c | named-code | latent-literal | **the 250ms poll quantum** - a bare 250 on non-const lines in ~9 production files, dominated by a 250ms poll/backoff quantum: agent minRetryWait = 250ms, safecommit lockReapPoll = 250ms, safesync's attempt-squared * 250ms push backoff, bare time.Sleep(250ms) in cmd/fak cluster.go and node.go, and watchdog_autoheal BaseDelay 250ms; conflated with turntaxmeter DefaultHookP99BudgetMS = 250 (a latency budget), a 250-mirror logvault verify sample default, 250-total-requests bench prose and an IQ3 table byte | `minRetryWait` |
| c | named-code | latent-literal | **the 8192-entry bounded cap** - a bare 8192 on non-const lines in ~7 production files, dominated by the repo's 8192-entry bounded-cap convention, declared over and over per package: session DefaultTableLimit, ctxmmu/secretgate/normgate DefaultMaxHeld, ifc DefaultLedgerLimit, ratelimit defaultMaxKeys, preflight DefaultMaxNegatives, gateway maxSessionPlanners/maxCtxValueSessions/maxResetHealthSessions, vdso node/witness limits - and restated numerically in microagent/preflight cross-reference comments; conflated with 8192-token budgets (gateway admission TokenBudget: 8192, ctxplan BudgetBounds Ceil: 8192, memory-recall byte budget default), 8192-byte buffers/tool-output caps (guard_local, swebench maxToolOutput) and FFN/vocab bench flag defaults | `DefaultTableLimit` |
| c | named-code | latent-literal | **the 95% confidence bar** - a bare 0.95 on non-const lines in ~6 production files, dominated by 95% confidence/coverage: turnbench OPE names it (const confidence95 = 0.95) while frontierswe's 0.95 CLI pass threshold and TokenMatchRate >= 0.95 bar, fleetsim's 0.95 clearance and trajhook's CostOutlier(0.95) quantile inline it; conflated with the P95 percentile pick (webbench serving), the compaction trigger fraction (~0.95 window-full in bench relayvscompaction) and a sessionobs structured-records KPI floor | `confidence95` |
| c | named-code | latent-literal | **the conflated 150 literal** - a bare 150 on non-const lines in ~7 production files conflating several concepts: a 150ms poll/backoff quantum - the dominant family (agent authRefreshPollInterval, cmd/fak guardHeadlessRehydratePollInterval, safecommit's attempt-squared * 150ms lock-contention backoff); the P2 priority weight (dispatchtick PriorityWeightP2 = 150, re-inlined in cmd/fak's tuiPriorityWeights map literal); ~150 tokens per navigation action in webbench geometry; the anthropic tier score in computeauto's ordering comment; a >=150 source-lines dispatch band; and an IQ3 table byte | `authRefreshPollInterval` |
| c | named-code | latent-literal | **the conflated 240 literal** - a bare 240 on non-const lines in ~10 production files conflating four concepts: 240-char/byte clips - the dominant family, named per-site where named at all (dogfoodscore harnessLineClip, gateway forbiddenDetailMax, hooks gate_priorart const budget = 240, commitintent's 240-byte subject cap checked inline); 240-second per-member timeouts (fak garden and tui flag defaults); the $240 UA123 flight price in the turnbench/agent mock-tool oracle (oracleCheapestFlight comments, amount = 240 salting in bench/fanrun); and an IQ3 table byte value | `harnessLineClip` |
| c | named-code | latent-literal | **the conflated 512 literal** - a bare 512 on non-const lines in ~21 production files conflating several concepts: 512-token budget floors and prefill ceilings (named repeatedly: ctxplan DefaultMinResidentTokens, compute MinAutoContextTokens, gateway rectPrefillTokenCeiling, model batchRectPrefillMaxTokens, BudgetBounds Floor: 512) - the dominant family; 512-byte error-body snippets (io.LimitReader(resp.Body, 512) in enginecache and gateway http_epfanout) and stub-log/byte caps (StubLogMaxBytes, maxIndexViewBytes, dispatchResolveLogStubFloorBytes); 512 KiB / 512 GiB shifts (512*1024, 512<<30); P=512 bench sweep sizes and 512 max-tokens flag defaults; AVX-512 rides along in comments | `DefaultMinResidentTokens` |
| c | named-code | latent-literal | **the conflated 600 literal** - a bare 600 on non-const lines in ~19 production files conflating at least five concepts: 600-second (10-minute) timeout floors and TTL margins (guardTimeoutFloorS declared twice - cmd/fak/guard_provider.go and cmd/dispatchworker/guard.go - plus dispatchtick LeaseTTLMarginS, sweepconfig TimeoutS: 600, a "600" env default in dispatch_tick) - the dominant family; 600-char tail/preview clips (dojocal tail, sessionreset clip, fleetmon const max = 600, fak debug previewBytes); CSS font-weight 600 in report HTML (cdb, lensviz); a 600ms quadratic retry backoff base (agent/retry.go); 600-token fixture sizes (tokendemo coldResultTokens, loopbench); and issue #600 references | `guardTimeoutFloorS` |
| d | named-doc | doc-only | **compatibility matrix** - the recurring heading (34 docs) whose canonical artifact is docs/integrations/compatibility-matrix.md: the per-tool table answering 'can this agent tool repoint its base URL at fak serve' across the OpenAI/Anthropic/MCP wires; other docs reference that one table rather than defining their own | `CompatibilityMatrix` |
| d | named-doc | doc-only | **durable ledger** - the recurring heading (16 docs; docs/cadence/README.md, docs/nightrun/README.md) for an append-only JSONL history file committed to the trunk as durable evidence (cadence history.jsonl rows, nightrun collected.jsonl rows) — 'durable trunk evidence, not a regenerable build artifact'; a real cross-cutting data-discipline concept | `DurableLedger` |
| d | named-doc | doc-only | **gpu server** - the recurring heading (80 docs, concentrated in docs/benchmarks/, docs/nightrun/, docs/notes/) for the lab's private 8-GPU datacenter box that big-model benchmarks run on; docs/gpu-server-private-boundary.md mandates 'GPU server' as the scrubbed public name for it (no host/IP/path/token), so the phrase is itself a deliberate anonymization convention, not a code artifact | `GpuServer` |
| d | named-doc | doc-only | **ground truth** - the section (29 docs) pointing at the artifact/command a reader can run to verify a claim themselves - fak's evidence-over-assertion convention, unnamed and undefined | `GroundTruth` |
| d | named-doc | doc-only | **honest fence** - the repo's core rhetorical device - an explicit statement of what a claim does NOT cover, placed next to the claim so it cannot be overread - used as a heading in 74+ docs under four drifting spellings (honest fence / honesty fence / honest boundary / honesty gate) with no definition anywhere and no code symbol | `HonestFence` |
| d | named-doc | doc-only | **honest scope** - the recurring section (54 docs) that states what a feature deliberately does not attempt - the scope-declaration counterpart of the honest fence (which guards a specific claim) - never defined as a term | `HonestScope` |
| d | named-doc | doc-only | **honesty ledger** - the recurring doc section (31 docs) recording which claims are proven vs open for a feature - readers meet it constantly, but which ledger file it denotes (if any) is never stated and no code writes a thing by this name | `HonestyLedger` |
| d | named-doc | doc-only | **hot path** - used in 45 docs for the latency-critical code route (decode loop, gateway serve path) - borrowed jargon everyone assumes, but WHICH paths are the hot paths is never enumerated, so the term cannot be checked | `HotPath` |
| d | named-doc | doc-only | **master ledger** - the per-theorem verdict table in docs/proofs/README.md — every module proof's theorem, deterministic witness test, and live verdict filled from actual test runs; the 22-doc presence is because every docs/proofs/*.md page defers its current verdict to 'the master ledger (README.md)' | `MasterLedger` |
| d | named-doc | doc-only | **mcp server** - the recurring heading (53 docs, mostly docs/integrations/ and docs/FAQ.md) for fak's Model Context Protocol server surface: `fak serve --stdio` (or POST /mcp over HTTP) speaking JSON-RPC 2.0 and exposing the fak_* adjudication tools; an external-protocol term applied to fak's own gateway, not a distinct code identifier | `McpServer` |
| d | named-doc | doc-only | **promotion gate** - the criteria a claim/feature must meet to move up a maturity rung - a heading in 22 docs (plus 'Promotion rule' in 10) with no code symbol and no definition, despite gating being fak's favorite mechanism | `PromotionGate` |
| d | named-doc | doc-only | **readiness score** - an overloaded phrase (19 docs) with two senses: (1) docs/fleet.md 'The readiness score' — the deliberately simple 0-100 operator blend `score` for a fleet node; (2) the *-READINESS-SCORECARD family (agent/persona/popularization/release) where 'readiness' is a scorecard dimension re-derived by tools/*.py; no single code identifier backs either | `ReadinessScore` |
| d | named-doc | doc-only | **threat model** - the recurring security-doctrine heading (28 docs; docs/fak/security.md, docs/FAQ.md, docs/playbooks/): fak's stated attacker assumption — the model is the untrusted program and the attacker controls everything it reads (prompt, retrieved docs, tool results), so refusal is gated by two structural gates rather than a classifier; a real system concept with no code identifier | `ThreatModel` |
| d | named-doc | doc-only | **triage decision** - a doc-section convention of the docs/notes/RESEARCH-*-triage notes (24 docs): the closing verdict section answering 'Adopt? Defend against? Ignore?' for an externally-scouted paper/repo, with reasons; it is the structured outcome slot of the research-triage workflow, not a code concept | `TriageDecision` |
| d | named-doc | doc-only | **trust boundary** - the line between what the kernel verifies and what it merely receives from an untrusted agent - a heading in 33 docs and the load-bearing idea of the whole system, with no code identifier and no glossary definition | `TrustBoundary` |
| d | named-doc | hinted | **unmeasured-containment residual** - the standing residual that fak's privacy/prompt-injection containment is a design posture, not a measured leakage number - repeated across research triages (MIRROR #1007) as an italicized hedge, never as a named, trackable residual | `ContainmentUnmeasured` |
| d | named-doc | doc-only | **verdict ladder** - the ordered best-to-worst verdict scale every scorecard defines for its rows (crystal..undocumented, explicit..latent) - the shared pattern is used by 8+ docs as a heading but is named nowhere as the reusable concept it is | `VerdictLadder` |
| d | named-doc | doc-only | **worked example** - a doc-structure convention (40 docs, heaviest in docs/explainers/): a section that walks one concrete scenario end-to-end with real numbers or a real fixture (e.g. addressable-kv-cache.md replays the exact TestWriteTimeEvictEqualsNeverSaw fixture); it names a writing pattern, not a system concept | `WorkedExample` |
| ~ | hinted | latent-literal | **MINSTD LCG multiplier** - a bare 48271 on non-const lines in ~12 production files with one dominant meaning: the MINSTD (revised Park-Miller) LCG multiplier in the next-token scrambler (id*48271 + 1) % vocab used to derive deterministic decode/continuation token streams across the bench mains (batchbench, demorace, sessionbench, ctxdemo, fleetserve, q8bench, modelbench, glmdsatput) and internal/model/profile.go; cmd/q8kernel also uses it as a value mixer ((i*48271+7)&0xffff); never declared as a const anywhere | `minstdMultiplier` |
| ~ | hinted | latent-literal | **Qwen-1.5B model geometry** - a bare 1536 on non-const lines in ~6 production files, all model-geometry but for related-yet-distinct dims: HiddenSize 1536 for the qwen25-1.5b reference shape (sessionbench, o1proof1b, turnbench longcontext - repeated as a struct literal each time), IntermediateSize 1536 in other shapes (sessionbench smollm2 config, tpcheck --inter default, model quant doc prose 'intermediate 1536'), glmdsatput's MLA q_lora_rank 1536 flag default, and a 1536-token demo prefix in ctxdemo; none of these dims is named as a const | `qwen15bHiddenSize` |
| ~ | hinted | doc-only | **acceptance checklist** - the recurring heading (9 docs) whose main artifact is docs/integrations/harness-acceptance-checklist.md: the six items (one wire + five dialect facts) that must be documented before an agent path is advertised as a first-class fak launcher; also reused for the branch-regime role-term checklist | `AcceptanceChecklist` |
| ~ | hinted | doc-only | **acceptance criteria** - a doc-structure convention (17 docs; heaviest in docs/notes/verification-ladder-epics.md) listing numbered, testable conditions an epic/slice must satisfy before it counts as done — often naming the exact regression tests; a spec-writing pattern, not a system concept | `AcceptanceCriteria` |
| ~ | hinted | doc-only | **adjudication cost** - the recurring heading (10 docs; docs/fak/agent-integration-architecture.md, docs/webbench-real-measurements-summary.md) for the measured per-call overhead of kernel adjudication — in-process p50 ~1,300 ns, vDSO hit p50 ~50 ns, i.e. the sub-millisecond tax the gate adds; a measured quantity with no code identifier | `AdjudicationCost` |
| ~ | hinted | doc-only | **agent programming grammar** - the recurring concept (4 docs) of docs/standards/agent-grammar.md, a normative standard fixing the grammar an agent fleet coordinates by — nouns, verbs, and closed vocabularies — so a second implementation can be conformance-checked, the role internal/abi's golden freeze plays for the ABI; the companion note CONCEPT-AGENT-PROGRAMMING-GRAMMAR-2026-06-28.md explains why and catalogs under-expressed concepts G1-G9. | `AgentProgrammingGrammar` |
| ~ | hinted | doc-only | **agentrun boundary** - the recurring concept (4 docs; heading in docs/notes/CONCEPT-TOOL-PROCESS-TABLE-2026-07-02.md) naming the authority boundary for a fleet of nested agents, distinct from the tool process table's lifetime engine: an AgentRun owns one agent loop, SpawnAttempt is the only pre-exec path to a child run, PolicyDigest pins the policy snapshot, and the launch ToolCallID is the join key; public contract in docs/agent-run-leak-prevention.md. | `AgentRunBoundary` |
| ~ | hinted | doc-only | **baton schema** - the recurring heading (5 docs, docs/notes/) for the fak.relay.baton.v1 relay-handoff wire contract: a data-only, pointer-only object a relay leg writes at a safe point for its successor (commits, ledger rows, issue numbers, memory slugs, file globs — never transcript bytes or a model-written recap). Pinned in RELAY-BATON-SCHEMA-2026-07-01.md; explicitly no Go type or CLI consumes the format yet. | `BatonSchema` |
| ~ | hinted | latent-literal | **bench LCG seed base** - a bare 2463534242 on non-const lines in ~13 production files with one dominant meaning: the base seed offset in state := 2463534242 + seed feeding a glibc-shaped LCG (state*1103515245 + 12345, masked to 31 bits) that generates deterministic synthetic token ids for the bench harnesses; extracted once as benchids.LCG (whose doc comment describes the behaviour) but copy-pasted lcgIDs clones in the bench mains (batchbench, demorace, sessionbench, radixbench, fleetserve, q8bench, glmdsatput, o1proof1b, modelbench, ctxdemo, cmdutil, bench/fanrun_timing) re-inline the constant; NOTE: the value is Marsaglia's classic xorshift32 example seed, but here it is used purely as an arbitrary reproducible offset for an LCG, not xorshift | `benchLCGSeedBase` |
| ~ | hinted | latent-literal | **bench continuation-stream seed** - a bare 991 on non-const lines in ~6 production files with one dominant meaning: the fixed seed for the per-turn continuation/result token stream in the bench family - ids0 := lcgIDs(C, vocab, 991) repeated in bench/fanrun_timing, sessionbench, demorace, ctxdemo/arms and fleetserve (twice), always paired with seed 1 for the shared prefix stream (prefix := lcgIDs(opts.Prefix, vocab, 1)); never named; internal/nightrun's '#991' hits are issue references, not this number | `benchContinuationSeed` |
| ~ | hinted | doc-only | **benchmark control** - the recurring heading (4 docs; section of docs/fak/server-config.md) for the FAK_BENCH_* environment variable table controlling quantized-GEMM benchmark shapes (prefix length, batch sizes, rectangular dims). A config-reference section convention; the variables exist in code but the grouping name does not. | `BenchmarkControl` |
| ~ | hinted | doc-only | **benchmark infrastructure** - the recurring heading (6 docs; docs/benchmarking/ and docs/benchmark/ subtrees) for the sweep/measurement plumbing as a whole — YAML sweep configs + Python orchestrator, demo servers (cmd/demorace), cross-machine run/manifest design; an umbrella term for a doc subtree, not one artifact | `BenchmarkInfrastructure` |
| ~ | hinted | doc-only | **budget dimensions** - the recurring concept (4 docs; section of docs/generation-super-loop-budgets.md) defining the four budgets every recurring super-loop plan must make visible — time (max_minutes/cadence), tokens (token_ceiling/prompt_budget), workers (max_workers/lane leases), and review (reviewer/deadline/escalation). Planning doctrine for loop plans, not a code type. | `BudgetDimensions` |
| ~ | hinted | doc-only | **by intent** - the recurring section (5 docs) of the generated benchmark plan (docs/bench-plan.md and its copies) that groups the planner's recommended runs by operator intent (e.g. Benchmark perf), asserting all four operator intents are surfaced. A generated-report grouping convention, not a system object. | `ByIntentPlanView` |
| ~ | hinted | doc-only | **cache family** - the glossary concept (5 docs; heading in docs/fak/concept-glossary.md) that disentangles the overloaded 'cache' root into planes, each answering a different question: storage (KV cache), virtualization (vCache), metadata (cachemeta), etc. A naming/disambiguation device for docs, not a runtime object. | `CacheFamily` |
| ~ | hinted | doc-only | **cache frontier operating plan** - the title concept (5 docs) of docs/CACHE-FRONTIER-OPERATING-PLAN.md: the project-management spine for fak's caching value-add that introduces no new benchmark claim but keeps existing claims, demos, and scorecards pointed at one product outcome (fak as the default memory/reuse kernel for long-running agents). A planning doc, not code. | `CacheFrontierPlan` |
| ~ | hinted | doc-only | **cache-value roll-up** - the recurring concept (5 docs; title of docs/cache-value-rollup.md) naming the front-door report for whether fak's cache work is paying off; it keeps the kernel-reuse proof and provider-dollar economics in separate tracks so trends never blend unlike evidence. Related to the fak cachevalue CLI but the roll-up itself is a doc-level concept with no identifier. | `CacheValueRollup` |
| ~ | hinted | doc-only | **capability matrix** - the recurring heading (13 docs) whose canonical artifact is docs/adoption/compare/matrix.md — the one-page build-vs-buy table scoring fak against three neighbouring product categories on the six capabilities fak claims; also reused generically (e.g. VTOOLCALL note's topology capability table) | `CapabilityMatrix` |
| ~ | hinted | doc-only | **checked-in artifacts** - the recurring benchmark submission-packet section (4 docs, docs/benchmarks/) listing the hash-pinned artifacts tracked in-repo (runbooks, results scaffolds, fixtures) with SHA-256 columns to re-derive via sha256sum before promoting a result. A packet-structure convention supporting evidence integrity. | `CheckedInArtifacts` |
| ~ | hinted | doc-only | **claude code integration** - the recurring heading (5 docs) for wiring fak as a kernel-adjudicated gateway in front of Claude Code / the Anthropic API (docs/integrations/claude.md guide; the dogfood-claude.sh walkthrough in docs/fak/agent-integration-architecture.md). Names an integration with an external harness, not an internal identifier. | `ClaudeCodeIntegration` |
| ~ | hinted | doc-only | **closed verdict vocabulary** - the recurring concept (4 docs; heading in docs/standards/verification-ladder-spec.md) that the spec's Verdict set is closed and additive — a new verdict is a new named value plus a fold arm, never a free-text field — mirroring the kernel's closed VerdictKind in internal/abi/types.go. A design principle stated in the standard, itself unnamed in code. | `ClosedVerdictVocabulary` |
| ~ | hinted | doc-only | **closure honesty** - the recurring heading (8 docs, anchored in docs/dispatch-status.md) for the dispatch-loop metric separating diff-witnessed closures from claimed ones: closure_rate = TRUE_RESOLVED / (TRUE_RESOLVED + CLAIMED_CLOSED), plus honest_close_rate crediting the DATA rung; a measured fleet-health concept | `ClosureHonesty` |
| ~ | hinted | doc-only | **cloud providers** - a doc-structure convention of the integration guides (11 docs; docs/integrations/aider|claude|cline|continue|jetbrains|vscode|zed.md): the config-recipe section for fronting a hosted API (OpenAI, Anthropic) with fak serve, sibling of the 'Local models' section; a template slot, not a system concept | `CloudProviders` |
| ~ | hinted | doc-only | **collective seam** - the recurring heading (6 docs; docs/serving/native-device-mesh-collectives.md, docs/explainers/multi-gpu-tensor-parallelism.md) for the cross-rank reduction interface: compute.CollectiveBackend declares the four canonical Megatron collectives, with the exact CPU reference in internal/compute/collective.go; the doc phrase for a seam that DOES have code behind it but no matching identifier named 'collective seam' | `CollectiveSeam` |
| ~ | hinted | doc-only | **comparison table** - a doc-structure convention of benchmark docs (6 docs; docs/benchmark/QUICKSTART.md 'generate comparison table' via fak bench-runs table, docs/benchmarks/ A/B arm tables): the side-by-side results table or the command that generates it; a template slot, not a system concept | `ComparisonTable` |
| ~ | hinted | doc-only | **completion bar** - a doc-section convention of the GPU benchmark runbooks (6 docs; docs/benchmarks/GPU-SERVER-GLM52-VLLM-AGENTIC-BENCHMARKS.md, VLLM-EP-EPLB-MOE-BASELINE-RUNBOOK.md): the closed checklist of conditions that must ALL hold before the benchmark counts as complete (preflight READY, artifacts present, ...); a runbook honesty gate written as a heading | `CompletionBar` |
| ~ | hinted | doc-only | **context-mmu quarantine** - the recurring heading (12 docs, anchored in docs/fak/agent-integration-architecture.md) for the mechanism that holds tool results flagged with certain taints OUT of the agent's context (the context-MMU's quarantine), so poisoned content never enters the model's window; a system concept whose docs heading carries no single code identifier | `ContextMmuQuarantine` |
| ~ | hinted | doc-only | **core concepts** - a doc-structure convention (8 docs; docs/FAQ.md, docs/fak/faq.md): the FAQ section grouping the mental-model questions (model as untrusted program, tool call as syscall, one boundary for security and performance); a navigation heading, not a system concept | `CoreConcepts` |
| ~ | hinted | doc-only | **core rule** - a doc-structure convention of the docs/generation-*.md family (6 docs): the opening section stating the one-line normative rule of the model being specified (e.g. 'Capacity is a constraint model, not a priority model', 'Generation is a planning horizon, not an isolation boundary'); a spec-template slot | `CoreRule` |
| ~ | hinted | latent-literal | **core-budget fraction example** - a bare 0.75 on non-const lines in ~8 production files, dominated by the fractional core budget example: 0.75 = use 75% of the machine's logical cores, repeated verbatim in the --budget flag help of modelbench, batchbench, demorace, ctxdemo and turntaxdemo and in internal/model/budget.go + parallel.go FAK_BUDGET docs - documentation of an env/flag convention rather than a wired default; conflated with the AWQ alpha grid point (awqDefaultAlphas), the 0.75 false-warm penalty weight in vcachescore risk, sessionbench sweep fractions {0.25,0.5,0.75,1.0} and a ~0.75 MiB/token KV-size note | `defaultCoreBudgetFraction` |
| ~ | hinted | doc-only | **corrected asset** - the recurring section (5 docs, all in docs/launch/) marking the paste-ready corrected version of a launch post after an adversarial fact-check pass; it follows the fact-check notes explaining which claims were re-fenced to stay ledger-clean. A doc-structure convention of the launch subtree, not a system concept. | `CorrectedAsset` |
| ~ | hinted | doc-only | **coverage matrix** - the recurring heading (6 docs) for a coverage-hole grid: docs/coverage-matrix.md is the Model x Backend matrix generated by `fak coverage-matrix`; docs/bench-plan.md has the bench-node x workload-kind matrix ('.' = feasible hole, '-' = infeasible); docs/HARDWARE-MATRIX.md the platform matrix — same idea, three artifacts | `CoverageMatrix` |
| ~ | hinted | doc-only | **current evidence** - the recurring notes section (4 docs, docs/notes/) inventorying the evidence that already exists in-tree at time of writing — concrete symbols, gates, and commands (e.g. internal/taskmgr.ReviewHandoff behavior) — before the note proposes next steps. A note-structure convention grounding proposals in shipped state. | `CurrentEvidence` |
| ~ | hinted | doc-only | **debt metric** - the recurring heading (15 docs, anchored in docs/generation.md) for the lightweight `debt_score` each generation lane carries in the milestone report — an operator signal, explicitly not a priority score or gate; the concept exists as a JSON field name but no named code identifier | `DebtMetric` |
| ~ | hinted | doc-only | **debugging a denied operation** - a doc-structure convention of the integration guides (6 docs; docs/integrations/aider|cline|continue|jetbrains|vscode|zed.md): the boilerplate section showing how to reproduce a DENY offline (preflight against the same policy) when the fronted tool reports a blocked operation; a template slot around the preflight verb | `DeniedOperationDebugging` |
| ~ | hinted | latent-literal | **default shared-prefix tokens** - a bare 2048 on non-const lines in ~11 production files, dominated by the default master-goal shared prefix of 2048 tokens in the fan/bench family (topobench, fanbench, fanrun and sessionbench --prefix flag defaults, bench/fanrun opts.Prefix = 2048, turnbench fanout PrefixTokens: 2048 and the longcontext committed-regime anchor P=2048, macbench prefill sweep 128,512,2048,4096); conflated with ctxmmu's PointerMax = 2048 handle cap, gateway embedMaxItems = 2048, 2048-byte body snippets (llmd_smoke io.LimitReader), a glmdsatput hidden-size default, and issue #2048 references in harnessres comments | `defaultSharedPrefixTokens` |
| ~ | hinted | doc-only | **demotion path** - the recurring generation-planning section (4 docs, docs/notes/GENERATION-*) prescribing what happens when a promoted item's gate regresses: record the failed gate with evidence, move off gen/now only if the horizon changed, prefer demotion/parking/retirement over stale labels. The inverse of Promotion Rules; process doctrine, not code. | `DemotionPath` |
| ~ | hinted | doc-only | **downstream rule** - the recurring research-note section (4 docs, docs/notes/RESEARCH-span-* and RESEARCH-memory-*) stating the rule any downstream consumer of the note's result must obey — e.g. a quoted span reward must report raw/expected attention mass, normalized reward, and the verifier verdict, else it is raw telemetry. A research-note contract convention. | `DownstreamRule` |
| ~ | hinted | doc-only | **evidence gate** - the recurring benchmark submission-packet section (4 docs, docs/benchmarks/) listing the conditions that must ALL be true before a fak result may be submitted upstream or copied into BENCHMARK-AUTHORITY.md (pinned release version, explicit date window, etc.) — the reason a packet ships with no number. A packet-gating convention. | `EvidenceGate` |
| ~ | hinted | doc-only | **exclusion baseline** - the recurring concept (4 docs; defined in docs/host-defender-exclusions.md) naming the one-paste Windows Defender path and process exclusion set for fleet hosts — repo tree, Git install, agent scratch dir, and the processes a tool call spawns — removing the synchronous real-time-scan tax on every agent tool call, with an honest coverage-cost note. | `ExclusionBaseline` |
| ~ | hinted | doc-only | **executive summary** - a generic doc-structure convention (6 docs; docs/fak-vs-alternatives-comparison.md, docs/prefill-elimination-explained.md, docs/proofs/safety-case doc, benchmarking docs): the standard opening summary section; pure document boilerplate with no repo-specific meaning | `ExecutiveSummary` |
| ~ | hinted | doc-only | **fact-check notes** - a doc-section convention of the docs/launch/ assets (6 docs; show-hn.md, reddit-*.md, x-thread.md, lobsters-and-blog.md): the preserved audit trail of the adversarial fact-check each launch asset ran against the honesty ledger (CLAIMS.md), listing every claim changed and why the survivors are ledger-clean; a launch-workflow convention | `FactCheckNotes` |
| ~ | hinted | doc-only | **fan-out benchmark** - the recurring heading (8 docs; docs/benchmarking/README.md, docs/fak-vs-alternatives-comparison.md, CLAIMS.md) for the fanbench experiment: one master goal fanned to N=1..1024 sub-agents with a shared prefix ladder, measuring cross-agent dedup uplift and prefix-KV reuse geometry; the docs phrase for what cmd/fanbench (and the live fanrun capstone) measures | `FanoutBenchmark` |
| ~ | hinted | doc-only | **fastest path** - a doc-structure convention (6 docs; docs/qwen36-claude-dogfood-playbook.md, docs/integrations/openai-codex.md): the opening quickstart section giving the minimal command sequence ('two commands, two terminals') to get running; a template slot, not a system concept | `FastestPath` |
| ~ | hinted | doc-only | **five benchmarks** - a recurring heading (4 docs) used in two distinct senses: the survey table of five external agentic leaderboards (BFCL V4, GAIA2, Galileo v2, Vals Index, AA Agentic Index) in docs/notes/AGENTIC-LEADERBOARD-SURVEY-1068, and the GLM-5.2 note's five locally reproducible on-box benchmarks. Not one fixed set — the name is overloaded across notes. | `FiveBenchmarks` |
| ~ | hinted | doc-only | **four axes** - the recurring concept (4 docs; section of docs/generation-future-portfolio-rsi-score.md) naming the four component axes of the composite portfolio RSI score — a vector (starting with generation balance B) always reported alongside the folded scalar because single numbers hide diverging axis health; every input is a count or ordinal bucket, no invented dollars. | `FourAxes` |
| ~ | hinted | latent-literal | **frontier output $/Mtok** - a bare 15.0 on non-const lines in ~6 production files with one dominant meaning: the $15/Mtok frontier (Sonnet-class) output-token price used in cost models - bench.go's dollars formula (inTok/1e6*3.0 + outTok/1e6*15.0), fanbench's --dollars-out 15.0 flag default, sessionaudit's price table (sonnet/fable Output: 15.0, and opus Input: 15.0), and turnbench's DollarsPerMTokOut defaults; the sessionaudit table is a named map but no const names the price itself; conflated only with benchpost's 15.0 min-drop-pct regression threshold | `outputDollarsPerMTok` |
| ~ | hinted | doc-only | **frozen-trajectory cache cliff** - the title concept of docs/explainers/frozen-trajectory-cache-cliff.md (7 docs reference it): vendors' 90%+ prompt-cache hit rates are bought with rigidity — prompt caching is a prefix match, so any byte change in the prefix invalidates everything after it; a named explanatory concept in fak's cache-positioning argument | `FrozenTrajectoryCacheCliff` |
| ~ | hinted | doc-only | **future implementation hooks** - a doc-section convention of the docs/generation-*.md spec family (6 docs; agent-capacity-model, portfolio-rsi-score, proof-of-value-simulator, real-options-model, sunset-criteria, loop-scheduling): the closing list of concrete next slices, each intended to become a separate issue 'so this spec is not silently deferred into code'; a spec-template slot | `FutureImplementationHooks` |
| ~ | hinted | doc-only | **gate evidence** - the recurring section (5 docs; heading in docs/stable-releases/2026-06-stable.md) carrying the JSON gate record for a stable-release promotion — candidate sha/tag, codename, and per-gate results such as ci_green. An evidence-block convention of release records rather than a named code object. | `GateEvidence` |
| ~ | hinted | doc-only | **gateway serving** - the recurring release-notes section (4 docs, docs/releases/) grouping changes to the fak serve gateway (internal/gateway) — e.g. v0.5.0's cross-agent tier-2 scoped invalidation and revocation. A release-notes category label for an area of the system, not itself an identifier. | `GatewayServing` |
| ~ | hinted | doc-only | **generation classification** - a doc-section convention of grooming artifacts (6 docs; docs/notes/GENERATION-* and audit notes, docs/cache-frontier/): the section classifying an issue into gen/now / gen/next / gen/future per docs/generation.md, with the promotion evidence that would move it closer to now; the applied form of the generation contract | `GenerationClassification` |
| ~ | hinted | doc-only | **generation contract** - the title concept of docs/generation.md (9 docs reference it): generation is the product horizon of a piece of work (gen/now / gen/next / gen/future) — 'which horizon is this optimizing for and what evidence would move it closer to now' — explicitly not priority, not branch strategy, not a runtime flag; the contract the generation-*.md family builds on | `GenerationContract` |
| ~ | hinted | doc-only | **generation stays orthogonal** - a doc-section convention of the docs/generation-future-*.md specs (6 docs): the section proving a proposed model (RSI score, proof-of-value simulator) touches none of the three axes the generation contract keeps separate (priority, branch strategy, runtime exposure); a contract-compliance template slot | `GenerationOrthogonality` |
| ~ | hinted | doc-only | **generation-specific witness ladders** - the recurring concept (5 docs; title of docs/generation-witness-ladders.md, issue #1659, stream gen/second-next) — a design memo defining a witness-ladder taxonomy per generation horizon that a later gen/next or gen/now stream could enforce with a lint or fak hygiene gate. Explicitly a compatibility policy, not yet a runtime gate. | `GenerationWitnessLadders` |
| ~ | hinted | doc-only | **hardware catalog** - the recurring concept (4 docs; title of docs/notes/HARDWARE-CATALOG.md) tracking all benchmark machines with full specs, onboarding procedures, and baseline runs (e.g. node-macos-a, workstation-a); the live machine-readable copy is experiments/benchmark/catalog.json managed via tools/bench_catalog.py. | `HardwareCatalog` |
| ~ | hinted | doc-only | **harness integration acceptance checklist** - the recurring concept (4 docs; title of docs/integrations/harness-acceptance-checklist.md): the acceptance checklist for putting fak in front of an agent harness, covering both compatibility surfaces — the model/provider wire (the easy half: repoint one base URL) and the host-tool dialect (tool names, arg shapes, denial behavior) that a working wire silently hides. | `HarnessAcceptanceChecklist` |
| ~ | hinted | doc-only | **health checks** - a doc-structure convention of the integration guides (8 docs; docs/integrations/*): the boilerplate section showing `curl http://127.0.0.1:8080/healthz` against a running fak serve; a template slot around the /healthz endpoint, not a distinct concept | `HealthChecks` |
| ~ | hinted | doc-only | **honest boundaries** - a doc-section convention of the honesty culture (6 docs; docs/serving/cxl-memory-pool.md, hardware-aware-cache.md, docs/explainers/, docs/notes/): the section stating exactly what the described feature does NOT do ('kept, not papered over') — e.g. the plane is payload-free, physical pooling is the adapter's job; a reporting pattern | `HonestBoundaries` |
| ~ | hinted | doc-only | **honest gaps** - a doc-structure convention of the honesty culture (11 docs; docs/serving/, docs/notes/): a table/list of real, named failure modes or missing pieces that have no closing witness yet — per the doctrine that a named gap is catalog-complete even when unclosed; a reporting pattern, not a system concept | `HonestGaps` |
| ~ | hinted | doc-only | **honest limits** - the recurring caveat section (5 docs, docs/integrations/ and docs/notes/) that lists what a feature or result does NOT cover before a reader relies on it (e.g. the agent-memory gate only sees model-proposed tool calls, never out-of-band SDK writes). A doc-honesty convention, sibling of Honest residue and Scope fences. | `HonestLimits` |
| ~ | hinted | doc-only | **honest one-liner** - the recurring section (5 docs, mostly docs/notes/) giving the single-sentence, fenced summary of what a measurement actually proved — e.g. the session-cache-savings ablation's admission that the provider cache did ~99.9% of the visible saving and fak's contribution was pricing it honestly and catching bad tool calls. A doc-structure convention. | `HonestOneLiner` |
| ~ | hinted | doc-only | **honest residue** - the recurring benchmark-results section (5 docs, concentrated in docs/benchmarks/) that labels the known gaps and unproven parts left after a result — 'labeled, not hidden' (e.g. CDB-RESULTS: pages are tool results by design; the ranker is extractive token-overlap). A doc-honesty convention, not a system object. | `HonestResidue` |
| ~ | hinted | doc-only | **honest status** - a doc-section convention of the honesty culture (17 docs; benchmark results and docs/notes/track-* parity trackers): a status section whose verdicts are read from the tree + git log, 'not from any worker's say-so', explicitly separating shipped vs scaffold vs deferred; a reporting discipline rather than a code concept | `HonestStatus` |
| ~ | hinted | doc-only | **implementation hooks** - a doc-section convention of the docs/generation-*.md family (7 docs), almost always as the longer heading 'Future Implementation Hooks': the list of concrete next slices, each meant to become a separate issue so the spec is not silently deferred into code; a spec-template slot largely subsumed by the 'Future Implementation Hooks' row | `ImplementationHooks` |
| ~ | hinted | doc-only | **in-kernel model engine** - the recurring concept (4 docs; FAQ headings) for fak's from-scratch pure-Go transformer forward pass, shipped as the default 'inkernel' engine: loads GGUF/safetensors into process address space, decodes over the kernel-owned KV cache, and is a correctness reference (bit-exact/argmax-exact vs a HuggingFace oracle) rather than a production serving engine. The component is real code, but no identifier is literally named 'in-kernel model engine'. | `InKernelModelEngine` |
| ~ | hinted | latent-literal | **in-repo deterministic LCG** - the textbook linear-congruential PRNG (state*1103515245 + 12345, masked to 31 bits) re-implemented inline in 14+ files (bench, benchids, cmdutil, agent) for deterministic jitter/ids - the multiplier and increment are never named, so the shared PRNG convention is invisible | `lcgMultiplier` |
| ~ | hinted | doc-only | **install the binary** - the recurring instruction heading (4 docs; e.g. docs/fak/deployment-guide.md) for installing the prebuilt static fak binary — the one-line install.sh that downloads, verifies SHA-256, and installs to PATH, plus env knobs like FAK_VERSION. An install-doc section convention, not a system concept. | `InstallBinary` |
| ~ | hinted | doc-only | **intake rules** - the recurring generation-process section (4 docs; canonical in docs/generation.md) giving the labeling rules at issue creation: add generation plus exactly one stream label, bind the matching milestone, use needs-triage rather than guessing, never use gen/future as a dumping ground, and state the horizon-specific witness in the body. Backlog process doctrine. | `IntakeRules` |
| ~ | hinted | doc-only | **integration points** - the recurring architecture-doc section (4 docs, mostly docs/fak/) listing where external systems plug into fak — e.g. the fak serve gateway's protocol entry points table (OpenAI-compatible /v1/chat/completions, Anthropic /v1/messages). A doc-organization convention, not one named object. | `IntegrationPoints` |
| ~ | hinted | doc-only | **interactive claude code** - the recurring heading (6 docs; anchored in docs/qwen36-claude-dogfood-playbook.md) for launching an interactive Claude Code session against a local model through fak (the fak-qwen36-claude preset without --probe) — the dogfood surface where Claude Code runs on Qwen3.6 behind the kernel; a usage-mode name, not a code identifier | `InteractiveClaudeCode` |
| ~ | hinted | doc-only | **lifecycle ladder** - the recurring concept (4 docs; docs/MATURITY-SCORECARD.md and CONCEPT-MATURITY-LADDER-2026-06-29.md) naming the closed, total-ordered feature-maturity ladder every internal/<leaf> lane in dos.toml is placed on: proposed -> prototyped -> tested -> dogfooded -> default, plus a benchmarked badge — each rung reached by evidence the author did not write. | `LifecycleLadder` |
| ~ | hinted | doc-only | **live mode** - mostly a matching artifact: the 32-doc presence count is dominated by the substring inside 'Live Model ...' text/headings ('live model' contains 'live mode' under alphanumeric normalization); the only literal heading found is a code-block comment '# Live mode' in docs/fak/related-items.md for running fleet watchdog tools with --live (act) instead of dry-run | `LiveMode` |
| ~ | hinted | doc-only | **load-bearing invariants** - a doc-section convention (9 docs; docs/benchmarks/ROPE-SCALING-RESULTS.md, SLIDING-WINDOW-RESULTS.md, docs/dispatch-loop.md): the section enumerating the hard guarantees a feature rests on (e.g. 'unset == default == byte-identical'), each backed by a named test rather than asserted; a writing discipline for guarantees, not one system concept | `LoadBearingInvariants` |
| ~ | hinted | doc-only | **local models** - a doc-structure convention of the integration guides (17 docs; docs/integrations/aider|cline|continue|claude|zed|vscode|jetbrains.md): the config-recipe section for fronting a locally-served model (Ollama, Qwen presets) with fak serve, paired with a sibling 'Cloud providers' section; a template slot, not a system concept | `LocalModels` |
| ~ | hinted | doc-only | **long-context defaults doctrine** - the recurring concept (5 docs; title of docs/long-context-defaults.md): the canonical policy layer above the mechanism docs stating that the model's advertised context window is a hard cap not a target, and governing how large --compact-history-budget, --ctx-view-budget, and future automatic context-budget defaults may make the model-visible working set. | `LongContextDefaultsDoctrine` |
| ~ | hinted | doc-only | **long-session economics** - the recurring concept (4 docs; title of docs/explainers/long-session-economics.md): the explainer of why long agent runs get expensive — every turn re-sends the whole transcript, provider prompt caches only discount while the prompt front stays byte-for-byte identical — written for readers with no fak internals. An explainer-level concept, not a code object. | `LongSessionEconomics` |
| ~ | hinted | doc-only | **machine registry** - the recurring heading (4 docs; section of docs/notes/HARDWARE-CATALOG.md) pointing at the live machine-readable benchmark-machine catalog: experiments/benchmark/catalog.json, managed via tools/bench_catalog.py. The registry file is real; the name exists only as this doc heading. | `MachineRegistry` |
| ~ | hinted | doc-only | **mcp over stdio** - the recurring heading (8 docs; docs/fak/agent-integration-architecture.md, FAQ, integrations) for the `fak serve --stdio` transport: newline-delimited JSON-RPC over stdin/stdout with no listener and no auth surface, serving the same dispatch as HTTP /mcp; a transport-mode concept | `McpOverStdio` |
| ~ | hinted | doc-only | **mcp tools** - the recurring heading (12 docs; docs/integrations/cursor.md, docs/supported/apis-and-protocols.md) for the fixed set of fak_* tools the MCP server exposes (fak_adjudicate, fak_syscall, fak_admit, fak_changes, fak_revoke, fak_context_change), each mirroring a fak-native request DTO | `McpTools` |
| ~ | hinted | doc-only | **measurement plan** - a doc-section convention of research notes (7 docs; e.g. docs/notes/RESEARCH-grammar-constrained-tool-call-decoding): the section stating how a claim will actually be measured (which corpus, which models, which execution path) before any number is quoted; a research-note template slot | `MeasurementPlan` |
| ~ | hinted | doc-only | **measurement status** - the recurring benchmark-results section (4 docs: SWEBENCH, SESSION-VALUE-STACK, webbench docs) declaring dataset, model, run count, artifacts, and a status label per number — MEASURED vs THEORETICAL (MODELED) vs not measured/verified. The evidence-labeling convention that keeps modeled numbers from posing as measured ones. | `MeasurementStatus` |
| ~ | hinted | doc-only | **metadata contract** - the recurring heading (6 docs; anchored in docs/playbooks/research-agent.md) for what the admit_and_log metadata must carry and why: audit visibility (would_deny names the exact policy gap admitted) plus the promotion-ledger signal; a policy-surface contract stated only in prose | `MetadataContract` |
| ~ | hinted | doc-only | **metrics endpoint** - the recurring heading (5 docs, docs/fak/) documenting fak serve's GET /metrics Prometheus endpoint — HTTP latency/status, verdict counters, and kernel counters (submits, denies, vDSO hits). A real gateway feature that in docs is only ever named by this generic heading, with no single code identifier called out. | `MetricsEndpoint` |
| ~ | hinted | doc-only | **missing evidence** - the recurring benchmark submission-packet section (5 docs, docs/benchmarks/) enumerating the artifacts that do not exist yet and must be checked in and hash-pinned before the packet can leave the BLOCKED_PRECREDENTIAL state (e.g. official repo revision, run contract, raw-arm generations). A packet-structure convention. | `MissingEvidence` |
| ~ | hinted | doc-only | **model integration** - the recurring phrase (4 docs); as a heading it is a to-do checklist subsection of docs/webbench-real-measurements-summary.md for wiring a real model API (Claude, GPT-4, or local) into the WebBench measurement rig — connect API, turn-by-turn execution, capture real token counts. A generic work-item label, not a repo concept. | `ModelIntegration` |
| ~ | hinted | doc-only | **named residual** - a doc-section convention of docs/notes/RESEARCH-*-triage notes (11 docs): the section naming the residual gap an external candidate exposes in fak (e.g. audit provenance, floor coverage), explicitly 'FILED not built' — the gap becomes an issue rather than immediate work; a triage-workflow concept | `NamedResidual` |
| ~ | hinted | doc-only | **one-command setup** - a doc-structure convention of the integration guides (7 docs; docs/integrations/* 'Quick Start: One-command setup', docs/fak/agent-integration-architecture.md): the fastest single command/script (e.g. scripts/dogfood-claude.sh) that builds fak and puts the kernel in front of a given tool; a quickstart template slot | `OneCommandSetup` |
| ~ | hinted | doc-only | **one-line test** - a doc-structure convention of docs/explainers/ (6 docs; agent-virtual-filesystem.md, memory-engineering.md): the closing single-sentence litmus question ('if your stack cannot answer X, you have features, not engineering') that compresses the explainer into one falsifiable test of the reader's own system; a rhetorical template slot | `OneLineTest` |
| ~ | hinted | doc-only | **one-paragraph version** - the recurring summary section (5 docs: EXECUTIVE-ROLLUP, model-routing, explainers, notes) titled 'The one-paragraph version' — a single-paragraph honest abstract of the whole doc placed near the top. Purely a doc-structure convention for skimmability. | `OneParagraphVersion` |
| ~ | hinted | doc-only | **openai-compatible surface** - the recurring heading (8 docs; anchored in docs/fak/api-reference.md) for the gateway's POST /v1/chat/completions adjudication proxy — the wire that lets any OpenAI-speaking tool point its base URL at fak; an API-surface concept, not a code identifier | `OpenAiCompatibleSurface` |
| ~ | hinted | doc-only | **operator readout** - the recurring heading (8 docs, anchored in docs/managed-context-continuous-usage.md) for the minimum honest status text an operator must be shown after a hidden context reset or long-run handoff — what survived, what was rebuilt, what is unverified; an operator-honesty convention of the managed-context docs | `OperatorReadout` |
| ~ | hinted | doc-only | **operator runbook** - a doc-structure convention (8 docs; docs/playbooks/coding-dev-agent.md, devops-sre.md, docs/notes/RELAY-OPERATOR-RUNBOOK): the step-by-step human procedure section (ship-gate flow, change-request escalation, start/observe/tune/park-resume); a document kind, not a system concept | `OperatorRunbook` |
| ~ | hinted | doc-only | **parent context** - a doc-section convention of grooming/cohort docs (7 docs; docs/blast-radius-containment-cohort.md, docs/notes/ backlogs): the per-item section naming the parent epic/milestone/program the item belongs to and its dependency siblings; a backlog-template slot, not a system concept | `ParentContext` |
| ~ | hinted | doc-only | **policy manifest schema** - the recurring heading (5 docs; section of docs/fak/server-config.md) documenting the JSON capability-floor manifest loaded by --policy, versioned fak-policy/v1, with fak policy --dump emitting the built-in DefaultPolicy as a template. The manifest is real and consumed by code, but the schema is described only in docs under this heading. | `PolicyManifestSchema` |
| ~ | hinted | doc-only | **portable agent-routing schema** - the recurring concept (4 docs; title of docs/standards/agent-routing-schema.md): a normative, declarative, version-tagged routing file that — unlike request-granularity LLM routers (OpenRouter, LiteLLM, etc.) — routes at aspect granularity (whole request, one tool call, sub-query, planner state, reasoning step) to different models, with ensembles folded by a Reduction as first-class plans. | `AgentRoutingSchema` |
| ~ | hinted | doc-only | **portable taint-check schema** - the recurring concept (4 docs; title of docs/standards/taint-check-schema.md): a normative portable contract for provenance-keyed information-flow control — kernel-authored taints stamped on values and propagated, refusing tainted-to-sensitive-sink flows before execution, because content detectors are sound-but-evadable while provenance cannot be paraphrased away. Shipped spine in internal/ifc (Ref.Taint). | `TaintCheckSchema` |
| ~ | hinted | doc-only | **portfolio fold** - the recurring section (5 docs, docs/notes/ score notes) that folds one score or observability run into the portfolio-level scorecard numbers — e.g. the scorecard_control_pane.py totals (finding, total_debt, grade_debt, measured, errored). A score-note reporting convention, not a code object. | `PortfolioFold` |
| ~ | hinted | doc-only | **priority order** - a doc-structure convention (6 docs; docs/notes/ fix lists and next-steps sections): an ordered list of actions ranked by what to do first ('Fixes (in priority order)', 'Next steps (priority order)'); generic list boilerplate, not a system concept | `PriorityOrder` |
| ~ | hinted | doc-only | **private bridge** - the recurring term (12 docs; docs/fak/lab-dev-loop.md, docs/fleet.md, docs/nightrun/) for the private Slack-protocol control channel that reaches the lab GPU boxes — it carries a host, channel id, and token that never enter the public repo; the public entry point is the stub docs/private-comms-channel.md; a deliberately unbound (private) system concept | `PrivateBridge` |
| ~ | hinted | doc-only | **problems found** - the recurring docs/launch section (4 docs) recording the adversarial fact-check of a draft launch post: each numbered problem names a mislabeled or non-ledger-backed claim (e.g. splicing a WebVoyager number with a different rig's vs-tuned figure) and its fix. Pairs with the CORRECTED ASSET section; a launch-subtree convention. | `ProblemsFound` |
| ~ | hinted | doc-only | **promotion rules** - the recurring generation-planning section (5 docs, docs/notes/GENERATION-*) stating the evidence-driven rules for moving backlog items between horizons: future -> second-next -> next -> now, each transition requiring a named kind of evidence (concrete option, simulation/policy evidence, landed gate or witness). Process doctrine, not code. | `PromotionRules` |
| ~ | hinted | doc-only | **promotion verbs** - the recurring generation-process section (4 docs; canonical in docs/generation.md) defining the closed verb set for moving backlog items between horizons: promote (evidence retired the blocker), demote (assumption failed or witness regressed), retire (obsolete/superseded), park (true but ownerless). A process vocabulary, not code. | `PromotionVerbs` |
| ~ | hinted | doc-only | **provenance labels** - the recurring heading (14 docs, anchored in docs/long-context-defaults.md) for the closed evidence-provenance vocabulary (WITNESSED / OBSERVED / ...) that grades how a quoted number or budget is known — local witnessed run vs external telemetry vs assumption; a real honesty-vocabulary concept with no code identifier | `ProvenanceLabels` |
| ~ | hinted | doc-only | **public tree** - the recurring term (13 docs, anchored at docs/gpu-server-private-boundary.md '## Public tree') for the public fak repository side of the public/private lab boundary: it may keep scrubbed GPU-server evidence but only in generic language (no lab host/IP/path/token) and must pass the scrub; a real boundary concept | `PublicTree` |
| ~ | hinted | latent-literal | **quarter-hour in seconds** - a bare 900 on non-const lines in ~8 production files, dominated by 900 seconds = 15 minutes: fleet DefaultStaleSec = 900, nightrun DefaultTaskTimeoutSec = 900, guardrsi DefaultCorrelateWindowSecs = 900, resume EffectiveReuse5mSeconds = 900 (the 5m billing tier's ~15-min warm window) and latency-histogram bucket edges - each 15-minute duration named per-site, the shared unit never; conflated with 900-token turn sizes in loopgate/loopbench fixtures, rng.Intn(900) mock amounts and issue #900 references in terminalbench | `quarterHourSeconds` |
| ~ | hinted | doc-only | **queried skill loader** - the recurring concept (4 docs; title of docs/SKILL-LOADER-QUERY-EPIC.md): the epic thesis that a skill should be queried/faulted in like a KV page rather than loaded up front — removing the O(N) system-prompt tax of listing all skill descriptions, making a skill a paged capability with zero marginal context cost at rest, eviction under pressure, and HIT serving on repeat invocation. | `QueriedSkillLoader` |
| ~ | hinted | doc-only | **quick reference** - the recurring section (5 docs: benchmark README, troubleshooting, integrations) giving a condensed command or vocabulary lookup — first-time setup commands, common commands, decision vocabulary. A generic doc-structure convention with no repo-specific meaning. | `QuickReference` |
| ~ | hinted | doc-only | **rate limiting** - the recurring heading (8 docs; docs/fak/server-config.md, docs/integrations/customer-support-playbook.md) for the gateway's per-key admitted-call quota, configured via FAK_RATELIMIT_* env vars or a policy `rate_limit` block (default disabled); a generic term for a specific config surface | `RateLimiting` |
| ~ | hinted | doc-only | **readiness ladder** - the recurring heading (6 docs, anchored in docs/persona-scorecard/README.md) for the ordered verdict vocabulary the persona scorecards grade with (e.g. '* served' = every hard affordance present); the rung table that turns scorecard evidence into a closed verdict; a scorecard-family concept | `ReadinessLadder` |
| ~ | hinted | doc-only | **recommended path** - the recurring section (4 docs, docs/benchmarking/) giving a phased recommendation out of an analysis — e.g. Phase 0 proxy mode (fak serve --base-url), Phase 1 external SGLang/vLLM engine, Phase 2 KV-coherence via abi.RegisterKVBackend. An analysis-conclusion convention, not a system object. | `RecommendedPath` |
| ~ | hinted | doc-only | **related issues** - a doc-structure convention (7 docs; docs/webbench-blockers.md, docs/notes/ design notes): the section listing the GitHub issues (with status) that track or intersect the doc's topic; a cross-reference template slot, not a system concept | `RelatedIssues` |
| ~ | hinted | doc-only | **related reading** - the recurring cross-link section (4 docs, docs/explainers/ and notes) listing companion docs with one-line reasons to read each (e.g. memory-engineering linking the four memory explainers). A pure doc-structure convention for navigation. | `RelatedReading` |
| ~ | hinted | doc-only | **relay reason vocabulary** - the recurring concept (4 docs; title of docs/notes/RELAY-REASON-VOCABULARY-2026-07-01.md): a data-only closed token vocabulary for relay legs — every row in the dos check-reason shape (token, category, summary, fix) — so legs never explain state with prose. Explicitly not consumed by any gate yet and not the exclusive dos.toml reason table. | `RelayReasonVocabulary` |
| ~ | hinted | doc-only | **remaining gap** - a doc-structure convention of the honesty culture (14 docs; docs/notes/ audits and backlogs): the section that names what is still missing after the work described — e.g. the missing grammar compiler in STRUCTURED-GENERATION-SOTA-BACKLOG, operator decisions in the DOS usage audit; a reporting pattern, not a system concept | `RemainingGap` |
| ~ | hinted | doc-only | **reproduce a denial offline** - the recurring integration-guide section (5 docs: docs/integrations/amp, gemini-cli, opencode, roo-code) showing the same fak preflight invocation that reproduces a policy denial fully offline (verdict=DENY reason=POLICY_BLOCK) with no model or network. A copy-pasted harness-guide convention demonstrating the preflight verb. | `OfflineDenialRepro` |
| ~ | hinted | doc-only | **reuse curve** - the recurring heading (6 docs; docs/benchmarking/FINAL-ANALYSIS.md, SESSION-SUMMARY.md) for the benchmark chart of KV-reuse speedup scaling across model sizes (135M -> 3B), built from the demo server's /api/curve endpoint; a benchmark-artifact name with no code identifier | `ReuseCurve` |
| ~ | hinted | doc-only | **schema validation** - the recurring heading (9 docs; anchored in docs/benchmark/README.md) for validating benchmark artifacts against the JSON Schemas in tools/schemas/ (machine-specs.v1.json, run-manifest.v1.json, ...); a generic engineering term applied to the benchmark artifact pipeline | `SchemaValidation` |
| ~ | hinted | doc-only | **scope fences** - the recurring notes section (5 docs, docs/notes/) that states what a number or result explicitly does NOT claim — e.g. the managed-cache proving ground grades witness reachability, never savings quality. Part of the repo's caveat-section family (with Honest limits / Honest residue); a doc-honesty convention. | `ScopeFences` |
| ~ | hinted | doc-only | **scout calibration** - a doc-section convention of docs/notes/RESEARCH-*-triage notes (13 docs): the section grading whether the research scout's topic scoring (topic, score, term hits, freshness/stars signals) surfaced an on-mission candidate — feedback that tunes the scout; a workflow concept with no code identifier in these docs | `ScoutCalibration` |
| ~ | hinted | latent-literal | **seconds per hour** - a bare 3600 on non-const lines in ~15 production files with one dominant meaning: seconds per hour - unit conversions (sec/3600, s%3600, *silentHours*3600, resetTZOffset*3600) in cmd/fak loop, loophealth, mutationbudget, resume watchdog/rehome and fleetsim rate math, plus 1-hour TTLs and cadences that each package names per-site (resume EffectiveReuse1hSeconds, leaseref DefaultIntentTTLSeconds, garden gardenTickIntervalSeconds, loopmgr DefaultHealthCadenceSeconds, loopDriveRegionTTLS); the conversion factor itself is never named | `secondsPerHour` |
| ~ | hinted | latent-literal | **seconds-per-day 86400** - a bare 86400 on non-const lines in ~6 production files - always seconds-per-day (day formatting in cmd/fak loop/sweep/release_status, day math in releasestale/resume/repoguard), unambiguous but never named, so day-arithmetic sites cannot be found by search | `secondsPerDay` |
| ~ | hinted | doc-only | **security model** - the recurring architecture-doc section (4 docs) describing a component's security design: default-deny posture and the closed refusal vocabulary in docs/fak/agent-integration-architecture.md; no-generic-exec, method-allowlist controls in docs/agent-machine-link-protocol.md. A section convention naming per-component security designs, not one object. | `SecurityModel` |
| ~ | hinted | doc-only | **security posture** - the recurring section/phrase (4 docs) describing the operational security stance of an integration — e.g. the Slack control foundation's rules (gate on immutable user IDs, one control channel, dedupe on event_id, lethal-trifecta governing rule) and a FAQ heading on completing an agent security posture. A doc convention overlapping with Security Model. | `SecurityPosture` |
| ~ | hinted | doc-only | **session value stack** - the recurring heading (8 docs; anchored in docs/benchmarking/README.md) for the multi-turn x multi-agent session-efficiency benchmark: ~4.1x vs a tuned warm per-agent KV cache on a 50-turn x 5-agent session (the ~60x figure is only vs the naive re-prefill floor); detailed in the private SESSION-VALUE-STACK-DECK.md companion | `SessionValueStack` |
| ~ | hinted | doc-only | **shared objects** - the recurring heading (5 docs; section of docs/fak/api-reference.md) grouping the wire objects shared across all fak-native API responses — chiefly the verdict object (WireVerdict). A doc-organization heading for the API reference, not itself a system concept (the objects under it are). | `SharedObjects` |
| ~ | hinted | doc-only | **shared state ladder** - the title concept of docs/shared-state-ladder.md (6 docs reference it): the vocabulary that keeps five related-but-different kinds of shared state in fak from being treated as one feature; a real taxonomy concept whose only home is that doc | `SharedStateLadder` |
| ~ | hinted | doc-only | **shared task record contract** - the recurring concept (5 docs; title of docs/shared-task-record-contract.md): the adapter-neutral contract for user-level shared task state — the rung where humans and agents co-edit a task board without turning edits into unstructured chat. Ships as JSON envelope schemas (tools/schemas/shared-*.json), runnable fixtures under examples/shared-task-record*, and an in-memory reference fold in internal/sharedtask. | `SharedTaskRecordContract` |
| ~ | hinted | doc-only | **ship discipline** - a doc-section convention of grooming notes (6 docs; docs/notes/MANAGED-CACHE-PROVING-GROUND, AGENTIC-LIVE-STATUS, WORKER-UNIT-CONSERVATION): the per-issue block restating the trunk rules — trunk only, commit by explicit path, Conventional-Commits subject + (fak <leaf>) stamp — plus an honest-scope fence for the claim being shipped; a restatement of the AGENTS.md contract as a template slot | `ShipDiscipline` |
| ~ | hinted | doc-only | **short answer** - a doc-structure convention (14 docs): the opening TL;DR slot — a '## Short answer' section (docs/notes/STRUCTURED-GENERATION-SOTA-BACKLOG) or a bold '**Short answer.**' lead paragraph in docs/explainers/ — giving the one-paragraph verdict before the long form; pure writing pattern | `ShortAnswer` |
| ~ | hinted | doc-only | **six primitives** - the recurring concept (4 docs; heading in docs/notes/context-safety-visual-primitive-schemas-1217.md) naming the six context-safety visualization primitives, each specified by what kernel surface it consumes — e.g. the context-event ledger ribbon consuming the hash-chained journal's CAP_EVICT/CAP_FAULT/CAP_VERSION_BIND rows and cache-event recorder tiers. A design-note taxonomy, not shipped code. | `SixPrimitives` |
| ~ | hinted | doc-only | **socket mode** - an external Slack technology (4 docs; heading in docs/notes/SLACK-CONTROL-FOUNDATION-2026-07-02.md): Slack's WebSocket-based delivery where events, slash commands, and block_actions arrive over a pre-authenticated socket with zero public ingress — documented as the transport choice for the planned Slack control channel. Not a repo concept. | `SocketMode` |
| ~ | hinted | doc-only | **sota comparison** - the recurring heading (6 docs; docs/webbench-baselines.md, docs/webbench-real-measurements-summary.md) for comparing fak's webbench numbers against published state-of-the-art baselines — explicitly tracked as still-to-run live work (checklists of pending measurements) versus the modeled floor already published; a benchmark-program concept | `SotaComparison` |
| ~ | hinted | doc-only | **source map** - the recurring section (4 docs: CACHE-FRONTIER-OPERATING-PLAN and notes) listing the source docs and links behind a page 'so the next agent does not re-trace' them — a bibliography of repo-internal pointers. A doc-navigation convention; unrelated to JavaScript source maps. | `SourceMap` |
| ~ | hinted | doc-only | **standing at a glance** - a doc-structure convention of the scorecard READMEs (6+ docs; docs/*-scorecard/README.md, docs/PRODUCT-STATUS.md): the section holding the regenerable one-line chart block (score, grade, debt counts) emitted by each scorecard's tools/*.py generator; a scorecard-template slot, not a system concept | `StandingAtAGlance` |
| ~ | hinted | doc-only | **status legend** - the recurring section (4 docs, docs/notes/) defining the closed status tokens a doc's tables use — e.g. runnable-now, run-contract-shipped, hardware-gated, adapter-gap, negative-witness in the benchmark run-packets note. A per-doc vocabulary-table convention; the token sets differ between docs. | `StatusLegend` |
| ~ | hinted | doc-only | **support-maturity honesty fence** - the title concept of docs/standards/support-maturity-honesty-fence.md (9 docs reference it): the graded, ordered ladder (M0 none -> M1 fenced -> M2 loads -> ... correct/optimized/SOTA-parity) that replaces the fuzzy word 'supported', from the support-maturity epic #1243; a real standards concept | `SupportMaturityFence` |
| ~ | hinted | doc-only | **system-prompt-mutation schema** - the recurring concept (4 docs; title of docs/standards/system-prompt-mutation-schema.md, plus negative fixtures under docs/standards/fixtures/system-prompt-mutation-invalid/): a portable normative schema for gating a self-improving agent's edits to its own base context, targeting the documented failure modes of context collapse / brevity bias (ACE) and misevolution. | `SystemPromptMutationSchema` |
| ~ | hinted | doc-only | **target model** - a doc-section convention of benchmark/results docs (10 docs; e.g. docs/benchmarks/IN-KERNEL-MODEL-RESULTS.md '## The target model'): the section naming the model under test and why it was chosen (SmolLM2-135M as the tractable, checkable Llama-architecture target); a results-doc template slot | `TargetModel` |
| ~ | hinted | doc-only | **test runner** - the recurring heading (7 docs, anchored at docs/dev-tooling.md '## The test runner') for `fak test`, the host-aware runner that resolves the right go test invocation per tier and on Windows routes through test.ps1/WSL to dodge the OS-policy block; a real tool the heading names but no doc binds explicitly | `TestRunner` |
| ~ | hinted | latent-literal | **the 0.20 weight/threshold literal** - a bare 0.20 on non-const lines in ~6 production files - almost always a one-fifth scoring weight or calibration threshold (dojo's GradeB calibration band, fusion-traffic target ratio), while sibling packages already name their own 0.20 const (wPin, selectWStaleness, DefaultTau, wStaleness) - the bare sites are the unnamed stragglers of an otherwise-named family | `fifthWeight` |
| ~ | hinted | latent-literal | **the GLM-5.2 version designator** - a bare 5.2 on non-const lines in ~19 production files, almost always inside the GLM-5.2 / glm-5.2 model designator: inline model-id strings (webbench-run --model glm-5.2, dispatchtick's "zai-coding-plan/glm-5.2", covmatrix "GLM-5.2-DSA") and pervasive comments/flag help naming the model family across agent, cachemeta, compute, ggufload and the glm* commands; only provider-specific ids are named (fleetaccounts NIMGLM52Model = "z-ai/glm-5.2"), the family designator itself is rebuilt inline everywhere | `glm52ModelFamily` |
| ~ | hinted | latent-literal | **the conflated 0.05 literal** - a bare 0.05 on non-const lines in ~8 production files: a 5% band with several distinct owners - regression/ratchet tolerances (cachevalueledger TrendRegressionTolerance, milestonereport Epsilon), rate thresholds (gateway DefaultStaleReadRatio, vcachescore DefaultMaxFalseWarmRate, resume RecoveryBand ColdMax), the quant oracle tolerance max|delta|<0.05 (model parallel/quant prose), SPRT alpha=beta=0.05 design (turnbench), a 0.05 GPU headroom (serveGGUFExpertParallelDeviceHeadroom), rsiloop's 0.05 gain step, and turnbench rate/share mix entries; each named use is package-private, the bare uses reference the same 5% idea without a name | `defaultToleranceEpsilon` |
| ~ | hinted | latent-literal | **the conflated 0.10 literal** - a bare 0.10 on non-const lines in ~7 production files conflating several 10% quantities: the cold-reuse regime ceiling reuse < 0.10 (named ColdCeil in cacheobs, restated in gateway metrics help text); the batch scheduler's default 0.10 pad-overhead bound (batchsched comments and Policy default); the in-kernel request trim margin (named inKernelRequestPressureTrimMarginRatio); 0.10 scorecard weights (scorecardpane redundancy/placement, ctxplan wHorizon); the dojo calibration band edge CalibratedMax: 0.10; and turnbench P10 percentile picks and rate-profile shares | `defaultMaxPadOverhead` |
| ~ | hinted | latent-literal | **the conflated 0.2 literal** - a bare 0.2 on non-const lines in ~7 production files with no shared owner: ctxplan's forecast Recency weight (Weights{..., Recency: 0.2}, with the ctxplanbench --primacy flag documented as '0.2 symmetric with recency') - the most connected use; fleet fold score shares (0.2 reach + 0.2 version coverage in the documented formula); a dogfoodscore share (cShare 0.2); the 1h-TTL cache refresh add-on in gateway cache-pricing prose (2.0 + 0.2); a synthetic-model logit scale; a tmux split-pane size ('-s 0.2'); and a ctxplan assumption fallback return | `recencyWeight` |
| ~ | hinted | latent-literal | **the conflated 0.25 literal** - a bare 0.25 on non-const lines in ~11 production files conflating at least five concepts: quarter score-weights each named privately where named at all (ctxplan wIntent, loopscore dogfoodWeight, heavinessscore metaShareRef, ctxplan falseRetainPenalty, the 0.25 false-cold penalty factor in vcachescore); the starving fault-ratio threshold fr > 0.25 (ctxplan signalnoise + snfitness); maxUnrouted = 0.25 var defaults in the dispatchtick router; the small-tier $0.25/Mtok input price in the modelroute price book and routebench examples; plus a latency-histogram bucket edge, the AWQ alpha grid point and sessionbench sweep fractions | `starvingFaultRatioMax` |
| ~ | hinted | latent-literal | **the conflated 0.50 literal** - a bare 0.50 on non-const lines in ~6 production files with distinct owners: the P50/median percentile pick (webbench serving, turnbench stochastic); memorystability DefaultBudget = 0.50; resume RecoveryBand WarmMin: 0.50; the 0.50 flop/byte memory-bandwidth roofline figure in model batch/quant doc prose; a product-scorecard honesty weight (0.50*honestyScore); an eveparity coverage fixture; and turnbench ProfileHigh PureRate - the same half-fraction, at least six unrelated meanings | `medianQuantile` |
| ~ | hinted | latent-literal | **the conflated 0.60 literal** - a bare 0.60 on non-const lines in ~7 production files with distinct owners: memorystability's DefaultFloor = 0.60; the scorecardpane severity ladder step (0.90/0.80/0.70/0.60); the dojo calibration grade-D band edge; a product-scorecard honesty weight (0.60*honestyScore); a TUI Jaccard near-duplicate threshold (>= 0.60); the relay-vs-compaction rotate point (~0.60 of the window); and a 0.60-0.97x throughput range quoted in ailuminate contract prose - the same number, at least six unrelated meanings | `defaultScoreFloor` |
| ~ | hinted | latent-literal | **the conflated 0.8 literal** - a bare 0.8 on non-const lines in ~6 production files: several distinct 80% thresholds - the --budget-warn-fraction 0.8 default (cmd/fak/serve.go flag plus internal/session observe docs restating '0.8 warns at 80%'), the ctxplan lean-regime bound (Ratio >= 0.8 in signalnoise and snfitness), vcachecal WarmRateFloor/ColdRateFloor = 0.8 (named), gateway ctxCadenceCheckpointShare = 0.8 (named), an rsiloop metric band edge, a dispatch-progress achieved >= target*0.8 check and a nightrun weight | `budgetWarnFraction` |
| ~ | hinted | latent-literal | **the conflated 0.90 literal** - a bare 0.90 on non-const lines in ~7 production files conflating several 90% quantities: 0.90 confidence/severity bars (selfquery ask-policy confidence floor, turnbench OPE confidence >= 0.90, scorecardpane severity ladder top step) - the most frequent family; the frozen-reuse regime floor reuse >= 0.90 (named FrozenFloor in cacheobs and restated in gateway metrics help text); metalQ8UploadFraction = 0.90 GPU-copy budget; loopscore passShare = 0.90; the P90 percentile pick (webbench, turnbench); and an eveparity fixture score | `highConfidenceBar` |
| ~ | hinted | latent-literal | **the conflated 120 literal** - a bare 120 on non-const lines in ~37 production files conflating at least six concepts: 120-char one-line clips (truncate/headLine/clip in agent/loop.go, ctxplan/store.go, memq, recall, dogfoodscore, issuecontractrepair, learningdebt) - the most widespread use; 120-second HTTP/exec timeouts (loadgen, guard_replay, release_status, scorecardpane, gateway IdleTimeout); the 120-minute dispatcher cooldown (named DefaultCooldownMinutes and DefaultCooldownSeconds=120*60 in dispatchtick/dispatchorder); 120-column render widths (tui flags); 120 decode tokens per turn (fanbench, turnbench fixtures); and 120ms latency/ticker values (demoui, modelroute Opus-class latency) | `oneLineClipChars` |
| ~ | hinted | latent-literal | **the conflated 127 literal** - a bare 127 on non-const lines in ~21 production files conflating four concepts: the int8 quantization max magnitude (d = amax/127 and +/-127 clamps across internal/compute, internal/model quant kernels and ggufload dequant - the dominant use), the shell exit code 127 for command-not-found (cmd/fak loop/node/release/main, edittx, qwen36nodereports, dispatchworker), the IEEE-754 float32 exponent bias (sign | uint32(exp+127)<<23 in quantbits/quant_q4k f16-to-f32 conversion, e8m0 2^(e-127) notes), and {0,127} regex length caps in commitintent; none of the four is named as a const anywhere | `q8MaxMagnitude` |
| ~ | hinted | latent-literal | **the conflated 128 literal** - a bare 128 on non-const lines in ~27 production files conflating several concepts: the attention head-dim 128 in model geometry (sessionbench/cachemeta KVConfig HeadDim, glmdsatput MLA qk-nope/v-head defaults, 128-wide dequant chunks in ggufload/model quant kernels) - the dominant use; ctxplan's bounded-probe candidate cap (named DefaultMaxCandidates); CUDA graphPrewarmDepth and ncclUniqueIDBytes (named); the ABI event-block boundary (EvRungLabel EventKind = 128, Range{96,128}); 128-token max-new flag defaults (simpledemo); ksignsIQ2XS[128] GGML tables; and batch/prefill sweep sizes | `defaultHeadDim` |
| ~ | hinted | latent-literal | **the conflated 180 literal** - a bare 180 on non-const lines in ~8 production files with no dominant owner: 180-char clips (adjudicator reversibility declares it only as a bare const max = 180, cmd/fak truncStatus(detail, 180) and sessions_codex_loop's own const max = 180), 180-second subprocess timeouts (release_status python calls), 180-day recency windows (fleetaccounts soonness 180*24*time.Hour, ideascout RecentDays: 180), 180-token demo table entries (tokendemo), an IQ3 table byte, and issue #180 references | `statusClipChars` |
| ~ | hinted | latent-literal | **the conflated 2000 literal** - a bare 2000 on non-const lines in ~8 production files conflating at least five concepts: 2000-char output tails - the dominant use (ablate tailString(stdout, 2000), qwen36nodereports tail(out, 2000), webbench-run truncateContent 2000, safecommit and selfinstall each declaring their own bare const max = 2000); adjReps = 2000 adjudication repetitions duplicated as a const in both internal/bench and internal/turnbench; dispatchtick ProcGuardDefaultMaxThreads = 2000; 2000ms thresholds (webbench --slo-ms default, preflight DefaultWarmBuildThresholdMS); a <=2000 context-tokens-left warning band in the TUI; and pervasive epic #2000 issue references | `outputTailChars` |
| ~ | hinted | latent-literal | **the conflated 256 literal** - a bare 256 on non-const lines in ~42 production files conflating at least five concepts: 256-token default decode budgets (envInt("FAK_INKERNEL_MAX_TOKENS", 256), fakchat --max-new, trychatdemo max_output_tokens), the 256-weight k-quant super-block (named q4kSuper/qkK/q4kSuperBlockWeights where declared, but recurring bare in dequant prose and dims like qkNope=192/vHead=256), 256-entry bounded caps that each package names privately (maxCacheEventKeys, sessionActivityCap, dedupCap, DefaultQueue, InlineMax, maxRCEShellSources), P=256 prefill/batch sweep sizes, and 256-byte vocab / C name buffers ([256]C.char); SHA-256 rides along in comments and identifiers | `defaultMaxNewTokens` |
| ~ | hinted | latent-literal | **the conflated 3.0 literal** - a bare 3.0 on non-const lines in ~6 production files conflating at least six concepts: the $3/Mtok frontier input-token price (bench.go's cost formula, sessionaudit's sonnet/fable Input: 3.0, fanbench --dollars-in default, turnbench DollarsPerMTokIn) - the dominant use; toolproc DefaultStallMultiplier = 3.0; a decode-roofline scalar declared twice as a bare const scalar = 3.0 (model decode_roofline.go and profile.go); vcacheobserve spikeRatio = 3.0; cachevaluereport minHonestSpanDays = 3.0; procguard DefaultCPUWindowSec = 3.0; and a /3.0 average in turnbench parity | `inputDollarsPerMTok` |
| ~ | hinted | latent-literal | **the conflated 300 literal** - a bare 300 on non-const lines in ~40 production files that conflates at least four distinct concepts: 300ms settle sleeps, the HTTP redirect boundary (StatusCode >= 300), a compaction-shed token budget, and sweep sizes - none of them named | `settleSleepMillis` |
| ~ | hinted | latent-literal | **the conflated 4000 literal** - a bare 4000 on non-const lines in ~7 production files, mostly ~4k-token budgets that are related but independently owned: fanbench's --fold-budget 4000 and turnbench FoldTurnTokenBudget: 4000 (the master's per-fold synthesis budget), resume DefaultSeedTokens = 4000, the 4000-token hot prefix driven through hwcachedemo/cxlpooldemo, the 4000/400k TPM tier in vcachegov warmbudget prose, and turnbench's frontier '4s, 4000tok' cost row; conflated with 4000-rune/byte output tails (cmd/fak testrepair testOutputTailRunes, advmodel max_bytes) and a 4000-iteration demo flag default | `defaultFoldTokenBudget` |
| ~ | hinted | latent-literal | **the conflated 8000 literal** - a bare 8000 on non-const lines in ~12 production files with a dominant meaning plus two stragglers: the default --ctx-view-budget of 8000 resident tokens, inlined at both front doors (cmd/fak/serve.go and guard.go flag defaults), re-asserted textually by the token_defaults/servewiring/loopindex self-checks, and echoed in guard_replay, ctxplanbench, ctxplandemo and horizonrecovery budgets; conflated with the vLLM port :8000 (webbench serving command, nightrun backlog ":8000/:8080") and an 8000-byte head clip in renameconcept; frontierswe separately names its own 8000 as defaultPrefixTokens | `defaultCtxViewBudget` |
| ~ | hinted | doc-only | **verdict object** - the recurring API-reference heading (4 docs; docs/fak/api-reference.md 'The verdict object') for the wire shape every fak-native response carries — fields kind (closed set ALLOW/DENY/TRANSFORM/QUARANTINE/REQUIRE_WITNESS/DEFER), reason (closed refusal vocabulary), by (adjudicator). The code name is WireVerdict; the doc heading uses this unnamed generic phrase instead. | `VerdictObject` |
| ~ | hinted | doc-only | **verification commands** - the recurring section (4 docs: docs/branch-regime-shadow-cutover.md, docs/benchmarking/README.md 'DOS Verification Commands') listing the minimum proof-bundle commands to verify a claimed state — git ls-remote, fak release status, go test, dos verify invocations. A proof-checklist convention, not a code object. | `VerificationCommands` |
| ~ | hinted | doc-only | **verification ladder** - a core doctrine concept (10 docs; docs/notes/verification-ladder-doctrine.md, verification-ladder-epics.md, docs/standards/symptom-witness.md): the ordered set of verification rungs, cheapest to costliest, with the discipline 'start at the lowest rung that could conclusively establish the property; climb only on INDETERMINATE or risk'; a system concept the standards docs build on | `VerificationLadder` |
| ~ | hinted | doc-only | **verification-ladder spec** - the recurring concept (4 docs; title of docs/standards/verification-ladder-spec.md, plus negative fixtures under docs/standards/fixtures/verification-ladder-invalid/): the normative spec that verification picks the smallest rung that conclusively establishes a property and climbs only on INDETERMINATE or elevated risk — the agent-kernel restatement of five Linux mechanisms (seccomp fold, least-privilege caps, AppArmor complain-to-enforce, integrity ladder, eBPF verifier), with the doctrine note as companion. | `VerificationLadderSpec` |
| ~ | hinted | doc-only | **vllm automatic prefix caching** - an external technology (12 docs; anchored in docs/fak-vs-alternatives-comparison.md and the cache-frontier/benchmark docs): vLLM's per-instance KV prefix cache (APC), used as a comparison baseline for fak's cross-agent/cross-session reuse claims — never a fak component | `VllmPrefixCaching` |
| ~ | hinted | doc-only | **windows powershell** - an external technology name (5 docs) used as a platform-marker heading for Windows-specific instruction variants — quick starts and manual wiring that give the PowerShell ($env:) equivalents of the bash steps (docs/integrations/claude.md, docs/fak/tutorial.md, playbooks). Not a repo concept; a platform-section convention. | `WindowsPowerShellSection` |

## Per-KPI (implicitness-debt = naming hygiene of the rows that exist)

| Group | KPI | Score | Debt | Detail |
|---|---|---:|:--:|---|
| well-formed | `well_formed` | 100 | 0 | all 247 rows well-formed |
| grounded | `evidenced` | 100 | 0 | every row's evidence appears in the tree |
| named | `named_resolves` | 100 | 0 | every claimed name resolves in the code |
| named | `anchored` | 100 | 0 | every explicit concept's definition is anchored on disk |
| named | `naming_planned` | 100 | 0 | every implicit concept has a naming plan |
| honesty | `explicitness_consistent` | 100 | 0 | every verdict matches its evidence |
| honesty | `name_quality_soft` | 100 | 0 | proposed names are name-shaped |

## Coverage by signal kind (how much of each implicit space is positioned)

| Signal | Positioned | Discovered | Unpositioned |
|---|---:|---:|---:|
| code-only | 2 | 2 | 0 |
| doc-only | 157 | 157 | 0 |
| hinted | 1 | 1 | 0 |
| latent-literal | 44 | 44 | 0 |

