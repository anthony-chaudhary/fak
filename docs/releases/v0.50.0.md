# fak v0.50.0: Minor release for worktree, syspromptmmu, gateway, metrics

This release contains 86 commits: 19 what changed changes, 27 reliability and correctness changes, 20 engineering quality and evidence changes. The sections below are grouped by operator impact, with the complete source history retained for auditability.

## What changed

- Test-witness-only landing gate for Astra manager to accept worker diffs (#11532).
- Enforce test breadth calibration to prevent reasoning model test over-engineering (#11520).
- Per-tool granular capability annotations for destructive and open-world operations (#11527).
- Prompt-level ambiguity resolution rule prioritizing minimal reversible action over freeze (#11521).
- Inject autonomous action-bias directive into headless workprofiles (#11519).
- Budget rendered tool results from live context reserves and risk (#10331).
- Surface IFC trust violation affordances and remedy notes.
- Route Astra child workers to Gemini with override support.
- Support Gemini thinking config and reasoning effort.
- Expose TRUST_VIOLATION metrics into mechanical boundary dimensions in Prometheus and Grafana (#11486).
- Disambiguate TRUST_VIOLATION metrics into mechanical boundary dimensions (#11486).
- Add turn-boundary taint scoping and declassification ledger for autonomous workflows (#11485).
- Add explicit verdict disposition to decouple loopback routing (#11484).
- Sub-harness runtime multi-versioning, sticky session routing, and canary traffic splitting.
- Add durable FAK-native session continuation.
- Expose bounded agent context controls and queries.
- Establish reasoning effort profiles with default baseline and on-demand high-effort delegation.
- Add single-line progress bar and verbose flag (#11480).
- Add MCP tool annotations and enforce schema strictness (#11451).

## Reliability and correctness

- Worktree pointer gitdir translation for detached worker admission under strict host sandboxes (#11530).
- Disambiguate RefusalSubtypeLabelKey to resolve merge symbol collision.
- Align implicit serve stdio posture with reasonable default-open guard behavior (#11370).
- Signal on drain token block and ensure deterministic latest-turn provenance.
- Exhaustive nullable-union synthesis for optional object properties in strict mode (#11518).
- Require witnessed boundary refusal receipt before admitting no allowed path DispCleanWrapup (#11503).
- Automated keyword sanitizer stripping unsupported JSON Schema draft keywords for strict models (#11517).
- Preserve LF in red-team report golden on Windows (#11345).
- Report output write failures instead of false success (#11341).
- Guard against speculative tool call execution from within thinking blocks (#11515).
- Avoid nil-map panic reconciling JSON null identity file (#11353).
- Reject forged pipe-witness claim receipts and cap stream memory buffer (#11343).
- Target-aware semantic identity and recovery streak reset for livelock detector (#11426).
- Require witnessed boundary refusal receipt before admitting DispCleanWrapup (#11503).
- Scope substring tool failure signatures to non-zero exit codes (#11505).
- Declare tier 5 for airgaptest package (#11387).
- Verify post-merge compilation before CAS-updating main in worker landing (#11460).
- Replace invalid operator-override recovery notes with scoped fail-to-abstain guidance (#11455).
- Treat diagnostic tool output and routine exit codes as non-failures (#11454).
- Enforce prospective tree buildcheck in fak sweep and pre-commit hook (#11459).
- Require witness verification before clean wrapup on noted no allowed path (#11453).
- Replace brittle model prefix matching with capability-driven taxonomy for caching, reasoning effort, and tier classification (#11452).
- Register PARENT_TOKEN_BUDGET_EXCEEDED in dos.toml and fak recover (#11469).
- Refuse masked overlay compilation failure instead of falling back to ambient live tree (#11457).
- Prevent cache invalidation desyncs from case-insensitivity on Windows and macOS (#11403).
- Eliminate synthetic Read transform loop inflating fak_read call amplification (#11476).
- Commit OverrideType struct field and event handling (#11458).

## Engineering quality and evidence

- Optimize exec_command and PowerShell file reads via in-process vDSO (#11524).
- Record completion and evidence for 11 resolved issues.
- Regenerate verb surface catalog and append nightrun memory entry.
- Normalize golden line endings and stabilize mock/catalog assertions.
- Promote exec_command and PowerShell file reads to in-process vDSO execution (#11524).
- Add regression tests for output write failure (#11341).
- Add determinism and race witness for refusal-safe model intake (#9532).
- Add determinism and race witness for live fanout contracts (#9523).
- Add determinism and race witness for CI base-red diagnosis (#9465).
- Add developer payload corpus for continuous zero false-positive regression testing (#11487).
- Derive README inventory counts from the demo corpus (#9332).
- Reconcile M8 prefill scheduler completion #9430.
- Add trust violation provenance and remediation audit tests.
- Add boundary refusal receipt witness tests (#11503).
- Add benchmarks for production operations (cachemeta, blobfs, binstamp, assumecheck).
- Add substantive benchmarks to retire maturity debt (strmatch, refutil, frontmatter, flock).
- Add package doc and establish non-test Go source (#11387).
- Add benchmarks and prune comment bloat (vllmcompile, taskvc, sessionsearch).
- Classify vendor NPU runtimes (#10684).
- Persist through recoverable blockers without weakening safety gates.

## Upgrade

No manual migration is required unless a change above says otherwise. Install or update with:

```bash
go install github.com/anthony-chaudhary/fak/cmd/fak@latest
```

## Release facts

- Version: `0.50.0`
- Commit count: 86
