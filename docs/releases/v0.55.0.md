# fak v0.55.0: Patch release for amdgpu, agent, sync, agents

This release contains 300 commits: 74 what changed changes, 63 reliability and correctness changes, 62 engineering quality and evidence changes. The sections below are grouped by operator impact, with the complete source history retained for auditability.

## What changed

### Security & Governance
- Add first class support for Pi as harness with fak serve on mac as backend without guard (#12554).
- Add subagent-specific capability floor and progressive MCP tool disclosure (#12306).
- Enforce fail-closed IQ3_XXS admission requiring backend capability (#12542).
- In-kernel circuit breaker for tool failure doom-loops (#12523) (adjudicator).
- Add companion-aware gate, provenance, and leak audit verbs to fak-dev.
- Add Strix power frontier software receipt contract.
- Implement production capability floor and fix operand parens.

### Autonomous Agent & Multi-Model Harness
- Support dynamic memory pressure and display buffers in turnkey sizing (#12652) (macfit).
- Add first-class Claude Code harness support with fak serve on Mac backend (#12552).
- Implement managed worktrees for Codex effect workers (#12480).
- Implement real-time Apple Silicon unified memory pressure governor for OpenCode local serving (#12305).
- Support codex as harness with fak serve on mac as backend (#12553).
- Carry typed assignments into managed Codex workers (#12480).
- Shift left memory fit, host pressure, and lease preflight diagnostics (#12616).
- Wire MemoryGovernor dynamic zero-swap admission into fak serve session loop (#12509).
- Add maintainer dev commands for fresh-status and subagent-fanout.
- Enforce native context windows and discovery.
- Implement macOS wired working-set memory locking via mach_vm_wire.
- Enable child subagents by default and add effort profile slider.
- Expose actual sequence-prefill route in native receipts.
- Wire HIL microdose probe, subagent fanout harness, and unified run runner.

### Serving Engine, Gateway & Kernel Acceleration
- Add align-mode recurrent state checkpointing and prefix caching for Qwen3.5/3.8 GDN layers (#12588).
- Add interactive model loading spinner and progress bar (#12510).
- Add native Anthropic Messages API protocol adapter for local Qwen ChatML serving (#12594).
- Add opt-in context-split decode attention (#12535).
- Implement default MTP runner envelope and adapters (#12239).
- Implement single-port reverse proxy for co-located Grafana under /grafana (#833).
- Implement speculative candidate tree KV cache recycling and branch demotion (#12529).
- Apply exact runtime heap limit (#12703).
- Auto-promote unhinted tool signatures from execution idempotency receipts (#12525).
- Bind paged RadixAttention block pool to Metal PagedAttention page tables (#12528).
- Define fail-closed IQ3_S HAL dtype and admission contract (#12545).
- Define fail-closed IQ3_XXS HAL dtype and admission contract (#12542).
- Enable native Apple Silicon Metal acceleration in run REPL (#12467).
- Enable native Apple Silicon Metal GPU acceleration in 'fak run' REPL on macOS (#12467).
- Expose dynamic hardware inventory as mcp resource with update streaming (#12476).
- Expose the owned Q6 upload contract (#12664).
- Retain packed Q4_K embeddings (#12704).
- Wire Metal MTP speculative decoding into fak serve OpenAI API completions (#12331).
- Add Vulkan speculative target verification.
- Support appliance dashboard catalog profile and default to fak-strix-index.
- Dynamic GPU occupancy gating with Split-KV FlashDecoding for low-batch long-context decode.
- Enable published DeepSeek V4 Flash expert runtime.
- Export public Strix acceleration engine and kernel bridge.
- Expose HIL MCP tools, ready endpoints, and Metal MTP state.
- Load published Flash metadata and dense FP8 pairs.
- One-time bulk lift and port of Strix Halo silicon physics to public fak.
- Resolve native context before model load.
- Wire internal/compute/strix into public fak serve and fak up runtime.

### Shared Trunk & Workspace Lifecycle
- Add 'fak sync heal' verb to repair phantom deletions and refresh index from HEAD (#12575).
- Implement automatic worktree management triggers with debounced sweep (#12505).
- Companion-aware git pre-commit hooks enforcing 5-gate boundary and leak scrubbing.

### Developer Platform, Tooling & Evidence
- Add authoritative Strix c1 observation (#12357).
- Add canonical Strix auto-discovery (#12562).
- Add frontpage demo for raw speed and batched subagents (#12551).
- Add optional repository root composition (#12584).
- Add Strix known-hosts broker (#12289).
- Add Strix known-hosts broker adapter (#12290).
- Add Strix request resource authority (#12501).
- Add tooling to detect and refresh stale docs (#8944).
- Add until expiration and workdir to cron opencode and emit (#11953).
- Implement peer-to-peer Thunderbolt 5 RDMA discovery and multi-Mac point-to-point mesh allocation (#12596).
- Implement tiered prompt cache eviction (assistant -> user -> system) with trimmable prefix reuse (#12593).
- Declarative fleet ops manifest schema (fak.fleet.ops/v1) and lane-disjointness validator (#12513).
- Add batched-copy embedding gather subkernel and ablation selector.
- Add companion ticket discovery and inspection verbs to fak-dev.
- Add mock hazard detector and platform coverage test helpers.
- Decouple preallocated generation budget from requested max_tokens.
- Honor native context limits end to end.
- Native cadence runner for garden sweeps.
- Record hil runtime proof and witness test.
- Seal non-forgeable physical execution authority.
- Set admission TokenBudget from measured warmup capacity probe.
- Wire ops schedule verb and prereq test coverage.

## Reliability and correctness

### Security & Governance
- Add pre-commit guard against phantom deletion of integrated additions (#12572).
- Align circular dependency refusal code to 1105.

### Autonomous Agent & Multi-Model Harness
- Align claude and codex serve prompt wiring and main dispatch (#12553).
- Align GGUF memory admission preflight with selected load arm (#12569).
- Observe cancellation at bounded native prefill chunks (#12691).
- Register qwen38:70b alias to prevent startup crash on high-memory Macs (#12650).
- Remove dangling pi serve handlers and finalize codex serve wiring (#12553).
- Resolve resident UD-Q2_K_XL memory on Metal and emit detailed hints (#12615).
- Update codex launcher model detection and serve flags (#12553).
- Update serve flags and memory governor intake (#12509).
- Document the implemented --session flag for wip admit.
- Ensure prompt replacement matches fresh session and clamp sampled endpoints.
- Remove unlanded claude verb from dispatch switch.

### Serving Engine, Gateway & Kernel Acceleration
- Add structured Apple GPU core discovery (#12676).
- Isolate cache lifetimes and physical slot reuse (#12423).
- Require owned Q6 no-copy backing (#12664).
- Auto-enable resident quant loading on Apple Silicon Metal (#12568).
- Batch incoming disjoint path checkouts in TransplantDisjointTree (#12612).
- Complete MemoryGovernor wiring in cmd/fak/serve.go (#12509).
- Declare Server warmup capacity and budget fields (#5266).
- Preserve unknown hardware telemetry (#12682).
- Quarantine unmerged mcp search test and batch release context generations (#12596).
- Register mock engine in gateway tests and pin current godfile baseline (#12599).
- Reject swap growth during model preparation (#12660).
- Return 400 invalid_request_error instead of 429 rate_limit_error for impossible request envelopes (#12655).
- Wire local model limits and current permissions (#12307).
- Preserve Qwen sequence attention parity (vulkan).
- Auto-select inkernel engine on model load and report planner in gateway banner.
- Expose ICB declarations to production builds.
- Fail closed on Vulkan readback errors.
- Make cpu-ref CloneTensor own quantization scales.
- Retain gateway key and provide remote navigation hints on homepage and dashboard error states.

### Shared Trunk & Workspace Lifecycle
- Add precompiled answer corpus index fallback for dos answer (#12469).
- Handle incoming path renames across synthetic tree transplantation (#12576).
- Keep managed no-op landing scoped (#12480).
- Prune published drain entries (#12696).
- Refuse incoming disjoint paths that collide with untracked local files (#12579).
- Sync git index with HEAD during RouteDisjointIntegrate (#12570).
- Sync incoming disjoint paths to working tree after TransplantDisjointTree (#12567).
- Sync trunk working tree for integrated peer commits upon worker land (#12574).

### Developer Platform, Tooling & Evidence
- Add a real wall-clock deadline to the race job (#12483).
- Add non-Darwin microdose platform shim (#12502).
- Admit outstanding non-secret env reads and remove deleted keys (#12601).
- Allow declared execution timeouts up to 2h for opencode (#11953).
- Define cmdClaude forwarding to cmdClaudeMacFak (#12552).
- Prefer observed Darwin pressure (#12701).
- Relax drainage timeout in TestNativeSchedulerCostAwarePreemptionPreservesOutputAndPins under package test load (#12527).
- Remove duplicate cmdClaude declaration from claude_mac_fak (#12552).
- Remove unused strings import from non-darwin tb5 rdma stub (#12598).
- Repair dependency quarantine, devindex leaks, verb tiers, and enumlint exemptions (#12602).
- Resolve committed ReasonCode 1100 collision (#12504).
- Update tier topology for benchauthority and modelengine, add amdgpu chat role (#12597).
- Align cooperative Q8 LDS rows.
- Classify hardcoded branch references and retire inactive change-detector entries.
- Handle missing private companion repo gracefully in git mode.
- Intercept known_hosts broker early to prevent secret logging.
- Make RegisterReason panic on clashes like its Register* siblings.
- Pin validation SSH host trust.
- Preserve offline diagnostics for explicit candidate LAN probes.
- Repair concept freshness, windowless helper, god file ceiling, and lane declarations.
- Repair dependency quarantine toolchains, gpudirect topology discovery, and env reads.
- Resolve public boundary root across companion chdir.
- Unlock locked and prunable orphan worktrees before removal.

## Engineering quality and evidence

### Security & Governance
- Align release build-flags drift guard with the canonical entrypoint (#12490).
- Reap stale guard-boundary framing and highlight local serving and subagents.

### Autonomous Agent & Multi-Model Harness
- Add guide for concurrent multi-agent fanout with fak up (#12550).
- Implement 24-agent concurrent load driver against live fak serve --metal (#12510).
- Implement runtime memory limits and GC pacing during large model loads (#12573).
- Require successful offline agent evidence (#12374).
- Fix model weight and memory guidance (#12700).
- Label simulated subagent benchmark (#12674).
- Codify companion-aware dev harness pattern in AGENTS.md and CLAUDE.md.
- Define automatic local-agent milestone.
- Document mandatory automated public OSS placement for new work.
- Integrate local-agent positioning [disjoint].
- Native harness orchestration and management architecture.
- Retain evaluated generation for continuation reuse.
- Stabilize buffer-pool contention metric.
- Track native validation baseline failures.
- Update system prompt to generic agent assistant and clarify mock fixture.

### Serving Engine, Gateway & Kernel Acceleration
- Add aligned Q6 storage for shared Metal backing (#12665).
- Add Metal ICB buffer limit distinction (#12688).
- Add page-aligned shared Q6 storage (#12665).
- Add shared Q6 weight backing for Metal (#12664).
- Implement 8-row B-tree SIMDgroup packing for Qwen3.5/3.8 GatedDeltaNet linear attention (#12586).
- Implement fused Dequantize-GEMV-SwiGLU kernel for Qwen MLP blocks on Metal (#12595).
- Implement Metal Indirect Command Buffer (ICB) replay for static decode graph execution (#12587).
- Implement single indirect command buffer for wide-M tree verification with <= 1.8x latency ratio (#12508).
- Implement Split-K workgroup saturation for low-batch quantized GEMM on Apple Silicon (#12592).
- Implement threadgroup-tiled FlashAttention and zero-copy UMA allocator for Metal backend (#12521).
- Route Metal hybrid MTP through wide-M batched verification (#12349).
- Use geometric KV reserve for repeated Vulkan sequence-prefill chunks (#12548).
- Wire 8-row B-tree SIMDgroup packed dispatch for Qwen3.5/3.8 (#12586).
- Wire agentic-mtp validation into make mac-perf and gate fixture comparison (#12511).
- Measure five f32 projection shapes.
- Share Vulkan KV backing across prefix forks.
- Skip metal mtp chat completions when metal gdn is unavailable.
- Threadgroup-tiled FlashAttention and zero-copy UMA allocator for Metal backend.
- Witness Vulkan sequence prefill continuity.
- Update hardware-in-the-loop roadmap, physical receipts, and model guides.

### Shared Trunk & Workspace Lifecycle
- Add multi-node concurrent integration race reproduction test (#12577).
- Document fak sync heal recovery verb and runbook options (#12578).
- Document three-tree synchronization invariants and multi-node safe sync rules (#12578).
- Integrate origin/main [disjoint] (sync).
- Restore working tree existence assertions in TestRouteReconciliationApplyExecution (#12571).
- Integrate disjoint benchmark tooling.
- Integrate latest origin/main [disjoint].
- Integrate origin/main.
- Integrate origin/main [superset].
- Integrate reviewed native context changes.
- Refuse orphan targets before Windows prepare mutates common Git state.
- Sync go.sum dependencies for companion bridge.
- Update runtime proofs for computebuild, qwen38campaign, and worktree.
- Verify shift-left prompt ordering and instruction precedence.

### Developer Platform, Tooling & Evidence
- Implement shared-DRAM decode coupling (#12394) (modelengine).
- Add M5 native serving qualification (#12675).
- Add native MTP benchmark tier (#12658).
- Implement decode priority between prefill chunks (#12395).
- Implement prefix-cache-aware chunked prefill scheduling (#12530).
- Bind packed KV append contract (#12399).
- Declare tier for cluster package (#12596).
- Harden FP16 KV contract invariants (#12388).
- Wire streamed Q4 checkpoint loading (#12658).
- Add candidate dependency edge and promotion prereq tests.
- Run gofmt -w across 70 unformatted Go packages to satisfy format gate.

## Upgrade

No manual migration is required unless a change above says otherwise. Install or update with:

```bash
go install github.com/anthony-chaudhary/fak/cmd/fak@latest
```

## Release facts

- Version: `0.55.0`
- Commit count: 300
