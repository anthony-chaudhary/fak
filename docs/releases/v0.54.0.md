# fak v0.54.0: Patch release for landingvoucher, sync, issuepolicy, workerworktree

This release contains 50 commits: 10 what changed changes, 15 reliability and correctness changes, 12 engineering quality and evidence changes. The sections below are grouped by operator impact, with the complete source history retained for auditability.

## What changed

### Security & Governance
- Implement shadow landing audit telemetry and canary discrepancy logger (#12370).

### Serving Engine, Gateway & Kernel Acceleration
- Expose live scheduler admission load on control socket IPC (#12419).

### Developer Platform, Tooling & Evidence
- Add detection for benchmarked lanes without executable regression gates (#12360).
- Add runtime attach and detach for remote L3 snapshot store (#12420).
- Implement anchored comparator authority (#12450).
- Implement Landing Voucher generator and pre-landing invariant auditor (fak-landing-voucher/v1) (#12335).
- Implement runner-owned timing windows (#12436).
- Implement strict-born-merged contract for atomic leaf dispatchability (#12334).
- Add hardware packet schema and verification.
- Wire default expanded surfaces and quote OpenCode shell args.

## Reliability and correctness

### Security & Governance
- Report retained worker on refusal (#12448).
- Retain preserved worker after land refusal (#12449).

### Autonomous Agent & Multi-Model Harness
- Keep review-only work agent-operated by default (#12418).

### Serving Engine, Gateway & Kernel Acceleration
- Implement fail-closed Metal MTP checkpoints (#12238).
- Retain admitted Q4_K MTP layout (#12236).

### Shared Trunk & Workspace Lifecycle
- Preserve sibling validation topology (#12447) (workerworktree).
- Bind symptom witness to prospective commits (#12431).
- Fail closed when reap pre-inventory is incomplete (#12404).
- Gate concept oracle by contract (#12457).
- Type isolated pre-CAS refusals (#12448).

### Developer Platform, Tooling & Evidence
- Add anchored Strix Git object snapshot (#12445).
- Add epoch-bound Strix controller authority (#12295).
- Add sterile Strix ancestry classifier (#12446).
- Align Strix pair order (#12364).
- Make metric-name contract mismatches fail instead of log (#12380).

## Engineering quality and evidence

### Security & Governance
- Add versioned capability identity for Qwen GDN backends (#12438).
- Route typed refusal normalization (#12448).
- Add mission contract and write-set violation parity tests.

### Autonomous Agent & Multi-Model Harness
- Add live hardware sub-component testing and shift-left doctrine (#12458).
- Upgrade opencode-wave to deterministic worktree-isolated multi-agent dispatch (#12454).

### Shared Trunk & Workspace Lifecycle
- Enforce fail-closed assertions on live-tree governance tests (#12378).
- Route sibling validation topology (#12447).
- Add allocation lifecycle, sweep, and env benchmarks.
- Integrate origin/main [disjoint] (sync).

### Developer Platform, Tooling & Evidence
- Add eviction halt predicate when shared capacity is sufficient (#12414).
- Add content, script, and AST scan benchmarks and API helpers.
- Add fanout validation and simulation benchmarks.

## Upgrade

No manual migration is required unless a change above says otherwise. Install or update with:

```bash
go install github.com/anthony-chaudhary/fak/cmd/fak@latest
```

## Release facts

- Version: `0.54.0`
- Commit count: 50

## Promotion source

- Source: main 12e42eaac46237c438a2fccdca1ffb110a0e59cf
- Target before promotion: main (unknown)
- Promoted source range: 12e42eaac46237c438a2fccdca1ffb110a0e59cf
