# fak v0.52.0: Patch release for orchestration, compute, tools, harnesskit

This release contains 50 commits: 16 what changed changes, 24 reliability and correctness changes, 7 engineering quality and evidence changes. The sections below are grouped by operator impact, with the complete source history retained for auditability.

## What changed

### Security & Governance
- Admit explicit policy exact commands within focused native tools (#11990).

### Autonomous Agent & Multi-Model Harness
- Generate queue reconcilers and admit maintenance by effect class (#8892, #6780).

### Serving Engine, Gateway & Kernel Acceleration
- Cache hint policies for quantized speculative tree verification (#506).
- Incremental transaction adoption and rollback for MTP (#11991).
- Resume batched GDN from committed native state (#11987).
- Tier-2 search result memoization with hierarchical path-scope invalidation (#11492).
- Verify multi-step MTP panel execution (#11989).

### Shared Trunk & Workspace Lifecycle
- Add leased Halo hardware validation to relevant worker prompts (#12033).

### Developer Platform, Tooling & Evidence
- Implement raw greedy decode with exact token accounting (#12012).
- Auto-arm host reboot-advisor and FakStallMonitor under S4U autoheal (#4610, #3360).
- Fence prefix-cache reuse by decode regime (#5273).
- Prefix singleflight coalescing for concurrent subagents (#11633).
- Activate public coordination contracts and expand tool scope permissions.
- Compose radix-prefix and addressed candidates into reuse plan.
- Cross-platform subprocess configuration with Windows console window suppression.
- In-kernel scheduled automations with at-most-once fire + catchup window + hard interrupt.

## Reliability and correctness

### Security & Governance
- Audit missing script actions and classify completed one-shot tasks (#6499, #6500, #6501).
- Retain explicit chat policy through native arm configuration (#11986).

### Autonomous Agent & Multi-Model Harness
- Make paged refs immediately restorable from in-memory staging (#10018).
- Preserve typed governance transformations in native MCP dispatch (#11446).
- Propagate tool catalogs and execute armed tools in raw baseline arm (#11251).

### Serving Engine, Gateway & Kernel Acceleration
- Apply configured QK normalization in scalar Qwen HAL attention (#12027, #12005).
- Harden Q2_K bounds validation and HAL selection (#11945).
- Honor the vectorized GDN disable setting (#12049).
- Resolve paged MMU refs with bounded slicing in ctxrestore (#10018).
- Add DeltaNet disable override.
- Correct Vulkan Gated DeltaNet recurrence.
- Make fak_read paged refs immediately restorable.
- Resolve data race on http.ResponseWriter between heartbeat ticker and SSE stream emitter.

### Shared Trunk & Workspace Lifecycle
- Auto-detect sibling repos in worktree doctor registration (#7205).
- Remove private Halo hostname from worker prompts (#12033).
- Cover FAK in the scheduled worktree doctor.
- Preserve live owners when heartbeat ages beyond reaper threshold.
- Preserve managed workers in legacy cleanup planners.

### Developer Platform, Tooling & Evidence
- Require physical Strix correctness evidence for relevant changes (#12037).
- Bind Halo validation to built source and reject invented ablation evidence (#12034).
- Duplicate unledgered pusher polls shared trunk (#6512).
- Scheduled doc refresh strands tracked WIP in shared checkout (#6507).
- Bound native retained prefix cache before cloning.
- Honor native backend in packed sessions.

## Engineering quality and evidence

### Autonomous Agent & Multi-Model Harness
- Skip empty extra-body reserialization.

### Serving Engine, Gateway & Kernel Acceleration
- Admit retained safetensors in MTP acceptance (#11994).
- Avoid synchronous disk read and hashing under mutex (#11402).
- Add isolated cooperative Q8 decode pipeline.
- Make Q2_K Vulkan Wave32 cooperative.
- Retire slower Q2_K Wave32 path.
- Skip unused inbound content snapshot on tool-free requests.

## Upgrade

No manual migration is required unless a change above says otherwise. Install or update with:

```bash
go install github.com/anthony-chaudhary/fak/cmd/fak@latest
```

## Release facts

- Version: `0.52.0`
- Commit count: 50

## Promotion source

- Source: main 2172aa3b33df2c5c69aa0607f99bb11ba0a56c37
- Target before promotion: main (unknown)
- Promoted source range: 2172aa3b33df2c5c69aa0607f99bb11ba0a56c37
