---
version: 0.42.0
date: 2026-07-29
headline: "Minor release for cmd, release, covmatrix, skillfootprint"
themes:
  - "cmd"
  - "release"
  - "covmatrix"
  - "skillfootprint"
  - "concept"
  - "safecommit"
  - "policy"
  - "evidence"
highlights:
  - "feat(cmd): price the instruction-pulled doc floor per section so the largest per-agent cost stops being invisible (fak cmd)"
  - "test(release): stop the lock suite from writing the repo lock when a lock-root override is inherited (fak tools)"
  - "fix(covmatrix): correct the accelerated-fence claim and bind the topology axis to the kernel (fak covmatrix)"
  - "feat(skillfootprint): measure the resident skill description floor and hold it under a two-sided ratchet (fak skillfootprint)"
  - "docs(concept): regenerate the disambiguation scorecard so the freshness gate stops blocking ci (fak docs)"
  - "fix(safecommit): admit an untracked path whose bytes already match the trunk copy instead of refusing a pure no-op (fak safecommit)"
  - "fix(policy): classify every exported field of the installed floor so a change to an unruled knob stops reading as a declared no-op (fak policy)"
  - "fix(evidence): commit the three cited experiment artifacts so ship-iff-cited passes in CI (fak experiments)"
---

**TL;DR** - Automatic minor release cut from 50 commit(s) since the previous tag.

## cmd

- feat(cmd): price the instruction-pulled doc floor per section so the largest per-agent cost stops being invisible (fak cmd)
- fix(cmd): refuse an unauthenticated serve bind that reaches beyond this host unless the operator names the override (fak cmd)
- fix(cmd): reclaim an earlier guard session's allow widening at boot so a killed session stops granting authority to the next one (fak cmd)
- fix(cmd): route the garden and unwitnessed-claim gh calls through the deadline-bound ghexec builder (fak cmd)
- feat(cmd): observe driver liveness from the process table so stopped mid-tool rows stop deferring unconditionally (fak cmd)

## release

- test(release): stop the lock suite from writing the repo lock when a lock-root override is inherited (fak tools)

## covmatrix

- fix(covmatrix): correct the accelerated-fence claim and bind the topology axis to the kernel (fak covmatrix)
- fix(covmatrix): correct the stale lens to key on the cell so the seven new cpu oracles do not erase the accelerated residual (fak covmatrix)

## skillfootprint

- feat(skillfootprint): measure the resident skill description floor and hold it under a two-sided ratchet (fak skillfootprint)

## concept

- docs(concept): regenerate the disambiguation scorecard so the freshness gate stops blocking ci (fak docs)
- docs(concept): regenerate the scorecard from the tracked tree so the freshness gate stops reading local scratch (fak docs)

## safecommit

- fix(safecommit): admit an untracked path whose bytes already match the trunk copy instead of refusing a pure no-op (fak safecommit)

## general

- .tmp\fleet\msg-advisory.txt
- .tmp\fleet\msg-steerability.txt

## policy

- fix(policy): classify every exported field of the installed floor so a change to an unruled knob stops reading as a declared no-op (fak policy)

## evidence

- fix(evidence): commit the three cited experiment artifacts so ship-iff-cited passes in CI (fak experiments)

## model

- refactor(model): extract six duplicated batched-lane bodies into named shared helpers (fak model)
- test(model): add a weight-free CPU numeric oracle for the DeepSeek-MLA family (fak model)
- test(model): add a weight-free CPU numeric oracle for the MiniMax-MSA family (fak model)
- test(model): add a weight-free CPU numeric oracle for the GLM-5.2-DSA family (fak model)
- fix(model): correct the gpt-oss sliding-window cadence so half the stack stops being silently clipped (fak model)
- test(model): add a weight-free CPU numeric oracle for the Mixtral MoE family (fak model)
- test(model): pin the HF safetensors and export loaders to the Cohere rotary re-layout (fak model)
- fix(model): correct the Cohere rotary pairing so HF checkpoints stop diverging from position 1 (fak model)

## fleetaccounts

- test(fleetaccounts): pin the fixture registry and probe ledger so the status fold stops depending on the host ambient fleet dir (fak fleetaccounts)
- fix(fleetaccounts): gate the probe-ledger rung on block-derivability so an unset FLEET_REG_DIR stops hiding a fresh probe (fak fleetaccounts)
- test(fleetaccounts): pin the Opus family at tier 1 across id shapes while rejecting substring matches (fak fleetaccounts)

## tools

- refactor(tools): split twenty god-functions into named helpers across ten dispatch and audit scripts (fak tools)
- fix(tools): run all nine watchdog tests under the no-pytest fallback by supplying tmp_path and moving the entrypoint last (fak tools)
- test(tools): cover the checksum-pinned generated-artifact leak exemption (fak tools)
- refactor(tools): split the Codex stop-failure audit into marker, rollup, and recommendation helpers (fak tools)
- refactor(tools): split the API-host proof rollups into named requirement builders (fak tools)

## gateway

- fix(gateway): name the structural system[] read failure and surface its prune result instead of discarding it (fak gateway)
- feat(gateway): resolve --key-principal specs into the serve keyset so each tenant key is authenticated and attributed (fak gateway)
- test(gateway): pin the cold-tool-defer shed as its own debug/vars fields separate from the compaction shed (fak gateway)
- feat(gateway): gate the pre-Submit account route on the caller's tenant principal (fak gateway)
- feat(gateway): derive the compaction bail HELP set from the emitter and publish a candidate-only bail rate (fak gateway)

## compute

- test(compute): hand the scaffold golden witness its sources through a build overlay so concurrent module builds stop racing it (fak compute)

## procguard

- fix(procguard): pin the ps census argv per GOOS and keep stdout on a non-zero exit so a BSD-dialect host stops reporting an empty process census (fak procguard)

## trunk-red

- fix(trunk-red): require a defined first-break symbol beside base ancestry so the fold stops hiding live breaks (fak cmd)

## quality

- refactor(quality): route the rubric oracles through the shared scorer and fold the duplicated decode loop (fak quality)
- style(quality): align the gptneox oracle rope comments so the package stops failing gofmt (fak model)
- fix(quality): declare the shared rubric scorer that the report oracles already call so the package builds at HEAD (fak quality)
- refactor(quality): share one report sentence splitter across the report oracles and route their outright-fails through rubricFail (fak quality)

## turnbench

- test(turnbench): skip the full-trial break-even sweep under the race detector so the job stops timing out (fak turnbench)

## tokencache

- test(tokencache): skip the wall-clock dogfood witness under the race detector so the job stops timing out (fak tokencache)

## promptmmu

- fix(promptmmu): restore the three structural array-splice reason returns a mid-experiment snapshot collapsed to no-breakpoint (fak promptmmu)

## engine

- refactor(engine): extract the duplicated /metrics scrape into one shared helper for both cache observers (fak engine)

## selfquery

- test(selfquery): pin the detail card's freshness rung and per-root supersession scoping across merged roots (fak selfquery)

## agent

- feat(agent): register the compaction bail-reason vocabulary and its pre-eligibility split so consumers stop re-typing it (fak agent)

## Promotion source

- Source: main d70e8d1afccd4cd07bc1d5e56a06a55025c45514
- Target before promotion: main (unknown)
- Promoted source range: d70e8d1afccd4cd07bc1d5e56a06a55025c45514
- Source CI: success
