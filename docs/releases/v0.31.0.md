---
version: 0.31.0
date: 2026-06-22
headline: "Minor release for public, tools, compute, model"
themes:
  - "public"
  - "tools"
  - "compute"
  - "model"
  - "repo-guard"
  - "model-arch"
  - "evidence"
  - "ci,pages"
highlights:
  - "fix(public): repoint private fleet backlinks to the public fak tracker"
  - "docs(tools): refresh dispatch-status after closing #465 + #494"
  - "feat(compute): async CUDA backend - fence only at Read/Argmax (#482)"
  - "perf(model): scalar-vs-NEON activation-quant micro-bench + arm64 acceptance script (#476)"
  - "fix(repo-guard): allow writes to the agent's own ~/.claude state tree"
  - "docs(model-arch): code-grounded status decomposition of seam epic #487"
  - "fix(evidence): commit the cited turntax-breakeven.png so ship-iff-cited passes in CI"
  - "fix(ci,pages): restrict Jekyll defaults to pages and raise the blob cap to 10 MiB"
---

**TL;DR** - Automatic minor release cut from 77 commit(s) since the previous tag.

## public

- fix(public): repoint private fleet backlinks to the public fak tracker

## tools

- docs(tools): refresh dispatch-status after closing #465 + #494
- feat(tools): implement DOS verdict-journal auto-emit for dispatched workers (#465)
- feat(tools): add a committed issue-dispatch status doc + silent-worker signal (fak tools)
- fix(tools): skip recently-attempted issues so the dispatcher walks the backlog (fak tools)
- fix(tools): propagate the python exit code through the scheduled-task wrapper (fak tools)
- fix(tools): report python exit codes from the scheduled dispatch tasks + harden the progress snapshot (fak tools)
- feat(tools): always-on close-the-witnessed harvesting watchdog (fak tools)
- add(tools): issue-resolve progress tracker toward an N-issue target (fak tools)
- feat(tools): issue-targeted dispatch arm + self-correcting account routing (fak tools)
- fix(tools): gate the GLM-5.2 preflight on per-GPU shard fit, not just aggregate VRAM (fak tools)
- fix(tools): harden the GLM-5.2 GPU server serve script against silent failures (fak tools)
- fix(tools): repoint the GCP bench + quota tools at the repo-root source (fak tools)
- fix(tools): repoint the GLM-5.2 serving witness at the repo-root Go module (fak tools)
- fix(tools): ignore .adjudicate scrub scratch and de-link the missing dgx serve script (fak tools)
- feat(tools): add archive/ignore/back verbs + a test to the commit-msg gate (fak tools)
- add(tools): dos_sync - back up durable .dos work-product to a safe archive (fak tools)
- chore(tools): scrub operator org codename -netra -> -acct in account tooling (fak tools)
- fix(tools): pass re.split maxsplit by keyword in commit-msg gate (fak tools)
- fix(tools): security_audit resolves go.mod at repo root, not just fak/ (fak tools)

## compute

- feat(compute): async CUDA backend - fence only at Read/Argmax (#482)
- feat(compute): on-GPU KV Evict, no host round-trip (#479)

## model

- perf(model): scalar-vs-NEON activation-quant micro-bench + arm64 acceptance script (#476)
- test(model): guard roofline ceiling leg when STREAM probe measures 0 (#494)
- feat(model): zero-copy mmap single-file safetensors so the whole file is never resident (#475)
- feat(model): family-aware tensor-name resolver + per-family fixture tests (#473)
- test(model): lock the R1 oracle no-cache block bit-identity on the synthetic model (#490)

## repo-guard

- fix(repo-guard): allow writes to the agent's own ~/.claude state tree

## model-arch

- docs(model-arch): code-grounded status decomposition of seam epic #487

## evidence

- fix(evidence): commit the cited turntax-breakeven.png so ship-iff-cited passes in CI
- feat(evidence): measure the canonical fak-vs-llama.cpp CPU Q8 parity baseline on M3 Pro (fak benchmark)
- feat(evidence): restore 18 more cited benchmark artifacts under experiments/ (fak benchmark)
- feat(evidence): restore cited *-RESULTS.md provenance under docs/benchmarks/ + close the gate
- feat(evidence): restore the cited benchmark artifacts (scrubbed) under experiments/

## ci,pages

- fix(ci,pages): restrict Jekyll defaults to pages and raise the blob cap to 10 MiB

## general

- Merge branch 'main' of https://github.com/anthony-chaudhary/fak
- ci: wire the cited-evidence guard (public_evidence_manifest --check)
- docs: de-link dead ../fak/ references to private-only companion docs (fak docs)
- Revert "fix(docs): repoint docs-scorecard CORE_DOCS + topics to hoisted root paths"
- docs: fix stale fleet-public build paths to the fak root-module layout
- docs: fix broken ../fak/ relative links in benchmark/explainer/integration docs (fak docs)
- docs: fix broken ../fak/ relative links in docs/fak onboarding docs (fak docs)
- test: fix architest tier table + witness path after module hoist (fak <architest,witness>)

## pages

- fix(pages): remove the Liquid include tag from head-custom.html that broke the build
- fix(pages): de-collide the showcase chart axis and add a site-wide favicon
- docs(pages): add a live GitHub Pages showcase and fix the sitemap URL

## repo

- chore(repo): ignore fak bench + floor.json getting-started scratch
- chore(repo): ignore issue-scrub scratch + regenerable kvmmu/turn-tax reports
- chore(repo): ignore public/private git-ls-files parity scratch (fak tools)

## guard

- feat(guard): refuse out-of-tree writes into sibling repos

## trust-floor

- docs(trust-floor): refresh stale dos-verify witness citations in dos.toml (#464)
- docs(trust-floor): code-grounded status decomposition of epic #492 children

## lifecycle

- test(lifecycle): witness policy hot-reload + bounded ledger + live trace reset (#493)

## agent

- test(agent): witness real rank-5 normgate on the pre-send outbound boundary (#491)

## hardware-matrix

- docs(hardware-matrix): repoint the benchmark-authority links and de-link the private DGX notes (fak docs)

## ggufload

- feat(ggufload): dequant legacy Q4_0/Q4_1 32-element GGUF blocks (#489)

## tokenizer

- test(tokenizer): witness the encoder oracle on a standard HF cache (#488)

## ci

- fix(ci): post-hoist file-admission data-dirs + regen HERO doc after RESULTS move
- feat(ci): cited-evidence manifest - the ship-iff-cited guard for benchmark artifacts
- fix(ci): restore the 2 hero-benchmark artifacts + artifact_root to root
- fix(ci): hero-benchmark doc_path to root + allowlist the HERO doc (post-hoist)
- fix(ci): gofmt-debt auditor globs the root module, not fak/ (post-hoist)
- fix(ci): drop the leftover HARD docs-scorecard step (keep the advisory one)
- fix(ci): full-history checkout so release-substrate tests can read the tag
- fix(ci): repoint notebook-sync + release-artifacts test to the hoisted root paths
- fix(ci): allow public front-door docs at the repo root
- fix(ci): upload only the archive each release-artifacts leg built

## notebooks

- fix(notebooks): make the hosted Colab quickstart build against the public root-module repo

## readme

- docs(readme): refocus the front page on the turn-tax hero; move breadth to the gallery
- docs(readme): anchor figure links, lead with the turn-tax curves

## visuals

- feat(visuals): regenerate the hero video at 1440p with an animated turn-tax scene

## tutorial

- docs(tutorial): module is the repo root, not a fak/ subdir

## docs

- fix(docs): repoint docs-scorecard CORE_DOCS + topics to hoisted root paths

## fleet_accounts

- test(fleet_accounts): cover resolve_account/read_oauth_token/annotated_roster; document the resolve path
- feat(fleet_accounts): add resolve_account, read_oauth_token, annotated_roster + CLI resolve

## dogfood

- feat(dogfood): route shell front doors through `resolve`; add PS --install

## fleet

- refactor(fleet): route consumers through annotated_roster + read_oauth_token
