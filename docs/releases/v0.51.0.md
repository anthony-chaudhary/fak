# fak v0.51.0: Patch release for brittleness, callavoid, turntaxmeter, sessionsignals

This release contains 50 commits: 17 what changed changes, 9 reliability and correctness changes, 20 engineering quality and evidence changes. The sections below are grouped by operator impact, with the complete source history retained for auditability.

## What changed

- Add macobs, goal sync, research arms, and trajectory audit CLI verbs.
- Add numa replicas flag and wiring to modelbench.
- Add subagent synthesis, blackboard benchmarks, and goal anchors.
- Add grafana panel audit, session checkpointing, and SLO telemetry.
- Add platform dispatch helpers for goal synchronization.
- Enforce description budget ceiling of 33415 bytes.
- Add goal registry file synchronization and state persistence.
- Add owned Q2_K buffer lifecycle.
- Update development tiers, verbs, and reuse classifications.
- Add demo live links gate and guard MCP status gate.
- Add research arms HTTP endpoints, metrics, and warmup checks.
- Add agentic comparison harness for Apple Silicon benchmarks.
- Add qwen38:27b-q2k and ud-q2kxl GGUF aliases.
- Add Mac Apple Silicon MLX and hardware observability package.
- Add agent harness pre-warming and model readiness checks.
- Add research arms server attribution and lease coordinator.
- Add blackboard pub-sub cache coordination.

## Reliability and correctness

- Advance maturity curve and retire debt (turntaxmeter, sessionsignals).
- Suppress console window spawns on windows background commands.
- Isolate git subprocess environment during push and test.
- Correct routine turn classification on tool failure receipts.
- Handle glob context cancellation and timeout as CodeCanceled (#11614).
- Align match text snippet around regex match on oversized lines (#11613).
- Drop transient RETRY rows from meta-RSI cycle counts and truth-clean keep rate (#11612).
- Scope sub-turn yield valve to active turn and prevent yield loop (#11625).
- Declare Tier 1 for capabilitymatrix leaf (#11507).

## Engineering quality and evidence

- Add benchmark suite to advance maturity curve.
- Add benchmark sanity execution test.
- Add goal specifications and subagent execution goals.
- Update mac-perf target and inline hygiene gates in Makefile.
- Rename plan benchmark to prevent collision with suite.
- Add execution benchmarks for warm and frozen cache restores.
- Document public private harness boundary contract.
- Register observer, trajhook, and agent engine driver roles.
- Add git-subagent-sync skill and goal sync workflow.
- Tolerate floating point precision delta in biased Q8 prefill.
- Register researcharm, goalsync, harnesswarm, and macobs tiers.
- Add buffer pool and bounded memory buffers for search and mutation.
- Optimize Q4_K tensor chunk loader for streaming loads.
- Add comprehensive benchmarks for rehydrate pipeline.
- Add benchmarks and isolate git env in reverify test.
- Add NUMA weight replicas and optimize k-quant and Q4_K routines.
- Add NUMA topology decode pool, prefill batching, and speculative verification.
- Register goalsync, harnesswarm, macobs, and researcharm lanes.
- Document architectural evaluation and public-private contract (#11610, #11611).
- Document divide-and-conquer and autonomous safe sync push defaults.

## Upgrade

No manual migration is required unless a change above says otherwise. Install or update with:

```bash
go install github.com/anthony-chaudhary/fak/cmd/fak@latest
```

## Release facts

- Version: `0.51.0`
- Commit count: 50
