---
version: 0.43.0
date: 2026-07-29
headline: "Minor release for onboarding, cmd, readme, mac"
themes:
  - "onboarding"
  - "cmd"
  - "readme"
  - "mac"
  - "air-gapped"
  - "fleetaccounts"
  - "branchrole"
  - "engine"
highlights:
  - "docs(onboarding): link the fork-and-PR route and the Code of Conduct from the entry docs (fak docs)"
  - "feat(cmd): route fak policy --check to the signed org-envelope verifier (#5320) (fak cmd)"
  - "docs(readme): add a \"What fak is not\" section bounding the management-plane scope (fak docs)"
  - "docs(mac): convert the macOS walkthrough to bash-primary command blocks (fak docs)"
  - "docs(air-gapped): correct the zero-dependency retraction to name the three pages that still carry the stale wording (fak docs)"
  - "feat(fleetaccounts): wire the api-key credential kind into fleet discovery so a key-backed seat is routable (fak fleetaccounts)"
  - "fix(branchrole): add the treedoctor ref-lock file to the hard-coded ref audit inventory (fak branchrole)"
  - "docs(engine): clarify the cache-capability vocabulary comment (fak engine)"
---

**TL;DR** - Automatic minor release cut from 50 commit(s) since the previous tag.

## onboarding

- docs(onboarding): link the fork-and-PR route and the Code of Conduct from the entry docs (fak docs)
- docs(onboarding): add the fork-and-PR route and unblock the newcomer entry surfaces (fak docs)

## cmd

- feat(cmd): route fak policy --check to the signed org-envelope verifier (#5320) (fak cmd)
- feat(cmd): add a core-lock-all ratchet verdict that refuses widening guard policy amendments (fak cmd)
- docs(cmd): correct the identity-resolution order the fak help comment claims (fak cmd)
- refactor(cmd): route the dispatch witness out-of-lane count through the shared workerworktree rule (fak cmd)
- docs(cmd): correct the guard-effective compaction budget and add a drift witness for it (#5430) (fak cmd)
- refactor(cmd): dedupe sixteen duplicated rule bodies by pointing their call sites at the owners (fak cmd)
- refactor(cmd): route the dispatch tick pid liveness probe through the shared dispatchaudit prober (fak cmd)

## readme

- docs(readme): add a "What fak is not" section bounding the management-plane scope (fak docs)

## mac

- docs(mac): convert the macOS walkthrough to bash-primary command blocks (fak docs)
- docs(mac): add model sizing, fak install, and a troubleshooting table to the Mac walkthrough (fak docs)
- docs(mac): add the from-scratch gateway setup the Mac showcase assumed you already had (#5460) (fak docs)
- fix(mac): add an up-front refusal that names the placeholder gateway and the setup path (#5457) (fak cmd)

## air-gapped

- docs(air-gapped): correct the zero-dependency retraction to name the three pages that still carry the stale wording (fak docs)

## fleetaccounts

- feat(fleetaccounts): wire the api-key credential kind into fleet discovery so a key-backed seat is routable (fak fleetaccounts)

## branchrole

- fix(branchrole): add the treedoctor ref-lock file to the hard-coded ref audit inventory (fak branchrole)

## engine

- Added the micro-context research fabric and verifier-backed stages: bounded 10,000-context orchestration, controlled 100/1,000 real-model ramps, descriptor hibernation/continuation, cache-observable shared-base reuse, compatibility-batch execution, and API-only/fairness boundaries. The interrupted/restarted controlled 10,000 real-model soak remains an explicit open witness rather than a release claim.

- docs(engine): clarify the cache-capability vocabulary comment (fak engine)
- refactor(engine): export the base-URL join so callers supply their own invalid-URL wording (fak engine)

## architest

- test(architest): retire the stale quality pureRoot and README zero-dep debt entries (fak architest)

## gateway

- refactor(gateway): fold eleven duplicated rule bodies onto single owners (fak gateway)

## examples

- docs(examples): add an orientation table so runnable demos stop hiding behind a policy title (#5471) (fak examples)

## devindex

- fix(devindex): align the verb-tier map entry so the gofmt gate passes on every committed file (fak devindex)

## bench

- refactor(bench): fold eight duplicated rule bodies onto single owners across nine packages (fak bench)

## safecommit

- feat(safecommit): add the refused-remove errno to ReapResult and emit a throttled LOCK_REAP_FAILED (fak safecommit)

## concept

- docs(concept): regenerate the disambiguation snapshot so its counts match the catalog that produces them (fak docs)

## adjudicator

- fix(adjudicator): route the disk and device verb rules through a structural decider so a quoted mention stops reading as a device operation (#5429) (fak adjudicator)

## rollback

- docs(rollback): correct the pinning ladder to match how identity now resolves (fak docs)

## general

- Merge remote-tracking branch 'origin/main'
- Merge remote-tracking branch 'origin/main' into HEAD
- Merge remote-tracking branch 'origin/main'
- Merge remote-tracking branch 'origin/main' into HEAD
- Merge remote-tracking branch 'origin/main' into HEAD
- Merge remote-tracking branch 'origin/main' into HEAD

## treedoctor

- fix(treedoctor): correct the lock-residue matcher and add the ref-lock diagnosis it missed (#5335) (fak treedoctor)

## appversion

- fix(appversion): derive the semver tag Go embeds in a go-installed binary (fak appversion)

## help

- fix(help): promote agent to the front door so the README's headline command is discoverable (fak cmd)

## agent

- refactor(agent): export the folded-system corrector so the gateway and the offline audit price a request identically (fak agent)

## milestonereport

- feat(milestonereport): expose --markdown on milestone-scorecard and product-scorecard so both render through the shared kernel (fak milestonereport)

## sotacoverage

- refactor(sotacoverage): fold the scorecard kernel into sota-coverage and mlp-score so neither re-derives its envelope (fak sotacoverage)

## tools

- docs(tools): fix the scorecard grade baseline by re-pinning it from a clone CI can reproduce (fak tools)
- docs(tools): fix two stale persona affordance pointers so they name the artifacts that exist (fak tools)

## gitgate

- fix(gitgate): guard the config and pipe spellings that let one act retire every remote ref (fak gitgate)

## chatrelay

- refactor(chatrelay): dedupe the Slack poll ordering and ticker loop the chatops door copied (fak chatrelay)

## dogfoodissues

- feat(dogfoodissues): add a shared seen-cache loader and writer for the debt-dispatch surfaces (fak dogfoodissues)

## guardcomplaint

- refactor(guardcomplaint): export the occurrence-count reader so other issue producers can share one marker parser (fak guardcomplaint)

## issuecohort

- refactor(issuecohort): export the split-target predicate so decompose and the cohort planner share one rule (fak issuecohort)

## strmatch

- refactor(strmatch): add shared common-prefix and tail-clamp helpers for the per-package copies (fak strmatch)

## maputil

- feat(maputil): add a generic string reader so packages stop copying the per-file str helper (fak maputil)

## accountprobe

- feat(accountprobe): add a per-seat probe-coverage grade so an aged ledger stops reading as health (fak accountprobe)

## Promotion source

- Source: main 56168b2950b7ddc775db8454c245b27b6920b862
- Target before promotion: main (unknown)
- Promoted source range: 56168b2950b7ddc775db8454c245b27b6920b862
- Source CI: success
