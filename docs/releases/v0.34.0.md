---
version: 0.34.0
date: 2026-06-25
headline: "Minor release for gateway, witness, cache, docs"
themes:
  - "gateway"
  - "witness"
  - "cache"
  - "docs"
  - "serve"
  - "cmd"
  - "simpledemo"
  - "tools"
highlights:
  - "feat(gateway): per-session compaction-health reset policy + resetScore shadow surface (fak gateway)"
  - "feat(witness): append-only decision recorder on refs/notes/fak/decisions (#833)"
  - "feat(cache): dogfood the vCache scorecard against real provider telemetry (fak experiments)"
  - "fix(docs): regenerate the demo-quality scorecard doc to its generator output (fak docs)"
  - "feat(serve): self-staging GLM-5.2 A100 serve path (unsloth UD-Q4_K_M) (fak serve)"
  - "feat(cmd): add fak signal <id> pause|resume|stop|steer - job control closing the SIGCONT+stdin gap (#760) (fak cmd)"
  - "docs(simpledemo): drop go -C fak prefix from demo test commands (fak examples)"
  - "fix(tools): resolve CLAUDE_CONFIG_DIR before ~/.claude in the historical-guard audit (fak tools)"
---

**TL;DR** - Automatic minor release cut from 119 commit(s) since the previous tag.

## gateway

- feat(gateway): per-session compaction-health reset policy + resetScore shadow surface (fak gateway)
- feat(gateway): wire INBOUND tool-def compaction onto the live wire - the dead-wired half of #555 (fak gateway)
- style(gateway): gofmt metrics.go - blank comment line between WITNESSED/OBSERVED blocks (fak gateway)
- docs(gateway): split compaction metrics into WITNESSED vs OBSERVED so a provider miss isn't read as a fak bug (fak gateway)
- fix(gateway): cap paced max_tokens by byte-splice, not a key-sorting re-marshal (fak gateway)
- feat(gateway): add history-compaction metrics + provider billing-truth check (fak gateway)
- feat(gateway): wire static replica proxy routing (fak gateway)

## witness

- feat(witness): append-only decision recorder on refs/notes/fak/decisions (#833)
- feat(witness): add notests reward-hack guard claim (#829) (fak witness)

## cache

- feat(cache): dogfood the vCache scorecard against real provider telemetry (fak experiments)
- ci(cache): add a repeatable vCache scorecard dogfood gate (fak tools)
- docs(cache): publish the vCache scorecard operator playbook (fak docs)

## docs

- fix(docs): regenerate the demo-quality scorecard doc to its generator output (fak docs)
- fix(docs): correct launch-kit webbench mentions to modeled (fak docs)

## serve

- feat(serve): self-staging GLM-5.2 A100 serve path (unsloth UD-Q4_K_M) (fak serve)

## cmd

- feat(cmd): add fak signal <id> pause|resume|stop|steer - job control closing the SIGCONT+stdin gap (#760) (fak cmd)
- fix(cmd): actionable error for an unknown bench/turntax/ablate --suite NAME (fak cmd)
- feat(cmd): wire `fak traj` into the main dispatch (fak cmd)
- fix(cmd): set repo git identity in safecommit integration test (fak safecommit)
- test(cmd): add real assertions to the two cmd/fak vacuous tests (fak cmd)

## simpledemo

- docs(simpledemo): drop go -C fak prefix from demo test commands (fak examples)

## tools

- fix(tools): resolve CLAUDE_CONFIG_DIR before ~/.claude in the historical-guard audit (fak tools)
- feat(tools): add the default-on garden bundle - converge the gardening passes (fak tools)
- feat(tools): add the conflation / provenance-honesty scorecard (fak tools)
- feat(tools): add 4-way benchmark provenance - measured/modeled/functional/unknown (fak status)
- feat(tools): add growth-invariant steerability scorecard + drift tracking (fak steerability)
- feat(tools): add the code-slop scorecard - the anti-slop measuring stick (fak tools)
- fix(tools): replace Codex PowerShell hook launcher (fak tools)
- fix(tools): fix session audit namespace filter (fak tools)
- feat(tools): use Codex login for OpenAI live proof (fak tools)
- feat(tools): add fresh-status cross-domain rollup (git+bench+work+industry) (fak status)
- feat(tools): add Claude historical guard replay audit (fak tools)
- feat(tools): add OpenAI live-prereq audit and hosted-pilot harness (fak tools)
- feat(tools): add guard MCP-status audit and live snapshot (fak tools)
- feat(tools): flag stale structured git-gate evidence in codex dos audit (fak tools)
- fix(tools): scope the provenance gate to staged ADDED lines, not whole files (fak tools)
- feat(tools): add the provenance-label gate so a modeled number can't be called measured (fak tools)

## ctxplan

- feat(ctxplan): IDF/selectivity-weighted inverted index (#564) (fak ctxplan)

## recall

- feat(recall): ECC-style per-page syndrome metadata + fault classification (#783, #785) (fak recall)
- feat(recall): add persisted witness keep-bit + sound-restore replay fixture vs ACRFence control (#762) (fak recall)

## bench-dx

- ci(bench-dx): add a repeatable bench-DX scorecard regression gate (fak tools)

## safecommit

- style(safecommit): gofmt safecommit.go import order (fak safecommit)
- fix(safecommit): symlink/realpath-harden lease containment + post-commit assertion (fak safecommit)
- fix(safecommit): clean OFF_TRUNK detail on a detached HEAD
- feat(safecommit): wire fak commit into the CLI dispatch
- feat(safecommit): ship fak commit, the safe shared-trunk commit verb

## cli

- docs(cli): disambiguate the two default capability floors in --policy help (fak cmd)

## marketing

- docs(marketing): align the Pages front door with the accessible README, scope security to its own track (fak docs)

## taskmgr

- feat(taskmgr): add the repeat-stall witness (fak taskmgr)
- feat(taskmgr): add liveness heartbeat snapshots (fak taskmgr)

## guard

- fix(guard): actionable guidance when the wrapped agent is missing or not logged in (fak guard)
- test(guard): match OBSERVED-labeled provider-cache summary wording (fak guard)
- feat(guard): add child restart on budget drain (fak guard)

## model

- style(model): gofmt hal.go and safetensors.go - strip trailing EOF blank line (fak model)
- perf(model): memoize Gemma-4 RoPE inv_freq across forwards (fak model)

## readme

- docs(readme): move cache deep-dives off the front door, sweep voice (fak docs)
- docs(readme): add a runnable Start Here proof for cold binary installs (fak docs)
- docs(readme): lead the front door with cost/control, scope security to its own audience track (fak docs)
- docs(readme): add the long-session lever to the front door, framed by the billing check (fak readme)
- docs(readme): reset agent kernel front door (fak readme)

## vdso

- feat(vdso): add the per-path write-generation invalidator (#795)
- feat(vdso): public Speculatable gate - default-deny on effects for spec execution

## general

- refactor: delete 8 confirmed-dead unexported symbols (fak tools)
- add reconcile flags to fak accounts list and rank canonical by name match (fak accounts)
- Merge branch 'main' of https://github.com/anthony-chaudhary/fak into HEAD
- Merge remote-tracking branch 'origin/main'
- Merge remote-tracking branch 'origin/main'
- Merge remote-tracking branch 'origin/main' into HEAD

## seo

- fix(seo): emit Jekyll front-matter from the conflation scorecard generator (fak seo)
- fix(seo): retire SEO/AEO debt to zero - meta on every page, quotable leads, no orphans (fak seo)

## notes

- docs(notes): record the job-side observe fix that makes gem8 visible (fak docs)
- docs(notes): add vToolcall fork + best-effort serve-tier design note (fak vdso)
- docs(notes): map the account-switcher single-source problem and the gem8 disappearance (fak docs)
- docs(notes): add vToolcall materialized-view design note (fak vdso)
- docs(notes): map agent-era serving assumptions (fak docs)

## session

- feat(session): add transition push-notifier seam + tiered sinks for the closed stop-reason vocabulary (#761) (fak session)

## explainers

- docs(explainers): formalize the compounding benefits of a saved call + savings-vector lens (fak benchmarks)

## agent

- perf(agent): content-address outbound tool-schema normalization (#796)
- fix(agent): keep the compaction stub alternating and guard the empty-window index (fak agent)
- fix(agent): anchor history compaction on the FIRST cache breakpoint so it fires on real Claude Code traffic (fak agent)

## sessionbench

- fix(sessionbench): reconcile -quant default polarity to false (fak cmd)

## agent,model

- test(agent,model): assert post-state in the last two vacuous tests (fak tools)

## gardener

- feat(gardener): add a `fak route` rung to the gardening model pick - first deployed routing-spine consumer (fak gardener)

## compute

- fix(compute): implement over-cap vulkan q8 weight chunking (fak compute)
- fix(compute): add over-cap vulkan refusal (fak compute)
- feat(compute): add cuda managed residency budget (fak compute)

## accounts

- fix(accounts): flag a name-lie only when no name token matches the login (fak accounts)
- feat(accounts): fingerprint the setup token and reconcile same-account seats (fak accounts)

## dogfood

- test(dogfood): cover recent feature scorecards (fak dogfood)
- feat(dogfood): run history compaction by default on the gcp serve gateway (fak dogfood)

## routing

- feat(routing): add gardening routing preset - dogfood per-aspect routing on background work (fak modelroute)

## index

- docs(index): index the vToolcall materialized-view note (fak docs)

## sessionimage

- test(sessionimage): add sound-restore replay fixture (fak sessionimage)

## benchcatalog

- feat(benchcatalog): add fak benchmarks index + bench-DX scorecard, 2x the benchmarking dev-experience (fak benchcatalog)

## loop

- feat(loop): route `fak loop` in main and add the tunable `fak loop admit` gate (fak loop)
- feat(loop): ship the durable loop-ledger dispatcher kernel (Rung 1) (fak loop)

## vcache

- feat(vcache): add scorecard gate (fak vcache)

## loopmgr

- feat(loopmgr): add the tunable loop-admission governor (fak loop)

## make

- build(make): run code-slop scorecard test in demo-scorecards gate (fak tools)
- build(make): wire guard-MCP and OpenAI-prereq audits into hygiene/demo-tool-tests (fak tools)

## architest

- fix(architest): declare the loopmgr tier so the trunk gate goes green (fak architest)

## dispatch

- feat(dispatch): wire the issue-resolve dispatcher + progress ticks into the loop ledger (fak loop)

## promptmmu

- feat(promptmmu): inbound prompt-MMU spine - cache-safe tool-def pruning (fak promptmmu)

## kernel

- feat(kernel): add handle fan-in reap (fak kernel)
- feat(kernel): add handle poll (fak kernel)

## comm

- feat(comm): add adjudicated collectives (fak comm)

## agenttopo

- feat(agenttopo): declare neighbor topology (fak agenttopo)

## experiments

- docs(experiments): fold the 100k-session value proof into one findable status packet (fak experiments)
- feat(experiments): add combined guard-floor + compaction dogfood proving both on one fak serve wrap (fak experiments)
- docs(experiments): add GLM-5.2 vLLM serving preflight and agentic battery (fak experiments)

## claims

- docs(claims): claim the 100k-session cache-preserving compaction lever + dogfood evidence (fak claims)

## gitgate

- feat(gitgate): add collective commit barrier (fak gitgate)

## cohort

- feat(cohort): add fail-closed shrink/agree (fak cohort)

## agent-live

- test(agent-live): refresh guard MCP evidence snapshot (fak experiments)

## serving

- docs(serving): update static replica router status (fak docs)

## fanbench

- feat(fanbench): add canonical D-001 scale grid (fak fanbench)

## launch

- docs(launch): add copy-paste directory-submission payloads (fak launch)

## dist

- feat(dist): add glama.json so Glama auto-indexes fak as an MCP server (fak docs)
- feat(dist): publish fak to the Official MCP Registry via an OCI image (fak docs)
