---
version: 0.35.0
date: 2026-06-30
headline: "Minor release for dispatch, cmd, wirescreen, bench"
themes:
  - "dispatch"
  - "cmd"
  - "wirescreen"
  - "bench"
  - "scorecard"
  - "tools"
  - "model"
  - "ci"
highlights:
  - "fix(dispatch): attribute sidecar-less worker logs by their fak-spawn header (#1452) (fak tools)"
  - "fix(cmd): add release ship parent lock #1391 (fak cmd)"
  - "perf(wirescreen): reduce redactor Apply/Propose allocations on the pre-send path (fak wirescreen)"
  - "fix(bench): treat throughput deltas as higher-is-better (fak ci)"
  - "chore(scorecard): pin Linux control-pane floor (fak tools)"
  - "fix(tools): keep cadence tag under release lock #1391 (fak tools)"
  - "test(model): add arm64 Q8 oracle gate (#6) (fak model)"
  - "fix(ci): add learning docs signpost and full-history garden checkout (fak ci)"
---

**TL;DR** - Automatic minor release cut from 50 commit(s) since the previous tag.

## dispatch

- fix(dispatch): attribute sidecar-less worker logs by their fak-spawn header (#1452) (fak tools)
- fix(dispatch): make issue-resolve-progress go quiescent when target met (#1453) (fak tools)
- test(dispatch): add end-to-end witness that a dead no-op docs lane returns WOULD_SPAWN not LANE_BUSY (#1398) (fak cmd)
- fix(dispatch): add an issue-text arm to the self-modify hold for mis-routed fak-own-code issues (#1397) (fak cmd)
- fix(dispatch): ship wave pricing helpers (fak cmd)
- fix(dispatch): add a self-modify pre-route that holds fak-own-code issues from self-guarded workers (#1397) (fak cmd)
- fix(dispatch): honor the recorded no-commit reason so the picker holds a re-blocked issue (#1396) (fak tools)
- fix(dispatch): split tick preflight helpers (fak cmd)

## cmd

- fix(cmd): add release ship parent lock #1391 (fak cmd)
- fix(cmd): reap a dead banner-no-op worker's lane so it stops blocking the lane (#1398) (fak cmd)
- feat(cmd): add priority-aware ordering to the per-lane issue picker (#1395) (fak cmd)
- fix(cmd): add live binary holder output to doctor (#1273) (fak cmd)

## wirescreen

- perf(wirescreen): reduce redactor Apply/Propose allocations on the pre-send path (fak wirescreen)

## bench

- fix(bench): treat throughput deltas as higher-is-better (fak ci)

## scorecard

- chore(scorecard): pin Linux control-pane floor (fak tools)
- chore(scorecard): re-pin the control-pane baseline after the raw-debt drop (fak tools)
- fix(scorecard): pin garden scorecard floor (fak tools)
- chore(scorecard): refresh grade floor after debt drop (fak tools)

## tools

- fix(tools): keep cadence tag under release lock #1391 (fak tools)
- test(tools): add a cadence-bot lock-owner assertion so the auto-cut cannot collide with a human owner (#1391) (fak tools)
- test(tools): add a contract test pinning the release-lock mutual-exclusion order in the cadence cut (#1391) (fak tools)
- test(tools): assert release cadence cut acquires the release lock and defers when held by another owner (#1391) (fak tools)
- fix(tools): update the disambiguation coverage literal to match the doc (fak tools)
- fix(tools): add a held-release-lock refuse to the cadence cut execute path (#1391) (fak tools)
- fix(tools): add a banner-no-op guard so a dead worker stops wedging its lane (#1398) (fak tools)

## model

- test(model): add arm64 Q8 oracle gate (#6) (fak model)
- fix(model): make fakaccel cgo build asm-free (fixes #1) (fak model)
- feat(model): add opt-in Accelerate Q8 prefill GEMM path (#1) (fak model)

## ci

- fix(ci): add learning docs signpost and full-history garden checkout (fak ci)
- fix(ci): normalize suffixed go test timings (fak ci)
- fix(ci): normalize spine and pin scorecard gates (fak ci)

## fak

- fix(fak): stamp built-in benchmark artifacts (#416) (fak cmd)

## engine

- docs(engine): record SGLang adapter closure (#39) (fak docs)

## docs

- fix(docs): regenerate llms-full.txt to match the current llms.txt (fak docs)

## concept-disambiguation

- feat(concept-disambiguation): position the scorecard/debt + dispatch-plan tokens to clear the 95% coverage floor (fak concept-disambiguation)

## cachevalue

- feat(cachevalue): add `fak cachevalue report --markdown` (mermaid + sparklines + provenance) (fak cmd)
- feat(cachevalue): add a markdown render of the two-track P&L (mermaid xychart + sparklines + provenance) (fak cachevaluereport)

## gateway

- feat(gateway): expose the Codex memories backend through fak_memory_run (fak gateway)

## memory

- feat(memory): wire `fak memory run --backend codex` to the external Codex memories backend (fak cmd)

## spine

- ci(spine): scrub latency log timings (fak ci)
- ci(spine): fix invariant normalizer yaml (fak ci)
- ci(spine): normalize invariant test timings (fak ci)

## architest

- fix(architest): declare dynamo engine owner (fak architest)

## accounts

- feat(accounts): add the StaleCred rehydration rung - refresh-or-refuse a dormant credential (fak accounts)

## guard

- fix(guard): wait for a re-login token to land before surfacing a 401 (fak guard)

## recall

- feat(recall): add the StaleRecall rehydration rung - withhold memory naming a stale artifact (fak recall)

## selfinstall

- fix(selfinstall): add unique Windows stale binary aside (#1273) (fak selfinstall)

## ggufload

- fix(ggufload): add Gemma3 and Qwen3 MoE GGUF tensor mappings (#7) (fak ggufload)

## promptmmu

- feat(promptmmu): add self-drop request planner input for #757 (fak promptmmu)

## appversion

- fix(appversion): add live stale fak process detection (#1273) (fak appversion)
