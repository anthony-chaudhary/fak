# fak v0.49.0: Patch release for ociartifact, gateway, sessionrecovery, trajhook

This release contains 50 commits: 20 what changed changes, 13 reliability and correctness changes, 1 engineering quality and evidence change. The sections below are grouped by operator impact, with the complete source history retained for auditability.

## What changed

- User-defined step classifiers and skill-based trajectory intervention hooks (#11410).
- Autonomous multi-agent roll-up digest for zero-self-report supervisor steering (#11411).
- Add guarded unused-listener canaries.
- In-kernel observer thread pool for zero-cost step-by-step agent output interpretation (#11408).
- Adaptive fleet density governor sizing co-hosted worker waves from host PSI and memory headroom (#11406).
- Goal-directed semantic episode tracking and CAS tool output compaction (#11409).
- Admit internal coordination tools and record model security overrides (#11375).
- Support pagination, line numbering, and symlink confinement in fak_read (#11399).
- Disaggregate coarse trust violations into fine-grained refusal reasons (#11377).
- Implement declarative permission profiles and policy context propagation (#11357).
- Arm native MCP tools and wire permissive default posture for fak agent harness.
- Add fak harness lint subcommand and structured platform linting (#11380).
- Add shift-left zero-egress CI bootstrap gate and dial trap (#11387).
- Add Cosign simple signing and public key verification to fakpack bundles (#11381).
- Implement NVIDIA RTX 5090 BaM GPU Direct NVMe P2PDMA and hierarchical memory architecture (#11326).
- Cryptographically bind raw harness and fak arm traces to tamper-evident audit decision journals.
- Unify lifecycle stop gates across fak guard and fak agent native harness.
- Default-on workspace memory store discovery and verified recall prompt assembly in agent loop.
- Implement all-in-one one-touch bootstrap orchestrator unifying lock verification, MCP broker, memory, and gateway (#11379).
- Wire upstream failure observer and render turn debug error.

## Reliability and correctness

- Remove literal verification bypass (#11388).
- Forward reasoning tokens and preserve thought content on OpenAI Responses wire (#11450).
- Reject server failures in readiness (#11449).
- Observe transient Codex readiness during bounded guarded startup (#11390).
- Classify MCP discovery and read tool annotations (#11397).
- Serialize detached worker admission in shared Git directory (#11335).
- Add benchmarks and prune comments to retire maturity debt across 8 packages (trendreport, tooltrend, toolrollup, stripeload, stepbaton, skillvalue, ...).
- Repair build failures and standardize formatting across packages.
- Cover ABI and dos.toml reason tokens in recovery catalog (#11381).
- Implement Ponytail bypass on explicit code change requests (#11429).
- Handle large issue_dispatch leases via temporary file (#11428).
- Define VerifyArtifact signature validator for allinone supervisor.
- Require endpoint readiness and prevent duplicate resume (Closes #10550).

## Engineering quality and evidence

- Scope Goal Invariant 5 logging to genuine refusals (#11429).

## Upgrade

No manual migration is required unless a change above says otherwise. Install or update with:

```bash
go install github.com/anthony-chaudhary/fak/cmd/fak@latest
```

## Release facts

- Version: `0.49.0`
- Commit count: 50

## Promotion source

- Source: main 95c486cc5ea939a6293d7c00d660330f5912b656
- Target before promotion: main (unknown)
- Promoted source range: 95c486cc5ea939a6293d7c00d660330f5912b656
