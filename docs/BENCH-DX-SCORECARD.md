---
title: "Bench-DX scorecard  -  the benchmarking developer experience"
description: "Deterministic score of how hard it is for a developer to discover, cold-start, learn, read, and compare a fak benchmark. Auto-generated; re-run tools/bench_dx_scorecard.py."
---

# Bench-DX scorecard 20260625T162042Z

_Auto-generated by `tools/bench_dx_scorecard.py`. Do not hand-edit; re-run the tool._

**score 92.0/100  -  grade A  -  bench-DX-debt 1**

The benchmarking developer experience, scored as the five steps a developer walks:
discover what exists, cold-start one, learn one flag set, read a number, compare results.

| step | debt | weight |
|---|---:|---:|
| discover | 0 | 28 |
| coldstart | 0 | 28 |
| learn | 1 | 16 |
| read | 0 | 14 |
| compare | 0 | 14 |

| KPI | step | status | detail |
|---|---|:---:|---|
| `catalog_verb` | discover | [x] | fak benchmarks catalog verb is wired and tracked |
| `catalog_registry` | discover | [x] | internal/benchcatalog/catalog.go registry present (24 entries) |
| `registry_covers_tree` | discover | [x] | registry covers all 24 surfaces one-to-one |
| `offline_set` | coldstart | [x] | registry marks the offline set (16 zero-asset benchmarks) |
| `webbench_runs_clean` | coldstart | [x] | fak webbench describe falls back to a committed sample (runnable with zero args) |
| `no_spurious_paths` | coldstart | [x] | no spurious fak/-prefixed default paths in cmd/*bench* |
| `out_flag_uniform` | learn | [x] | output flag is documented uniformly as -out/--out in the registry |
| `quant_polarity_consistent` | learn | [ ] +1 | -quant default polarity is split: true in ['sessionbench'], false in ['batchbench', 'modelbench', 'radixbench'] |
| `provenance_doc` | read | [x] | BENCHMARK-AUTHORITY.md present (every number traces to it) |
| `summary_self_describes` | read | [x] | all 24 registry rows carry a one-line 'what this number means' |
| `bench_cli_path_ok` | compare | [x] | tools/bench_cli.py BENCHMARK_DIR resolves from the repo root |
| `compare_documented` | compare | [x] | tools/bench_cli.py documents the list/compare loop |


## Baseline -> now (the 2x program)

This scorecard was created to score and then 2x-improve the benchmarking developer
experience. The before/after, both measured by this same tool against the
git-tracked tree:

| | bench-DX-debt | score | grade |
|---|---:|---:|:---:|
| baseline (origin/main, pre-change) | 35 | 14/100 | F |
| now | 1 | 92/100 | A |

That is a **35x reduction in bench-DX-debt** (target was 2x). The single remaining
debt item  -  the `-quant` flag's split default polarity (`true` in `sessionbench`,
`false` in the others)  -  is deliberately left as honestly-scored debt: flipping a
measurement default is a convention decision for the bench owner, not a silent edit.

Reproduce:

```bash
python tools/bench_dx_scorecard.py                       # the live score
python tools/bench_dx_scorecard.py --compare BASE.json   # vs a saved baseline
```

What retired the debt (each an ADDED affordance, not prose):

- **`fak benchmarks list|describe|run`** (`cmd/fak/benchmarks.go`)  -  the single
  discoverable door over all 24 benchmark surfaces, backed by an in-binary registry
  (`internal/benchcatalog`) that the scorecard cross-checks against the `cmd/` tree.
- **`fak webbench describe`** now falls back to a committed sample, so its
  advertised "RUNNABLE NOW" is true with zero args.
- **`paritybench`** no longer points its default flags at a non-existent
  `fak/experiments/parity/` path, so it runs cold against the committed cards.
- **`tools/bench_cli.py`** `BENCHMARK_DIR` no longer carries the doubled `fak/`
  prefix, so the run->record->compare query loop resolves the catalog.
