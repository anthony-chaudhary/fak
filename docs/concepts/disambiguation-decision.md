
## Follow-on discovered names

- **`decisional`** — the exact `decision` symbol; distinct from the broader family label and sibling operations.
- **`needsdecision`** — the exact `decision` symbol; distinct from the broader family label and sibling operations.
- **`dosdecision`** — the exact `decision` symbol; distinct from the broader family label and sibling operations.
- **`permissiondecisionreason`** — the exact `decision` symbol; distinct from the broader family label and sibling operations.
- **`indecision`** — the exact `decision` symbol; distinct from the broader family label and sibling operations.
