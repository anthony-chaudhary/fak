# plan names

This map positions the current `plan` coverage backlog. Each entry names the exact repository symbol; the family label remains the broader domain and is not a substitute for the symbol.

- **`issueplan`** — the exact `plan` symbol `issueplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`candidatesgathered`** — the exact `plan` symbol `candidatesgathered`; use this spelling for that operation rather than the undifferentiated family name.
- **`newconfiguredhttpplanner`** — the exact `plan` symbol `newconfiguredhttpplanner`; use this spelling for that operation rather than the undifferentiated family name.
- **`configrecoveryplans`** — the exact `plan` symbol `configrecoveryplans`; use this spelling for that operation rather than the undifferentiated family name.
- **`evictionplan`** — the exact `plan` symbol `evictionplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`explainplan`** — the exact `plan` symbol `explainplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`gathercandidates`** — the exact `plan` symbol `gathercandidates`; use this spelling for that operation rather than the undifferentiated family name.
- **`planpublish`** — the exact `plan` symbol `planpublish`; use this spelling for that operation rather than the undifferentiated family name.
- **`recoveryplans`** — the exact `plan` symbol `recoveryplans`; use this spelling for that operation rather than the undifferentiated family name.
- **`wiplandresult`** — the exact `plan` symbol `wiplandresult`; use this spelling for that operation rather than the undifferentiated family name.
- **`bindrecoveryplan`** — the exact `plan` symbol `bindrecoveryplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`candidatedomain`** — the exact `plan` symbol `candidatedomain`; use this spelling for that operation rather than the undifferentiated family name.
- **`candidatepath`** — the exact `plan` symbol `candidatepath`; use this spelling for that operation rather than the undifferentiated family name.
- **`candidatesformodel`** — the exact `plan` symbol `candidatesformodel`; use this spelling for that operation rather than the undifferentiated family name.
- **`dispatchwaveexecutionplan`** — the exact `plan` symbol `dispatchwaveexecutionplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`dispatchwaveskippedcandidates`** — the exact `plan` symbol `dispatchwaveskippedcandidates`; use this spelling for that operation rather than the undifferentiated family name.
- **`estimatecpuoffloadexpertsexpertparallelmemoryplan`** — the exact `plan` symbol `estimatecpuoffloadexpertsexpertparallelmemoryplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`falseplans`** — the exact `plan` symbol `falseplans`; use this spelling for that operation rather than the undifferentiated family name.
- **`fitandplanserveggufcpuoffloadpathondevice`** — the exact `plan` symbol `fitandplanserveggufcpuoffloadpathondevice`; use this spelling for that operation rather than the undifferentiated family name.
- **`fitandplanserveggufpathondevice`** — the exact `plan` symbol `fitandplanserveggufpathondevice`; use this spelling for that operation rather than the undifferentiated family name.
- **`goexeccandidates`** — the exact `plan` symbol `goexeccandidates`; use this spelling for that operation rather than the undifferentiated family name.
- **`hourlyplan`** — the exact `plan` symbol `hourlyplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`isgrowthcandidate`** — the exact `plan` symbol `isgrowthcandidate`; use this spelling for that operation rather than the undifferentiated family name.
- **`mergecandidatenotes`** — the exact `plan` symbol `mergecandidatenotes`; use this spelling for that operation rather than the undifferentiated family name.
- **`modelloadplan`** — the exact `plan` symbol `modelloadplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`newproxyplanner`** — the exact `plan` symbol `newproxyplanner`; use this spelling for that operation rather than the undifferentiated family name.
- **`planbytestotal`** — the exact `plan` symbol `planbytestotal`; use this spelling for that operation rather than the undifferentiated family name.
- **`planissues`** — the exact `plan` symbol `planissues`; use this spelling for that operation rather than the undifferentiated family name.
- **`planobservationstotal`** — the exact `plan` symbol `planobservationstotal`; use this spelling for that operation rather than the undifferentiated family name.
- **`planoracleerror`** — the exact `plan` symbol `planoracleerror`; use this spelling for that operation rather than the undifferentiated family name.
- **`planoraclesunavailable`** — the exact `plan` symbol `planoraclesunavailable`; use this spelling for that operation rather than the undifferentiated family name.
- **`planpin`** — the exact `plan` symbol `planpin`; use this spelling for that operation rather than the undifferentiated family name.
- **`pressuredcandidates`** — the exact `plan` symbol `pressuredcandidates`; use this spelling for that operation rather than the undifferentiated family name.
- **`publishplan`** — the exact `plan` symbol `publishplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`pyspawncandidates`** — the exact `plan` symbol `pyspawncandidates`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderdispatchwaveplanrows`** — the exact `plan` symbol `renderdispatchwaveplanrows`; use this spelling for that operation rather than the undifferentiated family name.
- **`runmodelplan`** — the exact `plan` symbol `runmodelplan`; use this spelling for that operation rather than the undifferentiated family name.
- **`runplanaudit`** — the exact `plan` symbol `runplanaudit`; use this spelling for that operation rather than the undifferentiated family name.
- **`runwipland`** — the exact `plan` symbol `runwipland`; use this spelling for that operation rather than the undifferentiated family name.
- **`unplanned`** — the exact `plan` symbol `unplanned`; use this spelling for that operation rather than the undifferentiated family name.

## Follow-on discovered names

- **`wipland`** — the exact `plan` symbol; distinct from the broader family label and sibling operations.
- **`wiplandsubject`** — the exact `plan` symbol; distinct from the broader family label and sibling operations.
