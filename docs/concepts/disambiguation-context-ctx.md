# context-ctx names

This map positions the current `context-ctx` coverage backlog. Each entry names the exact repository symbol; the family label remains the broader domain and is not a substitute for the symbol.

- **`contextual`** — the exact `context-ctx` symbol `contextual`; use this spelling for that operation rather than the undifferentiated family name.
- **`contextid`** — the exact `context-ctx` symbol `contextid`; use this spelling for that operation rather than the undifferentiated family name.
- **`additionalcontext`** — the exact `context-ctx` symbol `additionalcontext`; use this spelling for that operation rather than the undifferentiated family name.
- **`basecontext`** — the exact `context-ctx` symbol `basecontext`; use this spelling for that operation rather than the undifferentiated family name.
- **`ctxadvice`** — the exact `context-ctx` symbol `ctxadvice`; use this spelling for that operation rather than the undifferentiated family name.
- **`ctxstepadvice`** — the exact `context-ctx` symbol `ctxstepadvice`; use this spelling for that operation rather than the undifferentiated family name.
- **`generatereusedcontextwithbias`** — the exact `context-ctx` symbol `generatereusedcontextwithbias`; use this spelling for that operation rather than the undifferentiated family name.
- **`recallinjectiondebit`** — the exact `context-ctx` symbol `recallinjectiondebit`; use this spelling for that operation rather than the undifferentiated family name.
- **`retainedfactrecall`** — the exact `context-ctx` symbol `retainedfactrecall`; use this spelling for that operation rather than the undifferentiated family name.
- **`canarycontexts`** — the exact `context-ctx` symbol `canarycontexts`; use this spelling for that operation rather than the undifferentiated family name.
- **`compactioneconomics`** — the exact `context-ctx` symbol `compactioneconomics`; use this spelling for that operation rather than the undifferentiated family name.
- **`compactionnudgenearpercent`** — the exact `context-ctx` symbol `compactionnudgenearpercent`; use this spelling for that operation rather than the undifferentiated family name.
- **`defaultrecallinjectionledger`** — the exact `context-ctx` symbol `defaultrecallinjectionledger`; use this spelling for that operation rather than the undifferentiated family name.
- **`fakctxexpensegate`** — the exact `context-ctx` symbol `fakctxexpensegate`; use this spelling for that operation rather than the undifferentiated family name.
- **`hibernatedcontexts`** — the exact `context-ctx` symbol `hibernatedcontexts`; use this spelling for that operation rather than the undifferentiated family name.
- **`newmanagedcontext`** — the exact `context-ctx` symbol `newmanagedcontext`; use this spelling for that operation rather than the undifferentiated family name.
- **`queuepeakcontexts`** — the exact `context-ctx` symbol `queuepeakcontexts`; use this spelling for that operation rather than the undifferentiated family name.
- **`restoredcontexts`** — the exact `context-ctx` symbol `restoredcontexts`; use this spelling for that operation rather than the undifferentiated family name.
- **`tracefromcontext`** — the exact `context-ctx` symbol `tracefromcontext`; use this spelling for that operation rather than the undifferentiated family name.
