---
title: "guard-gate names"
description: "This map positions the current guard-gate coverage backlog. Each entry names the exact repository symbol;"
---
# guard-gate names

This map positions the current `guard-gate` coverage backlog. Each entry names the exact repository symbol; the family label remains the broader domain and is not a substitute for the symbol.

- **`corelockgate`** — the exact `guard-gate` symbol `corelockgate`; use this spelling for that operation rather than the undifferentiated family name.
- **`fakguardaffordancemode`** — the exact `guard-gate` symbol `fakguardaffordancemode`; use this spelling for that operation rather than the undifferentiated family name.
- **`releasegate`** — the exact `guard-gate` symbol `releasegate`; use this spelling for that operation rather than the undifferentiated family name.
- **`gatefileadmission`** — the exact `guard-gate` symbol `gatefileadmission`; use this spelling for that operation rather than the undifferentiated family name.
- **`gatescope`** — the exact `guard-gate` symbol `gatescope`; use this spelling for that operation rather than the undifferentiated family name.
- **`guardbanner`** — the exact `guard-gate` symbol `guardbanner`; use this spelling for that operation rather than the undifferentiated family name.
- **`guardcodexdefaultmodelid`** — the exact `guard-gate` symbol `guardcodexdefaultmodelid`; use this spelling for that operation rather than the undifferentiated family name.
- **`guardsplit`** — the exact `guard-gate` symbol `guardsplit`; use this spelling for that operation rather than the undifferentiated family name.
- **`guardtempdirowner`** — the exact `guard-gate` symbol `guardtempdirowner`; use this spelling for that operation rather than the undifferentiated family name.
- **`guardtoolprochooks`** — the exact `guard-gate` symbol `guardtoolprochooks`; use this spelling for that operation rather than the undifferentiated family name.
- **`guardupstreamposture`** — the exact `guard-gate` symbol `guardupstreamposture`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardaudit`** — the exact `guard-gate` symbol `cmdguardaudit`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardcommitgate`** — the exact `guard-gate` symbol `cmdguardcommitgate`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguarddeny`** — the exact `guard-gate` symbol `cmdguarddeny`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardprecompact`** — the exact `guard-gate` symbol `cmdguardprecompact`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardresume`** — the exact `guard-gate` symbol `cmdguardresume`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardsessions`** — the exact `guard-gate` symbol `cmdguardsessions`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardsessionstart`** — the exact `guard-gate` symbol `cmdguardsessionstart`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardstophook`** — the exact `guard-gate` symbol `cmdguardstophook`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardstops`** — the exact `guard-gate` symbol `cmdguardstops`; use this spelling for that operation rather than the undifferentiated family name.
- **`cmdguardstopsslack`** — the exact `guard-gate` symbol `cmdguardstopsslack`; use this spelling for that operation rather than the undifferentiated family name.
- **`codexguardwitness`** — the exact `guard-gate` symbol `codexguardwitness`; use this spelling for that operation rather than the undifferentiated family name.
- **`computegate`** — the exact `guard-gate` symbol `computegate`; use this spelling for that operation rather than the undifferentiated family name.
- **`fakctxexpensegate`** — the exact `guard-gate` symbol `fakctxexpensegate`; use this spelling for that operation rather than the undifferentiated family name.
- **`fakguardcappark`** — the exact `guard-gate` symbol `fakguardcappark`; use this spelling for that operation rather than the undifferentiated family name.
- **`fetchguardinfovars`** — the exact `guard-gate` symbol `fetchguardinfovars`; use this spelling for that operation rather than the undifferentiated family name.
- **`ffngateexps`** — the exact `guard-gate` symbol `ffngateexps`; use this spelling for that operation rather than the undifferentiated family name.
- **`fleetdupguard`** — the exact `guard-gate` symbol `fleetdupguard`; use this spelling for that operation rather than the undifferentiated family name.
- **`foldtrendgate`** — the exact `guard-gate` symbol `foldtrendgate`; use this spelling for that operation rather than the undifferentiated family name.
- **`gatebarecommitsweep`** — the exact `guard-gate` symbol `gatebarecommitsweep`; use this spelling for that operation rather than the undifferentiated family name.
- **`gatebrandconsistency`** — the exact `guard-gate` symbol `gatebrandconsistency`; use this spelling for that operation rather than the undifferentiated family name.
- **`gatebrowsercontract`** — the exact `guard-gate` symbol `gatebrowsercontract`; use this spelling for that operation rather than the undifferentiated family name.
- **`gateconceptadmission`** — the exact `guard-gate` symbol `gateconceptadmission`; use this spelling for that operation rather than the undifferentiated family name.
- **`gatedeadcode`** — the exact `guard-gate` symbol `gatedeadcode`; use this spelling for that operation rather than the undifferentiated family name.
- **`gateduplication`** — the exact `guard-gate` symbol `gateduplication`; use this spelling for that operation rather than the undifferentiated family name.
- **`gatee2eovermocks`** — the exact `guard-gate` symbol `gatee2eovermocks`; use this spelling for that operation rather than the undifferentiated family name.
- **`gategofmt`** — the exact `guard-gate` symbol `gategofmt`; use this spelling for that operation rather than the undifferentiated family name.
- **`gateindexsync`** — the exact `guard-gate` symbol `gateindexsync`; use this spelling for that operation rather than the undifferentiated family name.
- **`gatemodedefault`** — the exact `guard-gate` symbol `gatemodedefault`; use this spelling for that operation rather than the undifferentiated family name.
- **`gateprimary`** — the exact `guard-gate` symbol `gateprimary`; use this spelling for that operation rather than the undifferentiated family name.

## Follow-on discovered names

- **`gatepythongate`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`gatereason`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`gatesecretshape`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`gatetrustwidening`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardbannerversion`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardchildspawnmetadata`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardedonly`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardendpointnodes`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardfleetbus`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardinfobytestext`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardlaunchanim`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardreapstaletempdirs`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardreloaddefaultfloor`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardrotationruntime`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardtoolprocinstall`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardtoolprocsummaryline`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`hasguard`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`headburstgate`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`newguardendpointsprovider`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`skippedgates`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`startguardlifecycleserver`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`watchdogguards`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`breathgate`** — the exact `guard-gate` symbol; distinct from the broader family label and sibling operations.
- **`guardactiveenv`** — the exact guard-gate symbol, not the broader family label.
- **`guardcodexoauthplaceholderapikey`** — the exact guard-gate symbol, not the broader family label.
- **`guardalloweffectivereadlayers`** — the exact guard-gate symbol, not the broader family label.
