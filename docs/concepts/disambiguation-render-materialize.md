# render-materialize names

This map positions the current `render-materialize` coverage backlog. Each entry names the exact repository symbol; the family label remains the broader domain and is not a substitute for the symbol.

- **`docrender`** — the exact `render-materialize` symbol `docrender`; use this spelling for that operation rather than the undifferentiated family name.
- **`materializeraw`** — the exact `render-materialize` symbol `materializeraw`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderings`** — the exact `render-materialize` symbol `renderings`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderissue`** — the exact `render-materialize` symbol `renderissue`; use this spelling for that operation rather than the undifferentiated family name.
- **`rendertui`** — the exact `render-materialize` symbol `rendertui`; use this spelling for that operation rather than the undifferentiated family name.
- **`cronrender`** — the exact `render-materialize` symbol `cronrender`; use this spelling for that operation rather than the undifferentiated family name.
- **`eventmaterialized`** — the exact `render-materialize` symbol `eventmaterialized`; use this spelling for that operation rather than the undifferentiated family name.
- **`materializegittree`** — the exact `render-materialize` symbol `materializegittree`; use this spelling for that operation rather than the undifferentiated family name.
- **`phasematerialized`** — the exact `render-materialize` symbol `phasematerialized`; use this spelling for that operation rather than the undifferentiated family name.
- **`rendercli`** — the exact `render-materialize` symbol `rendercli`; use this spelling for that operation rather than the undifferentiated family name.
- **`rendercounts`** — the exact `render-materialize` symbol `rendercounts`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderdispatchwaveplanrows`** — the exact `render-materialize` symbol `renderdispatchwaveplanrows`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderdocindex`** — the exact `render-materialize` symbol `renderdocindex`; use this spelling for that operation rather than the undifferentiated family name.
- **`rendergitmainttext`** — the exact `render-materialize` symbol `rendergitmainttext`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderinfofleetrows`** — the exact `render-materialize` symbol `renderinfofleetrows`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderinventory`** — the exact `render-materialize` symbol `renderinventory`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderpromptrules`** — the exact `render-materialize` symbol `renderpromptrules`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderrecoverysummary`** — the exact `render-materialize` symbol `renderrecoverysummary`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderscore`** — the exact `render-materialize` symbol `renderscore`; use this spelling for that operation rather than the undifferentiated family name.
- **`renderundeclaredleafreason`** — the exact `render-materialize` symbol `renderundeclaredleafreason`; use this spelling for that operation rather than the undifferentiated family name.
- **`surrender`** — the exact `render-materialize` symbol `surrender`; use this spelling for that operation rather than the undifferentiated family name.
- **`testwaveunitneverrenderscompletevialeafderivation`** — the exact `render-materialize` symbol `testwaveunitneverrenderscompletevialeafderivation`; use this spelling for that operation rather than the undifferentiated family name.
