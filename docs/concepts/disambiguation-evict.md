---
title: "evict names"
description: "Map of exact repository symbols that use the evict root, kept distinct from the broader family label and sibling operations."
---

# evict names

This map positions the current `evict` coverage backlog. Each entry names the exact repository symbol; the family label remains the broader domain and is not a substitute for the symbol.

## Follow-on discovered names

- **`expertringevict`** — the exact `evict` symbol; distinct from the broader family label and sibling operations.
- **`qevict`** — the exact `evict` symbol; distinct from the broader family label and sibling operations.
- **`qevicteval`** — the exact `evict` symbol; distinct from the broader family label and sibling operations.
