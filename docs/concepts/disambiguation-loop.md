# loop names

This map positions the current `loop` coverage backlog. Each entry names the exact repository symbol; the family label remains the broader domain and is not a substitute for the symbol.

- **`resolvedcodexloophome`** — the exact `loop` symbol `resolvedcodexloophome`; use this spelling for that operation rather than the undifferentiated family name.
- **`codexloop`** — the exact `loop` symbol `codexloop`; use this spelling for that operation rather than the undifferentiated family name.
- **`applycodexloopsessionmeta`** — the exact `loop` symbol `applycodexloopsessionmeta`; use this spelling for that operation rather than the undifferentiated family name.
- **`codextoploopdrivingoutcome`** — the exact `loop` symbol `codextoploopdrivingoutcome`; use this spelling for that operation rather than the undifferentiated family name.
- **`diagnosecodexloop`** — the exact `loop` symbol `diagnosecodexloop`; use this spelling for that operation rather than the undifferentiated family name.
- **`loopcheck`** — the exact `loop` symbol `loopcheck`; use this spelling for that operation rather than the undifferentiated family name.
- **`loopdriveregionhold`** — the exact `loop` symbol `loopdriveregionhold`; use this spelling for that operation rather than the undifferentiated family name.
- **`looplist`** — the exact `loop` symbol `looplist`; use this spelling for that operation rather than the undifferentiated family name.
- **`loopts`** — the exact `loop` symbol `loopts`; use this spelling for that operation rather than the undifferentiated family name.
- **`loopwire`** — the exact `loop` symbol `loopwire`; use this spelling for that operation rather than the undifferentiated family name.
- **`ownssessionloop`** — the exact `loop` symbol `ownssessionloop`; use this spelling for that operation rather than the undifferentiated family name.
- **`startgatewayusagesnapshotloop`** — the exact `loop` symbol `startgatewayusagesnapshotloop`; use this spelling for that operation rather than the undifferentiated family name.
