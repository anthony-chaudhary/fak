---
title: "witness-proof names"
description: "This map positions the current witness-proof coverage backlog. Each entry names the exact repository symbol;"
---
# witness-proof names

This map positions the current `witness-proof` coverage backlog. Each entry names the exact repository symbol; the family label remains the broader domain and is not a substitute for the symbol.

- **`sourcewitness`** — the exact `witness-proof` symbol `sourcewitness`; use this spelling for that operation rather than the undifferentiated family name.
- **`changedwitness`** — the exact `witness-proof` symbol `changedwitness`; use this spelling for that operation rather than the undifferentiated family name.
- **`independentwitness`** — the exact `witness-proof` symbol `independentwitness`; use this spelling for that operation rather than the undifferentiated family name.
- **`correlatewitness`** — the exact `witness-proof` symbol `correlatewitness`; use this spelling for that operation rather than the undifferentiated family name.
- **`twitness`** — the exact `witness-proof` symbol `twitness`; use this spelling for that operation rather than the undifferentiated family name.
- **`witnesscorrelation`** — the exact `witness-proof` symbol `witnesscorrelation`; use this spelling for that operation rather than the undifferentiated family name.
- **`witnesstailbytes`** — the exact `witness-proof` symbol `witnesstailbytes`; use this spelling for that operation rather than the undifferentiated family name.
- **`changedwitnesskind`** — the exact `witness-proof` symbol `changedwitnesskind`; use this spelling for that operation rather than the undifferentiated family name.
- **`codexguardwitness`** — the exact `witness-proof` symbol `codexguardwitness`; use this spelling for that operation rather than the undifferentiated family name.
- **`commitiswitnessed`** — the exact `witness-proof` symbol `commitiswitnessed`; use this spelling for that operation rather than the undifferentiated family name.
- **`evidencewitnessed`** — the exact `witness-proof` symbol `evidencewitnessed`; use this spelling for that operation rather than the undifferentiated family name.
- **`issueresolvewitnessed`** — the exact `witness-proof` symbol `issueresolvewitnessed`; use this spelling for that operation rather than the undifferentiated family name.
- **`kvprefixwitness`** — the exact `witness-proof` symbol `kvprefixwitness`; use this spelling for that operation rather than the undifferentiated family name.
- **`resolvewitnessfiles`** — the exact `witness-proof` symbol `resolvewitnessfiles`; use this spelling for that operation rather than the undifferentiated family name.
- **`witnessschema`** — the exact `witness-proof` symbol `witnessschema`; use this spelling for that operation rather than the undifferentiated family name.
- **`fakwitnessargkey`** — the exact `witness-proof` symbol `fakwitnessargkey`; use this spelling for that operation rather than the undifferentiated family name.

### Native performance graph vocabulary

- **`ClassWitnessed`** — the exact `witness-proof` symbol `ClassWitnessed`; marks an observed throughput value backed by the named benchmark receipt or issue evidence; projected floors and roofs remain `ClassHypothesis` and cannot use this class.


### DisambiguationWitnesses (worker-land scorecard proof bundle)

DisambiguationWitnesses is the workerworktree landing receipt that groups the before, isolated-worktree, and post-apply concept-scorecard witnesses under one shared timeout authority.

**Distinct from:** It is the typed three-view proof bundle consumed by managed worker landing, not worktreewitness, which runs an arbitrary command in a detached trunk worktree.
