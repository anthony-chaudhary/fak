---
title: "score-debt names"
description: "Map of exact repository symbols that use the score-debt root, kept distinct from the broader family label and sibling operations."
---

# score-debt names

This map positions the current `score-debt` coverage backlog. Each entry names the exact repository symbol; the family label remains the broader domain and is not a substitute for the symbol.

## Follow-on discovered names

- **`mlpdebt`** — the exact `score-debt` symbol; distinct from the broader family label and sibling operations.


### performance-rsi-scorecard

The fak performance-rsi-scorecard CLI command, implemented by cmdPerformanceRSIScorecard, scores versioned end-to-end performance RSI evidence and optionally compares it with a prior report to measure whether the complete performance improvement loop is closing.

**Distinct from:** Use performance-rsi-scorecard for the end-to-end performance improvement-loop scorecard over versioned evidence, including comparison with a prior report. Unlike mlpdebt, it does not name the MLP scorecard hard-debt integer; unlike generic debt or score surfaces, it is specifically the performance RSI loop evaluator rather than an umbrella metric or reusable scoring mechanism.
