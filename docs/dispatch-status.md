---
title: "fak issue dispatch status: fleet worker and backlog tracker"
description: "Auto-generated fak issue dispatch status: dispatcher and worker state, open-issue backlog by lane, and closure-honesty rate across the fleet."
---

# Issue dispatch status — 2026-07-01

_Auto-generated by `tools/dispatch_status.py --md`. Do not hand-edit; re-run the tool (or the `FleetDispatchStatusDoc` task) to refresh._

- **dispatcher**: `STALLED` (ok)
- **workers**: 0/2 live (headroom 2); host clean
- **primary limiter**: `configured_max` (cap=2 live=0 headroom=2 max=2 target=0 host_cap=16 host_binding=cores seats=2 free=1 leased=0 lane_leases=0/0)
- **switcher account**: `day30-netra` (t1, opus), available=True
- **always-on watchdog**: installed (Ready)
- **supervisor**: `PLAN_SURFACE_EMPTY` (alive 0/0)
- **seat inventory**: 4 seat(s) - available=3 busy=0 cooling=1 unavailable=0
- **run status source**: `dos status` digests for 6 RID(s), errors=0
- **worker/lease cross-check**: clean=0, orphan-process=0, orphan-lease=0

## Backlog by lane (issue → lane sync)

Open issues: **264** — routed 236, unrouted 28.

| lane | open issues |
|---|---|
| docs | 91 |
| tools | 35 |
| metrics | 19 |
| bench | 17 |
| gateway | 12 |
| compute | 7 |
| ci | 5 |
| cmd | 5 |
| model | 5 |
| policy | 5 |
| engine | 4 |
| experiments | 4 |
| agent | 3 |
| claude | 3 |
| ggufload | 3 |
| cachemeta | 2 |
| kvmmu | 2 |
| scoreboard | 2 |
| session | 2 |
| sessionimage | 2 |
| ctxplan | 1 |
| devindex | 1 |
| examples | 1 |
| modelroute | 1 |
| promptmmu | 1 |
| recall | 1 |
| swebench | 1 |
| vdso | 1 |

## Closure honesty

`closure_rate` = **0.197** (TRUE_RESOLVED / (TRUE_RESOLVED + CLAIMED_CLOSED) — strict diff-witness); `honest_close_rate` = **0.1997** (also credits the DATA rung).

| bucket | count |
|---|---|
| TRUE_RESOLVED | 290 |
| DATA_RESOLVED | 5 |
| CLAIMED_CLOSED | 1182 |
| OPEN_WITNESSED (closable now) | 0 |

## Throughput (closed issues per hour)

`verdict` = **ON_TRACK** — **21.0/h** completed over the trailing **6h** window (target **10.0/h**). Graded on the *completed* (resolved, not wontfix/dup) rate.

| window | closed | completed | completed /h | loop-closed | loop /h |
|---|---|---|---|---|---|
| 1h | 17 | 17 | 17.0 | 0 | 0.0 |
| 3h | 64 | 64 | 21.33 | 0 | 0.0 |
| 6h | 126 | 126 | 21.0 | 13 | 2.17 |
| 12h | 193 | 193 | 16.08 | 31 | 2.58 |
| 24h | 238 | 238 | 9.92 | 40 | 1.67 |

Loop's last attributed close: 256.8 min ago. A gh-rate far above the loop-rate means humans/peers are draining the backlog, not the dispatcher.

## Active lane leases

No active lane leases under `refs/fak/locks/*`.

## Worker / lease cross-check

clean=0, orphan-process=0, orphan-lease=0.

Clean matches:

_No live worker has a matching active lease._

Orphan processes:

_No OS-visible dispatch worker is missing an active lease._

Orphan leases:

_No active lease is missing a local live worker sidecar._

## Backend health / reallocation

**1** backend(s) are spinning dead (a streak of banner-only / 0-byte worker logs the weekly-cap gate doesn't catch — e.g. a credit-walled codex or a glm worker that prints only its startup banner). The dispatcher holds their spawns and a healthy backend claims the freed lane + budget; one re-probe worker is admitted per interval, so each auto-restores the moment it produces a real turn again.

| backend | dead since | lane reallocated | re-probe (min) |
|---|---|---|---|
| opencode | 2026-06-30T22:07:59.281305Z | docs | 30 |

Backend stub-rate (recent resolve logs):

| backend | lookback | recent logs | productive | stub | stub rate | verdict | evidence |
|---|---:|---:|---:|---:|---:|---|---|
| opencode | 90m | 28 | 0 | 28 | 1.0 | **MAJORITY_STUB** | `resolve-1346-20260629-235906.log`, `resolve-1305-20260630-035909.log`, `resolve-1303-20260630-035901.log` |

## Hook health (guard-layer binding)

All backends bound their guard hooks over the recent window — no `hook: <name> Failed` storm.

| backend | lookback | sessions | sessions w/ hook fail | fail-session rate | hook failures | verdict | evidence |
|---|---:|---:|---:|---:|---:|---|---|
| claude | 90m | 28 | 0 | 0.0 | 0 | ok | — |

## Guard coverage (kernel decisions on the dispatch path)

`fak guard` recorded **673** kernel decision(s) across **24** guarded dispatch session(s) (**0** within 90m) — the WITNESS that the concurrent-dispatch path ran THROUGH the kernel, not just got configured to. Each session owns a unique hash-chained journal (`fak audit verify <file>`); the decision mix is the kernel's verdict tally.

- **denied** (DENY + RESULT_DENY): 4
- **quarantined**: 47
- **empty sessions** (booted under guard, no adjudicated tool call): 2

| decision kind | count |
|---|---:|
| DECIDE | 622 |
| QUARANTINE | 47 |
| DENY | 4 |

Recent journals: `sessionimage-claude-35732-11031111.jsonl`, `policy-claude-55308-8f2c7667.jsonl`, `cmd-claude-46072-e0af5ae5.jsonl`, `ci-claude-33728-1f0d8668.jsonl`, `docs-claude-1596-b44fb818.jsonl`.

## Workers that produced nothing

None — every spawned worker either produced output or is still running.

---

Reasons: safe to spawn: 0/2 live, account 'day30-netra' free; always-on watchdog installed (Ready); 1 backend(s) held dead, lane reallocated (opencode->docs) — a healthy backend is covering; auto-restores on recovery; backend stub-rate majority-stub over recent logs (opencode 28/28 stub) — inspect backend output; fak guard witnessed 673 kernel decision(s) across 24 dispatch session(s) (4 denied, 47 quarantined); throughput ON_TRACK (21.0/h completed over the 6h analysis window, target 10.0/h); run truth from dos status digest for 6 RID(s); seat inventory: 4 seat(s) - available=3 busy=0 cooling=1 unavailable=0
