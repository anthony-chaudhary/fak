# Current task queue read-back

Captured on 2026-08-11 from `gh issue list --state all --limit 100 --json number,title,state,body,labels,milestone`, rendered by the committed `fak task queue` implementation from a clean archive of `main`. Attempt input was an empty explicit fixture because this capture proves the durable default leaf fold; PID/account/token/heartbeat/lease details remain available only through `--attempts` or `--json`.

The machine-readable source was kept in the OS scratchpad rather than committed because issue bodies can contain operational details. The scrubbed human render is [`task-queue-current.txt`](task-queue-current.txt).
