{
  "schema": "fak-microcontext-semantic-packet/1",
  "corpus_sha256": "0740c911815eba7e44c63a14e021f418b66e8a42556d40f14b8b4cd58f173827",
  "selection": "lowest title_sha256 among records with body \u003e=300 bytes, independently per tune/test split",
  "records": [
    {
      "ID": "issue-6084",
      "Split": "tune",
      "Title": "fix(archreport): keep reports usable with stale tier declarations",
      "Body": "\u003c!-- archreport-dogfood-defect: stale-declaration-global-outage --\u003e\n\n## Parent context\n\n#6075\n\n## Problem\n\nA real `fak architecture` dogfood run against committed `origin/main` fails at the stale `docrender` tier row because `internal/docrender` does not exist. Since `Analyze` inspects every declared leaf even for `--leaf`, one stale row prevents both full reports and every scoped query.\n\n## Done\n\n- Architecture reporting represents stale tier declarations as named diagnostics instead of aborting the whole graph.\n- `--leaf` remains usable when an unrelated declaration is stale.\n- Full JSON/text reports name the stale declaration and recovery action.\n- A synthetic fixture proves one stale row does not suppress healthy leaves.\n\n## Witness\n\n`go test ./internal/archreport ./cmd/fak -run 'Stale|Missing|Architecture' -count=1`\n\n## Work estimate\n\n3 points.\n\n## Completion standard\n\nProduction.\n\n## Overall completion contribution\nContribution: 3/3 points.\n\n## Target operating envelope\n- healthy declared leaves: \u003e= 1 leaves\n- stale declarations: \u003e= 1 declarations\n\n## Witnessed operating envelope\n- healthy declared leaves: \u003e= 1 leaves\n- stale declarations: \u003e= 1 declarations\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/6084",
      "Number": 6084
    },
    {
      "ID": "issue-5146",
      "Split": "tune",
      "Title": "fix(ci): reconcile remaining decisive-gate governance baselines",
      "Body": "## Problem\n\nDecisive ci-fast run 29535590661 is green through build, vet, and uncached architest, then fails in go test ./... on stale generated/governance expectations after current trunk additions.\n\n## Done condition\n\n- Reproduce each remaining governance failure against committed tip 39122526c1.\n- Update only the authoritative baseline/fixture or fix the violating implementation; do not weaken gates.\n- ci-fast.yml is green on the resulting main SHA.\n\n## Witness\n\nhttps://github.com/anthony-chaudhary/fak/actions/runs/29535590661",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5146",
      "Number": 5146
    },
    {
      "ID": "issue-5782",
      "Split": "tune",
      "Title": "idea-scout: AgentAntibody: An Adaptive Immune System for Defending LLM Agents against Prompt Injection",
      "Body": "\u003e Auto-filed by the daily **idea-scout** (`tools/idea_scout.py`, 2026-08-06). A candidate RELATED idea found on arXiv; **needs human triage** — close as `wontfix`/`duplicate` if it is not worth pursuing.\n\n**Source:** https://arxiv.org/abs/2608.04053\n\n**Authors:** Shihao Weng, Yang Feng, Xiaofei Xie, Jiongchi Yu\n**Submitted:** 2026-08-04\n\n**Why surfaced** (topic `prompt-injection-defense`, score 57): terms: prompt injection(title), defense, agent(title); recent (2d); very fresh (≤30d)\n\n### Dispatchability\n- dispatchability: `triage_only`\n- reason: idea-scout candidates need human scope, lane, witness, and acceptance criteria before they become worker-ready leaves.\n\n**Summary**\n\nPrompt injection remains a critical threat to LLM agents, yet existing defenses treat each task as a self-contained problem, independent of previous encounters. In practice, user requests are often underspecified: they describe the desired outcome without fully specifying acceptable behavior. An injection can exploit this ambiguity, causing the agent to complete the task in a way the user would reject. As the user's expectations become clearer through concrete cases, a defense should learn from each encounter and apply what it learns to the next. Inspired by adaptive immunity, we propose AgentAntibody, which equips LLM agents with a self-evolving immune system against prompt injection. Ag…\n\n---\n_Triage hint: is this a capability fak should adopt, a threat it should defend against, or prior art to cite? If none, close it._\n\u003c!-- idea-scout-source: arxiv:2608.04053 --\u003e",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5782",
      "Number": 5782
    },
    {
      "ID": "issue-5211",
      "Split": "tune",
      "Title": "Fail closed on Claude dogfood candidate provenance before install swap",
      "Body": "## Finding\n`scripts/dogfood-claude.ps1 --install` builds and lock-safely swaps `fak.exe`, but does not execute the staged candidate and verify that its reported `build:` revision equals repository HEAD before the swap.\n\nThe committed-HEAD archive fallback is now stamped (c37a73776), and CI has a static regression test, but an install-time read-back would fail closed against future provenance regressions or a stale/corrupt staged copy.\n\n## Done condition\n- Add a fail-closed candidate provenance read-back before `Move-Item` swaps the installed executable.\n- Verify both the built path and staged `.new` path report the expected 12-character HEAD revision.\n- Add a regression test proving verification occurs before swap.\n- Capture PowerShell parse plus `fak validate --mine` evidence.",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5211",
      "Number": 5211
    },
    {
      "ID": "issue-5718",
      "Split": "tune",
      "Title": "research(serve): evaluate triton-inference-server/server against fak",
      "Body": "## Parent context\r\n\r\nPart of #2136.\r\n\r\n## Research source\r\n\r\n- GitHub repository: https://github.com/triton-inference-server/server\r\n- Freshness observed: pushed `2026-08-06T14:48:56Z`; 10,909 stars at triage time.\r\n- Source summary: The Triton Inference Server provides an optimized cloud and edge inferencing solution. \r\n\r\n## Why this matters\r\n\r\nThis active project overlaps fak's serving boundary. We need measured compatibility and differentiated behavior, not a name-only landscape entry.\r\n\r\n## Proposed contract\r\n\r\nRun a bounded serving comparison for routing, prefix reuse, cancellation, streaming, and failure receipts.\r\n\r\n## Done when\r\n\r\n1. Pin the upstream revision or release and record license, supported transports/providers, and claimed operating envelope.\r\n2. Commit a bounded, rerunnable probe using fak's real public seam; do not rely on screenshots or prose-only comparison.\r\n3. Capture machine-readable results for one known-positive case and one adversarial, cancellation, isolation, or failure case.\r\n4. Produce a source-linked gap table that classifies each finding as adopt, interoperate, differentiate, or no-action.\r\n5. File implementation follow-ups for every adopted gap; if no action is warranted, record the evidence-backed reason.\r\n\r\n## Proof artifacts\r\n\r\n- Pinned upstream identity and source URL.\r\n- Committed probe/fixture plus raw machine-readable receipt.\r\n- Gap table with fak module owners and explicit disposition.\r\n\r\n## Dedupe marker\r\n\r\n`research-source:https://github.com/triton-inference-server/server`\r\n\r\n## Work estimate\r\nEstimate: 1 point. Uncertainty: bounded research spike; implementation work is filed separately.\r\n\r\n## Overall completion contribution\r\nContribution: 1/100 points.\r\n\r\n## Completion standard\r\nresearch\r\n\r\n## Target operating envelope\r\n- repository scope: \u003e= 1 pinned upstream repository\r\n- witness cases: \u003e= 2 (known-positive plus adversarial/failure)\r\n\r\n## Witnessed operating envelope\r\n- repository scope: = 0 before this issue\r\n- witness cases: = 0 before this issue\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5718",
      "Number": 5718
    },
    {
      "ID": "issue-5847",
      "Split": "tune",
      "Title": "fix(pythongate): port-or-delete tools/fleet_regdir*.py — TestNoNewPythonTools is red on main and blocks every tools-lane commit",
      "Body": "## Problem\n\n`go test ./internal/pythongate -run TestNoNewPythonTools` is **red on `main`** and has been since\n5 Aug 2026. Two tracked Python tools are not in the grandfathered baseline:\n\n```\n--- FAIL: TestNoNewPythonTools (0.04s)\n    2 NEW python tool(s) not in the grandfathered baseline:\n      tools/fleet_regdir.py is a NEW python tool; port it to Go instead (NEW_PYTHON_TOOL)\n      tools/fleet_regdir_test.py is a NEW python tool; port it to Go instead (NEW_PYTHON_TOOL)\nFAIL\tgithub.com/anthony-chaudhary/fak/internal/pythongate\t0.192s\n```\n\n## Why it matters\n\nThe `tools` lane's refusal mode is \"the pre-commit hook refuses the commit until the package is\ngreen.\" So this does not just fail a test — it **blocks every `tools`-lane commit on trunk**. It was\nfound while working #5624, whose commit path it blocks independently of that issue's own blockers.\n\n## Evidence\n\n| fact | value |\n|---|---|\n| introducing commit | `6f98eb4106` `fix(tools): converge the Python fleet registry resolvers on the host ladder (fak tools)` |\n| date | Wed 5 Aug 2026 |\n| ancestor of `HEAD` | yes |\n| `git status --porcelain` on both paths | clean — tracked and committed, not in-flight |\n| `internal/pythongate/baseline.go` entries | 593 |\n| `git ls-files 'tools/*.py'` | 595 |\n\nThe 2-entry gap is exactly these two files. `6f98eb4106` added them without the ratchet ever going\ngreen.\n\n## The fix is NOT to grow the baseline\n\n`internal/pythongate/doc.go` is explicit: the allowlist \"can never grow\", and `baseline.go` is\nregenerated \"only to TIGHTEN it after a port-and-delete — never to re-admit a new tool.\" Appending\nthese two paths would silently loosen the ratchet and defeat the de-Python policy it exists to\nenforce.\n\nSo the correct resolutions are, in preference order:\n\n1. Port `fleet_regdir.py` to Go (house pattern: `internal/\u003cname\u003e/` package + `cmd/fak/` shell),\n   `git rm` both Python files, then regenerate `baseline.go` by the recipe in `doc.go` (which will\n   shrink it, as intended).\n2. If the logic already exists in Go and these are dead, delete them and regenerate.\n\n## Non-goals\n\n- Not a change to the pythongate ratchet's policy or its allow/refuse semantics.\n- Not a fix for #5624's own blockers (pythongate-vs-spec, and the AF_UNIX transport gap).\n- Not a sweep of other Python tooling.\n\n## Witness\n\n`go test ./internal/pythongate -count=1` green on `main`, with `baseline.go` entry count strictly\n**lower** than 593 (never higher), and `git ls-files 'tools/*.py'` matching it.\n\n## Routing\n\nLane: `tools` (tree `tools/**`, `scripts/**`) — currently held exclusive by live holder `w4591`\n(pid 80596, acquired 2026-07-18), which is why this is filed rather than fixed. The `internal/pythongate`\nhalf is a separate tree if the port lands there.\n\nRelated: #2653 (epic: pre-push the live-tree governance ratchets so the trunk can't red on\nunclassified new work) — this is a concrete instance of exactly that failure mode, but that epic is\nabout prevention and does not clear this red.\n\nFound while working #5624.\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5847",
      "Number": 5847
    },
    {
      "ID": "issue-5501",
      "Split": "tune",
      "Title": "idea-scout: Just Testing, Move Along: Evasion of LLM-based System Log Interpretation by Prompt Injection",
      "Body": "\u003e Auto-filed by the daily **idea-scout** (`tools/idea_scout.py`, 2026-07-30). A candidate RELATED idea found on arxiv; **needs human triage** — close as `wontfix`/`duplicate` if it is not worth pursuing.\n\n**Source:** https://arxiv.org/abs/2607.24174\n\n**Authors:** Max Landauer, Florian Skopik, Markus Wurzenberger, Franciszek Górski, Mateusz Krzysztoń\n**Submitted:** 2026-07-27\n\n**Why surfaced** (topic `prompt-injection-defense`, score 47): terms: prompt injection(title), untrusted; recent (3d); very fresh (≤30d)\n\n### Dispatchability\n- dispatchability: `triage_only`\n- reason: idea-scout candidates need human scope, lane, witness, and acceptance criteria before they become worker-ready leaves.\n\n**Summary**\n\nLarge Language Models (LLMs) are increasingly integrated into Security Operations Center (SOC) workflows, where they support analysts in tasks such as the interpretation of system logs. However, the ability of LLMs to directly process untrusted textual input also introduces new attack surfaces. In particular, attackers can inject contextual information or explicit instructions into log entries in order to influence how malicious activity is interpreted by the model. Despite the growing adoption of LLMs for log analytics, the robustness of such systems against adversarial log injection remains largely unexplored. To address this gap, this paper presents a framework for evaluating prompt in…\n\n---\n_Triage hint: is this a capability fak should adopt, a threat it should defend against, or prior art to cite? If none, close it._\n\u003c!-- idea-scout-source: arxiv:2607.24174 --\u003e",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5501",
      "Number": 5501
    },
    {
      "ID": "issue-5253",
      "Split": "tune",
      "Title": "fix(commitlane): auto-reap a stale bare .git/index.lock left by a dead raw-git holder (recurring fleet-wide commit wedge)",
      "Body": "## Symptom\n\nRecurring **fleet-wide commit outage**: for 10–15+ minutes at a stretch, `HEAD` goes static, `git log --since='3 minutes ago'` shows **0 commits across the whole fleet**, and every `fak commit` returns `LOCK_BUSY` (`another git process is active (index.lock held Ns); retry shortly`). Polite spaced retries starve — dozens of workers all block on the same held lock.\n\n## Evidence (observed twice in one session)\n\n- `.git/index.lock` held **512s**, then later **713s**, by a holder that was **already dead** — no live `git.exe` process was anywhere near that age (verified via process start-times; newest real git PIDs were 0–1s reads, the rest 15h-idle daemons).\n- Throughout, `.git/fak-commit.lock` churned young (1–16s) — peers were *trying* and failing, not holding.\n- Each wedge eventually cleared on its own (HEAD advanced again), but only after the multi-minute outage.\n\n## Root cause: a bare `index.lock` is never auto-reaped\n\nThe fleet reaps the *other* lock classes but deliberately not this one:\n- `internal/treedoctor` reaps a **dead-holder `fak-commit.lock`** and renamed-aside **`.lock.orphan`** residue only. Its own invariant (`treedoctor.go`): *an active git lock ends in exactly `.lock` (`index.lock`, …), so a name carrying `.lock.orphan` is provably NOT a lock git holds* — i.e. a **bare `index.lock` is conservatively skipped**.\n- `internal/commitlane/status.go` `DefaultStaleIndexAge = 15m` only sets `StaleHint` on the **operator surface** — it triggers no auto-clear.\n- The `leaseref` reaper keys on a **holder PID**, which git's `index.lock` does not carry.\n\nSo when a *raw* `git` subprocess (not `fak-commit`) dies holding `index.lock`, nothing recovers it until a human/operator intervenes or it happens to clear.\n\n## Why this is safely fixable\n\ngit writes `index.lock` with `O_CREAT|O_EXCL` — a **plain leftover file**, no OS-level lock (unlike `fak-commit.lock`'s Windows `LockFileEx`). A stale one is just a stale file. treedoctor already owns the safe \"rename-aside to `*.lock.orphan-recovered-\u003cn\u003e` then reap residue\" pattern.\n\n## Proposal\n\nExtend `fak dev tree-doctor --apply` (and/or the commit-lane maintenance tick) to recover a bare `.git/index.lock` when **both**:\n1. age ≥ a conservative threshold (reuse `DefaultStaleIndexAge = 15m`), **and**\n2. a liveness gate shows no committer is actually mid-write (e.g. no live `fak-commit` lease holder + no advancing HEAD),\n\nby renaming it aside to the existing `index.lock.orphan-recovered-\u003cn\u003e` residue form (which the current residue reaper then removes). Report-only by default; only `--apply` acts.\n\nImpact: turns a 10–15min recurring fleet-wide commit stall into a self-healing event, and is the same delicate contention/wedge class the always-on loop keeps hitting.",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5253",
      "Number": 5253
    },
    {
      "ID": "issue-5238",
      "Split": "tune",
      "Title": "idea-scout: How Agents Ask for Permission: User Permissions for AI Agents, from Interfaces to Enforcement",
      "Body": "\u003e Auto-filed by the daily **idea-scout** (`tools/idea_scout.py`, 2026-07-18). A candidate RELATED idea found on arxiv; **needs human triage** — close as `wontfix`/`duplicate` if it is not worth pursuing.\n\n**Source:** https://arxiv.org/abs/2607.13718\n\n**Authors:** Alexandra E. Michael, Franziska Roesner\n**Submitted:** 2026-07-15\n\n**Why surfaced** (topic `prompt-injection-defense`, score 47): terms: prompt injection, agent(title); recent (3d); very fresh (≤30d)\n\n### Dispatchability\n- dispatchability: `triage_only`\n- reason: idea-scout candidates need human scope, lane, witness, and acceptance criteria before they become worker-ready leaves.\n\n**Summary**\n\nAs AI agents gain prevalance, users are increasingly exposed to the risks such systems entail. Prompt injection attacks, as well as hallucination, can cause agents to leak private information to third parties. As autonomous systems, agents also present the more active danger of performing sensitive tasks, such as bank transactions, without the user's intent or authorization. Recognizing this challenge, the agentic security community has developed numerous proposals for secure agentic systems. Much of this work has focused on product-level approaches, where agentic system developers determine and apply the same security policies and permissions to all users. Yet different users have differ…\n\n---\n_Triage hint: is this a capability fak should adopt, a threat it should defend against, or prior art to cite? If none, close it._\n\u003c!-- idea-scout-source: arxiv:2607.13718 --\u003e",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5238",
      "Number": 5238
    },
    {
      "ID": "issue-5507",
      "Split": "tune",
      "Title": "idea-scout: Security of World-Model-Based Embodied AI: A Lifecycle of Threats, Defenses, and Evaluation",
      "Body": "\u003e Auto-filed by the daily **idea-scout** (`tools/idea_scout.py`, 2026-07-31). A candidate RELATED idea found on arxiv; **needs human triage** — close as `wontfix`/`duplicate` if it is not worth pursuing.\n\n**Source:** https://arxiv.org/abs/2607.28226\n\n**Authors:** Fazhong Liu, Zhuoyan Chen, Haozhen Tan, Yan Meng, Guoxing Chen, Haojin Zhu\n**Submitted:** 2026-07-30\n\n**Why surfaced** (topic `prompt-injection-defense`, score 50): terms: prompt injection, defense(title), tool; recent (1d); very fresh (≤30d)\n\n### Dispatchability\n- dispatchability: `triage_only`\n- reason: idea-scout candidates need human scope, lane, witness, and acceptance criteria before they become worker-ready leaves.\n\n**Summary**\n\nWorld models give embodied AI a predictive core: they compress observations into states, simulate action-conditioned futures, and enable planning beyond reactive control. This predictive layer, however, opens a new security boundary-compromise can propagate from data, sensors, prompts, or feedback into physical action. Rather than treating world models as an isolated component, this survey traces threats across their entire lifecycle-from data construction and representation learning, through state grounding and imagination, to trajectory evaluation, execution, and long-term adaptation via memory and tools. We show that familiar attack families: poisoning, backdoors, adversarial examples,…\n\n---\n_Triage hint: is this a capability fak should adopt, a threat it should defend against, or prior art to cite? If none, close it._\n\u003c!-- idea-scout-source: arxiv:2607.28226 --\u003e",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5507",
      "Number": 5507
    },
    {
      "ID": "issue-6014",
      "Split": "tune",
      "Title": "feat(benchauthority): add assertion-level witness records and recover the second-next rung",
      "Body": "## Parent context\r\n\r\nFollow-up to #3431. Spine witness: `PARENT_COMMIT` adds `docs/notes/PER-CLAIM-BENCHMARK-WITNESS-GRANULARITY-2026-08-09.md`, which decides the schema before implementation.\r\n\r\n## Generation\r\n\r\nnext (`gen/next`)\r\n\r\n## Current state\r\n\r\n`internal/bench/authorityview.go` reads one row-level `status` and `provenance`. Its `EntitledHorizon` comment explicitly says the coarse registry cannot represent the simulation/fixture rung, so the fold never emits `second-next`. The `compaction-shed-per-fire` row in `docs/benchmarks/registry.jsonl` is the proven mixed case: it combines a measured per-fire shed assertion, a functional token-equivalent share, and a retracted historical share under one `live / measured` envelope.\r\n\r\n## Why this is next\r\n\r\nThe design decision is committed and bounded. Implementing the smallest read-only schema spine now prevents future authority consumers from laundering one assertion's witness onto its siblings and recovers a generation rung the current executable view declares collapsed.\r\n\r\n## Generation contract\r\n\r\nThis is `gen/next` because the design spine is committed and the remaining work is a bounded read-only implementation with no runtime refusal. Promote the leaf when the assertion-aware fixture, scalar-compatibility proof, and recovered `second-next` histogram are green. Demote or split it if a census finds no second mixed-witness row. The design is invalidated if stable assertion IDs cannot survive prose edits or the conservative roll-up breaks a current scalar consumer. Generation is orthogonal to priority, branch choice, and runtime policy: `priority/P2` schedules the work, while `gen/next` states its evidence horizon.\r\n\r\n## Working spine\r\n\r\n`docs/benchmarks/registry.jsonl` optional assertion records -\u003e `internal/bench/authorityview.go` claim-level derivation -\u003e deterministic view/test output, with old scalar readers receiving a conservative envelope.\r\n\r\n## Priority context\r\n\r\nWorking path: benchmark registry -\u003e authority derivation -\u003e generated/read-only authority surfaces. Current blocker: mixed rows are machine-readable only as one scalar witness. Unblocks: a later renderer can cite `row/assertion` without inventing provenance. Not polish: this closes a declared missing horizon in the executable fold.\r\n\r\n## Work unit\r\n\r\nleaf\r\n\r\n## Expected steps\r\n\r\n6\r\n\r\n## Assumptions\r\n\r\n- Standard JSON decoding keeps the optional assertion field additive for current subset readers.\r\n- Stable assertion IDs can be attached to independently citable statements.\r\n- `compaction-shed-per-fire` is sufficient as the first real migration fixture.\r\n\r\n## Confusion risks\r\n\r\n- Do not wire a refusing CI gate; derive and render first.\r\n- Do not make assertions mandatory for scalar-witness rows.\r\n- Do not label the functional token-equivalent share as measured merely because its inputs are observed.\r\n- Do not fork a second assertion schema in `internal/benchauthority`; that typed transcription remains a later consumer of this one contract.\r\n\r\n## Coordination notes\r\n\r\n- Preserve existing row-level `status` and `provenance` fields as the conservative compatibility envelope.\r\n- Keep the change on the read-only authority seam; no runtime behavior or benchmark-number change.\r\n\r\n- Required model tier: T1\r\n- Optimal model tier: T1\r\n\r\n## Trigger\r\n\r\n#3431 lands the per-claim schema decision and names the cache-savings row as the first fixture.\r\n\r\n## Batch policy\r\n\r\nOne implementation issue for the JSONL + executable authority-view spine. Renderer/productization or a refusing gate are separate leaves only after this witness lands.\r\n\r\n## In scope\r\n\r\n- Add an optional claim-level assertion shape to `internal/bench/authorityview.go` with closed `status` and `witness_kind` vocabularies.\r\n- Derive `measured -\u003e now`, `functional -\u003e next`, `simulated -\u003e second-next`, and `modeled|unknown -\u003e future`, with tombstone status overriding live horizons.\r\n- Implement the conservative scalar roll-up for old consumers.\r\n- Migrate `compaction-shed-per-fire` in `docs/benchmarks/registry.jsonl` into measured, functional, and retracted assertions.\r\n- Extend `internal/bench/authorityview_test.go` with the real mixed-row fixture, second-next recovery, deterministic output, and scalar-compatibility proof.\r\n\r\n## Out of scope\r\n\r\n- No `internal/benchauthority` transcription, generated ledger rewrite, runtime behavior, public benchmark number change, or CI refusal.\r\n- No bulk migration before a census proves another mixed row.\r\n\r\n## Done condition\r\n\r\nThe executable authority view can classify stable `row/assertion` units, emits `second-next` for a simulated assertion, keeps `compaction-shed-per-fire/shed-per-fire` at `now`, classifies its token-equivalent share at `next`, retains the old share as `retired`, and presents a conservative row envelope to scalar readers.\r\n\r\n## Witness\r\n\r\n`go test ./internal/bench -run 'AuthorityView|AuthorityDerivation|AuthorityAssertion' -count=1`\r\n\r\n## Acceptance gate\r\n\r\n`fak validate --mine internal/bench/authorityview.go --mine internal/bench/authorityview_test.go --mine docs/benchmarks/registry.jsonl`\r\n\r\n## Lane\r\n\r\nbench\r\n\r\n## Path hints\r\n\r\n- `internal/bench/authorityview.go`\r\n- `internal/bench/authorityview_test.go`\r\n- `docs/benchmarks/registry.jsonl`\r\n\r\n## Boundary notes\r\n\r\nPublic repository artifacts only; no private benchmark data, credentials, or lab paths.\r\n\r\n## Closure binding\r\n\r\nThe resolving commit cites this issue and carries the `(fak bench)` trailer; close only after the focused witness and exact-path validation are green on committed main.\n\n## Work estimate\nEstimate: 3 points. Uncertainty: operator-supplied estimate; revise through the parent denominator when evidence changes.\n\n## Overall completion contribution\nContribution: 3/3 points.\n\n## Completion standard\ndevelopment\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/6014",
      "Number": 6014
    },
    {
      "ID": "issue-6016",
      "Split": "tune",
      "Title": "idea-scout: The Bitter Lesson of Tool Calling",
      "Body": "\u003e Auto-filed by the daily **idea-scout** (`tools/idea_scout.py`, 2026-08-09). A candidate RELATED idea found on arXiv; **needs human triage** — close as `wontfix`/`duplicate` if it is not worth pursuing.\n\n**Source:** https://arxiv.org/abs/2608.06370\n\n**Authors:** Ishan Patel, Sahil Sen, Elias Lumer, Vamse Kumar Subbiah\n**Submitted:** 2026-08-06\n\n**Why surfaced** (topic `tool-call-adjudication`, score 47): terms: tool call(title), capability; recent (2d); very fresh (≤30d)\n\n### Dispatchability\n- dispatchability: `triage_only`\n- reason: idea-scout candidates need human scope, lane, witness, and acceptance criteria before they become worker-ready leaves.\n\n**Summary**\n\nTool use transforms LLMs into agents that act beyond their training data, and for code-capable models, programmatic tool calling extends this further by replacing rigid JSON calls with scripts that chain and parallelize naturally. However, a systematic evaluation of tools as code on an established benchmark across current and prior model generations under real-world task conditions has not been conducted. In this work, we empirically compare programmatic tool calling (PTC) to native JSON tool calling across 14 language models on BFCL v4. In the programmatic tool calling paradigm, tools are exposed as typed Python stubs that the model invokes through code, with execution and results handle…\n\n---\n_Triage hint: is this a capability fak should adopt, a threat it should defend against, or prior art to cite? If none, close it._\n\u003c!-- idea-scout-source: arxiv:2608.06370 --\u003e",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/6016",
      "Number": 6016
    },
    {
      "ID": "issue-6072",
      "Split": "tune",
      "Title": "qa: adversarial + edge-case sweep for Architecture graph report",
      "Body": "\u003c!-- fak-issuefanout-key: fanout-archreport-qa-edge-sweep --\u003e\n\n## Generation stream\n\ngen/now\n\n## Parent context\n\n#6068\n\n## Current state\n\nThe working spine of Architecture graph report exists (e0338749c41d); this follow-on has not started.\n\n## Why this is next\n\nSpine-first default: the spine just shipped, so its follow-on backlog is filed at creation time while context is hot.\n\n## Working spine\n\nEvery documented input class of Architecture graph report keeps working under hostile and edge input; the test table is the map of what is proven.\n\n## In scope\n\nTable-driven tests over internal/archreport/, cmd/fak/architecture.go covering empty, oversized, malformed, and hostile inputs and every error path.\n\n## Out of scope\n\nNew features; refactors beyond what a failing test forces.\n\n## Done condition\n\nEdge/adversarial table tests land in internal/archreport/, cmd/fak/architecture.go and fail before / pass after any fix they force.\n\n## Witness\n\ngo test ./internal/archreport -run 'Edge|Adversarial' -v output captured in the commit or issue.\n\n## Acceptance gate\n\nmake test-fast green including the new tests.\n\n## Work estimate\n\nEstimate: 3 points\n\n## Overall completion contribution\n\nContribution: 3/3 points\n\n## Completion standard\n\nproduction\n\n## Target operating envelope\n\n- declared leaves: \u003e= 1 leaves\n- report modes: \u003e= 2 modes\n\n## Witnessed operating envelope\n\n- declared leaves: \u003e= 1 leaves\n- report modes: \u003e= 2 modes\n\n## Closure binding\n\nClose via a commit whose body carries `Closes #\u003cn\u003e` and whose diff lands the witness above.\n\n## Lane\n\narchreport\n\n## Work unit\n\nleaf\n\n## Expected steps\n\n3\n\n## Assumptions\n\n- The spine at e0338749c41d still builds/runs on trunk.\n\n## Confusion risks\n\n- A green test that never drives the real seam is not a witness.\n- Harden what exists; do not grow the spine's scope inside this follow-on.\n\n## Coordination\n\n- Stay inside internal/archreport/, cmd/fak/architecture.go; check the lane lease before dispatch.\n\n## Trigger\n\none-shot: filed at spine-ship time by fak issue fanout\n\n## Batch policy\n\none batch per spine, capped at --max candidates; marker-key (fanout-archreport-*) dedupe against existing issues before filing\n\n## Likely files\n\n- `internal/archreport/`\n- `cmd/fak/architecture.go`\n\nFiled by `fak issue fanout --live`; the marker key above is the rerun dedupe contract.\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/6072",
      "Number": 6072
    },
    {
      "ID": "issue-5232",
      "Split": "tune",
      "Title": "fix(next): admit tool-terminal wake before queue consumption",
      "Body": "## Parent context\n\nParent: #4992.\n\n## Problem\n\nThe agent loop consumes and acknowledges a ToolTerminalWake, appends its synthetic USER payload, and only then calls gateTurn. A terminal/non-proceed session verdict therefore drops the queued wake without rendering it and without a shared Next row. The paused path happens to wait and recover, but stopped/drained/budget-refused paths irreversibly consume before admission.\n\n## Done\n\n- The terminal wake remains queued until the next turn is admitted by gateTurn.\n- A terminal/non-proceed gate cannot acknowledge or drop the wake and emits no applied Next row.\n- An admitted turn consumes exactly one wake, renders its exact JSON payload, and emits exactly one correlated shared Next row.\n- A focused test proves refused-then-admitted behavior from the queue journal, rendered prompt, and independent Next readback.\n\n## Work estimate\nEstimate: 2 points. Uncertainty: operator-supplied estimate; revise through the parent denominator when evidence changes.\n\n## Overall completion contribution\nContribution: 2/20 points.\n\n## Completion standard\nproduction\n\n## Target operating envelope\nterminal wake outcomes: \u003e= 2 cases\n\n## Witnessed operating envelope\nterminal wake outcomes: \u003e= 2 cases\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5232",
      "Number": 5232
    },
    {
      "ID": "issue-5738",
      "Split": "tune",
      "Title": "research(runtime): evaluate microsoft/semantic-kernel against fak",
      "Body": "## Parent context\r\n\r\nPart of #2136.\r\n\r\n## Research source\r\n\r\n- GitHub repository: https://github.com/microsoft/semantic-kernel\r\n- Freshness observed: pushed `2026-08-06T09:54:50Z`; 28,427 stars at triage time.\r\n- Source summary: Integrate cutting-edge LLM technology quickly and easily into your apps\r\n\r\n## Why this matters\r\n\r\nThis active project overlaps fak's runtime boundary. We need measured compatibility and differentiated behavior, not a name-only landscape entry.\r\n\r\n## Proposed contract\r\n\r\nRun one equivalent multi-step tool task and compare lifecycle, interruption, handoff, memory, and receipt contracts.\r\n\r\n## Done when\r\n\r\n1. Pin the upstream revision or release and record license, supported transports/providers, and claimed operating envelope.\r\n2. Commit a bounded, rerunnable probe using fak's real public seam; do not rely on screenshots or prose-only comparison.\r\n3. Capture machine-readable results for one known-positive case and one adversarial, cancellation, isolation, or failure case.\r\n4. Produce a source-linked gap table that classifies each finding as adopt, interoperate, differentiate, or no-action.\r\n5. File implementation follow-ups for every adopted gap; if no action is warranted, record the evidence-backed reason.\r\n\r\n## Proof artifacts\r\n\r\n- Pinned upstream identity and source URL.\r\n- Committed probe/fixture plus raw machine-readable receipt.\r\n- Gap table with fak module owners and explicit disposition.\r\n\r\n## Dedupe marker\r\n\r\n`research-source:https://github.com/microsoft/semantic-kernel`\r\n\r\n## Work estimate\r\nEstimate: 1 point. Uncertainty: bounded research spike; implementation work is filed separately.\r\n\r\n## Overall completion contribution\r\nContribution: 1/100 points.\r\n\r\n## Completion standard\r\nresearch\r\n\r\n## Target operating envelope\r\n- repository scope: \u003e= 1 pinned upstream repository\r\n- witness cases: \u003e= 2 (known-positive plus adversarial/failure)\r\n\r\n## Witnessed operating envelope\r\n- repository scope: = 0 before this issue\r\n- witness cases: = 0 before this issue\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5738",
      "Number": 5738
    },
    {
      "ID": "issue-6075",
      "Split": "tune",
      "Title": "dogfood: run Architecture graph report on this repo's own live work",
      "Body": "\u003c!-- fak-issuefanout-key: fanout-archreport-dogfood-self-run --\u003e\n\n## Generation stream\n\ngen/now\n\n## Parent context\n\n#6068\n\n## Current state\n\nThe working spine of Architecture graph report exists (e0338749c41d); this follow-on has not started.\n\n## Why this is next\n\nSpine-first default: the spine just shipped, so its follow-on backlog is filed at creation time while context is hot.\n\n## Working spine\n\nArchitecture graph report is used for the real work of the repo that ships it, and the readout is committed evidence, not a claim.\n\n## In scope\n\nOne captured run of the spine (e0338749c41d) against the repo's real data; defects found get filed; readout committed under docs/notes/ or a ledger.\n\n## Out of scope\n\nFixing every defect the run surfaces (file them).\n\n## Done condition\n\nA committed readout of a real run exists and every defect it surfaced is filed with a marker key.\n\n## Witness\n\nThe committed readout path plus the filed issue numbers.\n\n## Acceptance gate\n\nReadout committed on trunk; issues visible on the tracker.\n\n## Work estimate\n\nEstimate: 3 points\n\n## Overall completion contribution\n\nContribution: 3/3 points\n\n## Completion standard\n\nproduction\n\n## Target operating envelope\n\n- declared leaves: \u003e= 1 leaves\n- report modes: \u003e= 2 modes\n\n## Witnessed operating envelope\n\n- declared leaves: \u003e= 1 leaves\n- report modes: \u003e= 2 modes\n\n## Closure binding\n\nClose via a commit whose body carries `Closes #\u003cn\u003e` and whose diff lands the witness above.\n\n## Lane\n\narchreport\n\n## Work unit\n\nleaf\n\n## Expected steps\n\n3\n\n## Assumptions\n\n- The spine at e0338749c41d still builds/runs on trunk.\n\n## Confusion risks\n\n- A synthetic fixture run is not dogfood; use the repo's live backlog/data.\n- Harden what exists; do not grow the spine's scope inside this follow-on.\n\n## Coordination\n\n- Stay inside internal/archreport/, cmd/fak/architecture.go; check the lane lease before dispatch.\n\n## Trigger\n\none-shot: filed at spine-ship time by fak issue fanout\n\n## Batch policy\n\none batch per spine, capped at --max candidates; marker-key (fanout-archreport-*) dedupe against existing issues before filing\n\n## Likely files\n\n- `internal/archreport/`\n- `cmd/fak/architecture.go`\n\nFiled by `fak issue fanout --live`; the marker key above is the rerun dedupe contract.\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/6075",
      "Number": 6075
    },
    {
      "ID": "issue-5158",
      "Split": "test",
      "Title": "feat(loop): structured mid-flight session verbs — interrupt / drop-pending-call / set-budget at the turn boundary (#2388)",
      "Body": "## Summary\n\nSplit from the write-half of #2403 (parent epic #2388). The **read half** —\ntyped loop-progress SSE (`turn_started` → `tool_started` → `call_adjudicated` →\n`result_admitted` → `turn_done`) emitted by the owned loop and projected into\nstructured native SSE — is delivered by **#5148**. This ticket tracks the\n**write half**: structured *mid-flight* session verbs that let an operator steer\na running `serve --native` loop, landing their effect at the next **clean turn\nboundary** with a tamper-evident journal witness.\n\nNow that the loop's state is legible (the read half), an operator can finally\nact on it. Today the native loop is drive-able only at the request boundary\n(run-state / budget / pace via the session table); there is no way to reach into\na turn that is already in flight.\n\n## Scope\n\nThree structured verbs, each landing at the next turn boundary (never mid-tool,\nnever mid-adjudication):\n\n- **`interrupt`** — stop the loop at the next boundary with a closed-token\n  witness, distinct from a hard cancel/terminate. Reuses the existing\n  terminate seam + `ArmMetrics.StoppedBySession`.\n- **`drop-pending-call`** — skip a specific queued tool call by `call_id` before\n  it is dispatched. This is the **net-new** piece: the loop has no per-call skip\n  consult today. *(design risk — flagged below.)*\n- **`set-budget`** — adjust the live token/turn budget the loop reads each turn\n  (the session table already carries budget; this is the mid-flight setter).\n\n## Acceptance\n\n`TestInterruptLandsAtTurnBoundary` (and per-verb siblings): an `interrupt`\nissued while turn *N* is dispatching tool calls does **not** cut a tool or an\nadjudication short — the loop completes turn *N*'s admitted results, then stops\nat the boundary carrying the closed witness token; the journal records the verb,\nits arrival, and the boundary it landed at. `drop-pending-call` skips exactly the\nnamed `call_id` and nothing else. Byte-for-byte invariant: with no verb issued,\nthe loop is the historical loop (the same guard the read half honors).\n\n## Reuse targets (verified against the tree)\n\n- **Boundary-drain seam** already lands operator input at the clean turn boundary:\n  `internal/agent/loop.go` (after `cfg.gateTurn`, ~L463–505). `interrupt` reuses\n  the terminate seam + the `ArmMetrics.StoppedBySession` closed token.\n- **Closed op/witness vocabulary already declared** in\n  `internal/sessionctl/vocab.go`: `OpPause/OpResume/OpCancel/OpTerminate/OpBudget`,\n  `OpSpec`, and `WitnessBoundaryStop`/`WitnessSafePointStop`. A new interrupt verb\n  registers an `OpSpec{Witness: WitnessBoundaryStop}` — no new vocabulary invented.\n- **Durable write-through** for a persisted verb: `applySessionControlDurable` →\n  `mirror.writeThrough` (`cmd/fak/session_durable.go`).\n- **Observer seam (read half, #5148)** gives the write half its acceptance\n  witness: an `interrupt` should surface as a `turn_done`-then-stop in the same\n  structured SSE stream, so the operator sees their verb take effect.\n\n## Design risk\n\n`drop-pending-call` is the only piece with no existing seam: the loop must\nconsult a per-`call_id` skip set *before* dispatching each queued `tc` in the\ntool-call loop. Needs a small, nil-safe skip-consult (same additive-RunOption\nshape as `WithProgressObserver`) so the historical loop is unchanged when unused.\n\n---\nPart of #2388 · write half of #2403 · sibling of read-half #5148\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5158",
      "Number": 5158
    },
    {
      "ID": "issue-5713",
      "Split": "test",
      "Title": "research(gateway): evaluate open-webui/open-webui against fak",
      "Body": "## Parent context\r\n\r\nPart of #2136.\r\n\r\n## Research source\r\n\r\n- GitHub repository: https://github.com/open-webui/open-webui\r\n- Freshness observed: pushed `2026-08-05T20:05:47Z`; 148,043 stars at triage time.\r\n- Source summary: User-friendly AI Interface (Supports Ollama, OpenAI API, ...)\r\n\r\n## Why this matters\r\n\r\nThis active project overlaps fak's gateway boundary. We need measured compatibility and differentiated behavior, not a name-only landscape entry.\r\n\r\n## Proposed contract\r\n\r\nMap its provider-routing and retry semantics to fak and probe one compatible and one adversarial request.\r\n\r\n## Done when\r\n\r\n1. Pin the upstream revision or release and record license, supported transports/providers, and claimed operating envelope.\r\n2. Commit a bounded, rerunnable probe using fak's real public seam; do not rely on screenshots or prose-only comparison.\r\n3. Capture machine-readable results for one known-positive case and one adversarial, cancellation, isolation, or failure case.\r\n4. Produce a source-linked gap table that classifies each finding as adopt, interoperate, differentiate, or no-action.\r\n5. File implementation follow-ups for every adopted gap; if no action is warranted, record the evidence-backed reason.\r\n\r\n## Proof artifacts\r\n\r\n- Pinned upstream identity and source URL.\r\n- Committed probe/fixture plus raw machine-readable receipt.\r\n- Gap table with fak module owners and explicit disposition.\r\n\r\n## Dedupe marker\r\n\r\n`research-source:https://github.com/open-webui/open-webui`\r\n\r\n## Work estimate\r\nEstimate: 1 point. Uncertainty: bounded research spike; implementation work is filed separately.\r\n\r\n## Overall completion contribution\r\nContribution: 1/100 points.\r\n\r\n## Completion standard\r\nresearch\r\n\r\n## Target operating envelope\r\n- repository scope: \u003e= 1 pinned upstream repository\r\n- witness cases: \u003e= 2 (known-positive plus adversarial/failure)\r\n\r\n## Witnessed operating envelope\r\n- repository scope: = 0 before this issue\r\n- witness cases: = 0 before this issue\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5713",
      "Number": 5713
    },
    {
      "ID": "issue-5148",
      "Split": "test",
      "Title": "feat(gateway): typed loop-progress observer on the owned loop → structured native SSE events (#2388)",
      "Body": "Part of #2388 (harness-native program #2387). The **net-new** half of #2403.\n\n## Thesis\nThe native owned-loop stream (`runNativeArmStream`, `internal/gateway/native_serve.go`)\nhand-builds **text-only** Anthropic SSE. Everything the owned loop does between\n`message_start` and `message_stop` — turn boundaries, per-call adjudication verdicts,\ntool dispatch, result admission/taint — is invisible until the terminal `ArmMetrics`\nwitness on `message_stop`. An operator or client cannot watch *what the loop is doing*\nmid-flight; it can only read the prose and the final counters.\n\nThe interrupt/command half of #2403 is already substantially built (the session control\nplane — `POST /v1/fak/session/{id}/{verb}` run/budget/pace/priority/wall/throughput\nverbs, plus the steer route with `class:\"now\"` boundary-interrupt semantics and the\n`#2758` terminate signal). What is genuinely missing is the **outbound** half: the stream\ncarrying the loop's *typed state*, not just its prose. This issue ships that spine.\n\n## Distilled inspiration (never copied)\nThe reference harness's stream carries provenance-tagged loop-state events (each tied to\nits originating call), not only text. fak's version additionally carries **kernel\nverdicts** — the adjudication reason classes a client can gate on — drawn from the same\nclosed vocabulary the rest of the kernel refuses with. Every emitted event is a witnessed\nfact about a real lifecycle transition, never narration.\n\n## Design\n1. **`internal/agent`** — a typed, nil-safe loop-progress observer (a pure additive\n   `RunOption`, in the family of `WithSessionGate` / `WithSpeculator`):\n   - `ProgressEvent{Kind, Turn, CallID, Tool, Verdict, Reason, Taint}` with a\n     `ProgressEventKind` closed set: `turn_started`, `call_adjudicated`, `tool_started`,\n     `result_admitted`, `turn_done`.\n   - `WithProgressObserver(func(ProgressEvent)) RunOption` sets `runConfig.observer`.\n   - `runArm` calls a nil-safe `cfg.emitProgress(...)` at each lifecycle point. When no\n     observer is wired the emit is a no-op, so the historical loop stays **byte-for-byte\n     unchanged** (the same guarantee every other `RunOption` carries).\n2. **`internal/gateway`** — `runNativeArmStream` wires `WithProgressObserver` to emit a\n   typed **custom SSE event** per `ProgressEvent` (`send(kind, payload)`), interleaved\n   with the existing text deltas, each tagged with the session trace and originating\n   call id. The observer runs on the same request goroutine as the text sink (the loop is\n   synchronous within `CompleteStream`), so no cross-goroutine send race is introduced.\n\n## Acceptance (witnessable, on-box)\nGolden SSE test `TestNativeStreamStructuredEvents` in `internal/gateway`: a multi-turn\n`serve --native` stream in which one tool call is **denied** and one is **executed**\nemits the full typed event sequence **in order** — `turn_started` →\n(`tool_started` → `call_adjudicated{verdict}` → `result_admitted{taint}`)+ → `turn_done`\nper turn — interleaved with the text deltas, terminating on `message_stop` carrying the\n`fak.native_arm` `ArmMetrics`. Runs under a deterministic mock streaming planner (no model\nserving), witnessed via WSL `go test` against the committed tip.\n\n## Out of scope (tracked follow-on)\n- The **inbound** command half of #2403 (`TestInterruptLandsAtTurnBoundary`) — the session\n  control plane already carries the mechanism; a boundary-lands witness test is a sibling\n  follow-on, filed separately.\n\n## Seams\n- `internal/agent/loop.go` (runArm lifecycle points), `internal/agent/loop_session.go`\n  (`runConfig`, `RunOption`)\n- `internal/gateway/native_serve.go` (`runNativeArmStream` SSE synthesis)\n\n## Parent context\nPart of #2388 (harness-native program #2387).\n\n## Current state\n`runNativeArmStream` emits text-only SSE; loop lifecycle state is invisible until the\nterminal ArmMetrics on `message_stop`.\n\n## Why now\nThis is the outbound half of loop ownership — streaming that carries the loop's state, the\nprecondition for any client that watches, gates on, or reacts to a running native loop.\n\n## Expected steps\n1. Add the typed observer + `WithProgressObserver` `RunOption` in `internal/agent`.\n2. Emit at the runArm lifecycle points, nil-safe.\n3. Wire the observer to typed SSE events in `runNativeArmStream`.\n4. Add and green the golden test `TestNativeStreamStructuredEvents`.\n\n## Batch policy\nSingle coherent slice, landed once — no spam of per-event commits.\n\n## Done when\n`TestNativeStreamStructuredEvents` is green on the committed tip and the native stream\nemits the typed event sequence with the historical (unwired) loop byte-for-byte unchanged.\n\n## Work estimate\nEstimate: 3 points. Uncertainty: operator-supplied estimate; revise through the parent denominator when evidence changes.\n\n## Overall completion contribution\nContribution: 3/3 points.\n\n## Completion standard\nproduction\n\n## Target operating envelope\ntests: \u003e= 1 golden-test\n\n## Witnessed operating envelope\ntests: \u003e= 1 golden-test\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5148",
      "Number": 5148
    },
    {
      "ID": "issue-5207",
      "Split": "test",
      "Title": "feat(guard): P0 PreCompact async mini-lookahead",
      "Body": "In `runGuardPreCompact` on an allow decision, gate via `AdmitSpeculation` then spawn a detached fire-and-forget rollout (fork + --max-turns 3 + deny floor) and exit 0 immediately — never delay the hook. Pickup at `runGuardSessionStart` `source=compact`: inject a fresh same-base-SHA lesson as additionalContext beside the compacted summary. Fail-open both sides. Cheapest dogfood (P0). Leaf: guard. Child of #5202; see docs/notes/CONCEPT-LOOKAHEAD-RESET-2026-07-17.md.",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5207",
      "Number": 5207
    },
    {
      "ID": "issue-5859",
      "Split": "test",
      "Title": "A dead worker's lane lease blocks its lane forever: acquire never applies the kernel's own dead-lease filter, and no reap verb is exposed",
      "Body": "## Symptom\n\nThe `tools` lane was held by worker `w4591` from **2026-07-18** until tonight (**2026-08-06**) — 19 days. Every peer that asked for the lane was refused:\n\n```\n$ dos lease-lane acquire --lane tools --owner \u003cpeer\u003e\nreason = lane 'tools' is already held by a live loop - pick a different --lane or wait.\n```\n\nLane-journal seq 2294 shows the same refusal issued to an unrelated peer (`harvest-recovery`), so this was chronic, not a race. `w4591` had been dead the whole time.\n\n## The holder was provably dead, and the kernel can already prove it\n\n```\n$ python -c \"from dos import proc_delta; print(proc_delta.probe(80596, host_id='DESKTOP-BB3FMHP', this_host='DESKTOP-BB3FMHP', recorded_starttime=134288779379632999))\"\nProcLiveness(alive=False, detail='pid 80596 is gone (win32 INVALID_PARAMETER)')\n```\n\nSame host, so the local pid+`proc_starttime` probe is authoritative (no cross-host TTL overlay needed). Both arms of the kernel's own `_lease_is_dead` predicate (`lane_lease.py:224`) were satisfied: heartbeat-stale far past TTL **and** confirmed-dead holder pid.\n\n## Root cause\n\n`lane_lease.live_leases(config, *, expire_dead: bool = False)` — the dead-lease filter is **opt-in and defaults OFF**. `acquire()` (`lane_lease.py:389`) deliberately passes the structural fold:\n\n```python\nlive = live_leases(config) + extra   # expire_dead=False, by design\n```\n\nThe in-code rationale is sound for its own case: `expire_dead=True` would probe the pid of a *fresh* lease whose short-lived journaling subprocess has already exited, report `alive=False`, and let a racing acquirer double-book (the TOCTOU the lease exists to prevent; it previously regressed `test_coord_demo_k4_serializes_writes`).\n\nBut the consequence is that **the authority path never self-heals**. A lease whose holder died has no expiry on the only call that grants lanes. Only two callers pass `expire_dead=True` (`cli.py:6330`, `cli.py:8377`) and neither grants a lane.\n\n## No reap verb is exposed\n\n`dos lease-lane` offers exactly `{acquire, release, heartbeat, spawn, live}`. The two functions that *are* meant to clear an orphan have **no CLI surface at all**:\n\n- `lane_lease.adopt()` (`lane_lease.py:559`) — no `add_parser(\"adopt\")`, no caller in `cli.py`.\n- `SCAVENGE` — recognised as a journal op (`cli.py:8590` `_QUEUE_DRAIN_OPS`) but nothing emits it from the CLI.\n\n`live_leases`' own docstring says the dead orphan is kept visible \"precisely so [`adopt()` and the orphan-reclaim sweep] can transfer/scavenge it\" — but neither is reachable by an operator.\n\n## The workaround I used (undocumented, and too blunt)\n\n`release()` matches on `holder == owner`, so naming the dead holder works:\n\n```\n$ dos lease-lane release --lane tools --owner w4591\n{\"lane\": \"tools\", \"owner\": \"w4591\", \"released\": true}\n```\n\nThis is append-only and auditable, but it is indistinguishable from a legitimate self-release: it asserts no liveness check, so it will just as happily evict a **live** holder. It is a footgun, not a reap.\n\n## Why this is worth fixing\n\nThis is the same defect class as the goal-park bug fixed tonight: **state that outlives the condition that created it, with no sanctioned way to clear it.** A single crashed worker permanently removes a lane from the fleet, and the blast radius grows monotonically — `dos lease-lane live` currently shows leases still held from 2026-07-16 by `fable-superloop-23874`, `ultracode-5025`, `goal-4876`, and others, all with the same shape.\n\n## Suggested fix\n\nExpose a reap verb that applies the liveness predicate the kernel already has, rather than trusting the caller:\n\n```\ndos lease-lane reap --lane \u003cname\u003e [--owner \u003cholder\u003e]\n```\n\nrefusing unless `_lease_is_dead(lease, now, this_host)` holds, and journaling a `SCAVENGE` (already in `_QUEUE_DRAIN_OPS`). That keeps `acquire()`'s TOCTOU-safe structural fold untouched — the self-heal becomes an explicit, evidence-gated operator action instead of an implicit read-time elision.\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5859",
      "Number": 5859
    },
    {
      "ID": "issue-5636",
      "Split": "test",
      "Title": "feat(modelroute): produce fak.modelroute.serving.v1 from a live gateway so the placement ladder's fleet rung stops being a hand-written file",
      "Body": "_Parent epic: #5632 — the gateway as the fleet's front door._\n\n## Current state\n\n`internal/modelroute` has a serving-liveness record and nothing in fak produces it.\n`internal/modelroute/serving.go:76` declares:\n\n```go\nconst ServingReportSchema = \"fak.modelroute.serving.v1\"\n```\n\nGrepping the whole tree for that string returns **only** `_test.go` fixtures\n(`cmd/fak/route_place_serving_test.go`, eight occurrences) and one doc sample\n(`docs/model-accounts.md:516`). The single ingestion point is `loadServingReport`\n(`cmd/fak/route_place_serving.go:121-137`), which reads a path an operator passes with\n`--serving \u003cpath\u003e`.\n\nSo the **fleet rung of the placement ladder is gated by a hand-written JSON file.**\n`modelroute.PlaceWithServing` walks device → fleet → vendor with a closed reason vocabulary\n(`internal/modelroute/placement.go`, `zone.go`), and whether a fleet zone is admissible depends\nentirely on a file a human remembered to refresh. Meanwhile a real prober sits one package away:\n`internal/gateway/fleet_membership.go` runs a health loop with hysteresis and drain and exposes\n`Snapshot() []WorkerStatus` (`:261`). The two never meet — `Place`/`PlaceWithServing` has **zero\ncallers in `internal/gateway`** (only `cmd/fak/dispatch_rung_escalate.go:392`,\n`cmd/fak/dispatch_rung_resolve.go:310`, `cmd/fak/route_place.go:172`).\n\n`internal/modelroute/serving.go` is explicit that it does not intend to probe — *\"Probing is\nsomebody else's job, above this tier.\"* This issue is that job.\n\n## Why this is next\n\nA placement decision that consults a stale operator file is worse than one that consults nothing:\nit reports a **zone** verdict with the ladder's closed reason vocabulary, which reads as measured\nwhen it is asserted. The producer is the smallest change that turns the ladder's fleet rung from a\nconvention into an observation, and every downstream multi-model decision (which zone, which\nworker, which account) inherits its honesty.\n\n## Working spine\n\nA serve process that maintains a `FleetMembership` emits a `fak.modelroute.serving.v1` document\nfrom its own `Snapshot()` — one entry per worker with its observed state and observation timestamp\n— to a well-known path under the fleet state dir. `loadServingReport` gains a\nfreshness/staleness rule so a report nobody refreshed degrades to *unknown*, not to *healthy*.\n`--serving \u003cpath\u003e` keeps working unchanged as the operator override.\n\n## In scope\n\n- A producer that folds `FleetMembership.Snapshot()` into `modelroute.ServingReport` and writes it\n  atomically (temp-then-rename, per the `fleetbus.Announce` precedent at\n  `internal/fleetbus/dirbus.go:142-176`).\n- A staleness rule on the consumer side: an entry older than a stated TTL is `unknown`, and the\n  ladder's reason vocabulary gains (or reuses) the token for \"fleet liveness not observed\".\n- The default location, derived from the existing fleet state dir rather than a new flag.\n\n## Out of scope\n\n- Cross-host aggregation of reports from several machines. A file per instance is the v1; folding\n  them is a follow-on.\n- Calling `PlaceWithServing` from `internal/gateway` — the *consumer* join is separate work; this\n  issue lands the **producer** so that join has something true to read.\n- Latency/throughput/goodput signals. This is **liveness**, not performance. (fak does measure\n  TTFT and goodput in `internal/gateway` — `debug.go:92 TTFTTurns`, `MeanTTFTSeconds`,\n  `fak_serving_goodput_requests_per_second`; feeding *those* into placement is a different issue\n  and is not claimed here.)\n\n## Done condition\n\nWith a `FleetMembership` probing two endpoints, one up and one down, a `fak.modelroute.serving.v1`\ndocument appears at the default path whose entries match the observed states; `fak route place`\nwithout `--serving` consumes it and reaches the same verdict it reaches today when handed an\nequivalent file by hand; a report older than the TTL yields *unknown* rather than a stale positive.\n\n## Witness\n\n`go test ./internal/modelroute -run 'TestServingReportStaleness'` and\n`go test ./cmd/fak -run 'TestRoutePlaceReadsProducedServingReport'` — the second builds a\nmembership over two httptest endpoints, produces the report, and places against it with no\n`--serving` flag.\n\n## Acceptance gate\n\nBoth tests pass; `go test ./internal/modelroute ./cmd/fak` green; passing `--serving \u003cpath\u003e`\nexplicitly still takes precedence over the produced file (asserted); no placement verdict changes\nfor an operator who has not opted in.\n\n## Dependencies\n\n#4013 — nothing constructs `FleetMembership` in a live process today, so the producer has no\nsource until that lands. The staleness rule and the schema fold can be built and tested against an\ninjected snapshot before then.\n\n## Confusion risks\n\n- **A produced report must not be more trusted than a hand-written one.** Both are claims with a\n  timestamp; the TTL applies to both.\n- **Do not let \"no report\" mean \"fleet is down\".** The ladder already has a closed reason\n  vocabulary — pick the token that means *unobserved*, and if none fits, add one rather than\n  overloading a health token.\n- **The gateway must not import a placement decision to write this file.** Producing a report is\n  an *emission*; consuming it is the other direction and is deliberately not in scope, so the two\n  packages stay acyclic.\n\n## Likely files\n\n`internal/modelroute/serving.go`, `internal/modelroute/placement.go`,\n`internal/gateway/fleet_membership.go`, `cmd/fak/route_place_serving.go`,\n`cmd/fak/serve.go`, `docs/model-accounts.md`\n\n```routing\nlane: modelroute\npaths: internal/modelroute/**, cmd/fak/route_place_serving.go\nexpected_steps: fold Snapshot into ServingReport; atomic write to the default path; TTL/staleness on the consumer; tests\n```\n\n## Parent context\n\n**#5632** — the gateway-as-fleet-front-door epic. This leaf is one of its eleven children and is dispatchable on its own.\n\n## Work unit\n\nleaf\n\n## Work scale\n\nS1\n\n## Expected steps\n\n6\n\n## Trigger\n\nFiled from the #5632 research pass (2026-08-05), which measured this gap first-hand at HEAD; the grounding lines in **Current state** are the evidence.\n\n## Batch policy\n\nOne leaf per gap, one gap per lane. No further issues are expected from this trigger; a follow-on rung, if any, is named in **Out of scope**.\n\n## Closure binding\n\nCloses on a commit stamped `(fak modelroute)` producing the report plus the consumer TTL, with `TestServingReportStaleness` and `TestRoutePlaceReadsProducedServingReport` green.\n\n- **Required model tier:** T2\n- **Optimal model tier:** T2\n\n### Related\n\n#4013, #595, #5416, #2236, #4231\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5636",
      "Number": 5636
    },
    {
      "ID": "issue-5347",
      "Split": "test",
      "Title": "fix(fleet): add age-based retention to the worktree-doctor sweep archive (4.6GB leak) (fak fleet)",
      "Body": "## Parent context\n\nChild of the create-without-collect leak census #5345, under retention/GC epic\n#3873. This is row 1 of that census — the LARGEST leak by bytes, and a pointed\nirony: the collector for one leak (stray worktrees) is itself the creator of this\none. Bare parent: #5345.\n\n## Problem\n\n`tools/worktree_doctor.py` `do_sweep` (around lines 580-596) archives every dirty\nswept worktree to `%LOCALAPPDATA%/Fleet/watchdog/worktree-archive/\u003cdate\u003e/` (via\n`archive_worktree` / `_default_archive_dir`, around lines 609-615) BEFORE force-\nremoving it. There is no reaper — nothing anywhere in the repo ever deletes an\narchive day. Evidence: `worktree-archive/2026-07-14` alone holds 16 full worktree\ncopies = 4.6GB (one is 1.3GB — full build artifacts archived wholesale). This is\nthe single largest create-without-collect leak by bytes on the live tree.\n\n## Fix direction\n\nAdd age-based retention to the archive directory: after a sweep writes a new\narchive day (or at the start of `do_sweep`), prune archive-day directories older\nthan a bounded window. Pick a conservative default (e.g. 7 days — long enough to\nrecover a mistakenly-swept worktree, short enough to bound 4.6GB/2-week growth);\nmake it a named constant / overridable via the same config channel the doctor\nalready uses for its other knobs.\n\nSafety: an archive day is a post-mortem copy of an ALREADY-swept (dirty) worktree\n— it is a recovery convenience, not live state. Deleting an OLD archive day past\nthe window is safe; deleting a RECENT one is not (someone may still want to\nrecover a same-week accidental sweep). So age, with a generous floor, is the\ncorrect signal here (unlike the wip-ref case where recoverability, not age,\ndecides). Never delete the archive day created by the current sweep run.\n\n## Likely files\n\n- tools/worktree_doctor.py (add archive-day retention in/around `do_sweep` / `archive_worktree`)\n\n## Expected steps\n\n1. Read `do_sweep`, `archive_worktree`, `_default_archive_dir` and confirm the\n   `worktree-archive/\u003cdate\u003e/` layout and how the date-day directories are named.\n2. Add an age-based prune of archive-day directories older than the window\n   (default 7 days), guarded so the current run's archive day is never removed.\n3. Add a test: seed several `worktree-archive/\u003cdate\u003e/` day dirs with dummy\n   content, some older than the window and some fresh, run the prune, assert only\n   the stale days are removed and fresh ones (incl. today's) survive. Mirror the\n   existing tools test style.\n4. Re-measure the live archive tree after a doctor run and confirm total archive\n   bytes stop climbing past the window.\n\n## Batch policy\n\nOne retention prune for one leak. Do not fan out per-archive-day or per-worktree\ntickets; the prune sweeps all stale days in one pass. Do not touch other census\nrows in this change.\n\n## Work estimate\n\n2 points. An age-based directory prune plus a temp-dir retention test, in an\nisolated Python tool file with no cross-package coupling.\n\n## Overall completion contribution\n\n2/20 points of the retention/GC epic #3873 (via census #5345). Closes the\nlargest-by-bytes row of the 12-site survey (4.6GB reclaimable).\n\n## Target operating envelope\n\n- retention tests: \u003e= 1 case\n- stale archive days pruned while fresh ones and today survive: \u003e= 1 case\n\n## Witnessed operating envelope\n\n- retention tests: \u003e= 1 case\n- stale archive days pruned while fresh ones and today survive: \u003e= 1 case\n\n## Completion standard\nproduction\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5347",
      "Number": 5347
    },
    {
      "ID": "issue-5802",
      "Split": "test",
      "Title": "feat(modelaccept): grade tool-call WIDTH per turn, not just MinToolCalls per task",
      "Body": "## Summary\n\n`internal/modelaccept` can already require a **total** number of tool calls for a task\n(`MinToolCalls`, `internal/modelaccept/modelaccept.go:28`, gated at `:162` and validated at\n`:308`). It cannot express or grade **width** — how many of those calls the model issued in\na single turn. That makes the acceptance corpus blind to the one tool-use axis the\nliterature says separates models on long-horizon work, and it grades the axis that is\nconfounded with retries.\n\n## The gap, concretely\n\n`examples/model-acceptance-agentic-v1.json` has tasks like:\n\n- `multi-step-tool` — \"Use the provided read-only lookup tool at least twice\", `min_tool_calls: 2`\n- `retry-recovery` — \"Call the provided flaky read-only tool ... retry once\", `min_tool_calls: 2`\n\nBoth are satisfied identically by a model that issues two calls in two serialized turns and\nby a model that issues two independent calls in one turn. Those are very different\ncapabilities. Worse, `retry-recovery` reaching `min_tool_calls: 2` is *by construction* a\nretry — so the same field is being used to encode \"the model batched\" and \"the model\nfailed once and recovered\", which are opposite signals.\n\n`examples/model-acceptance-prospective-v3-report.json` makes the conflation explicit — its\n`production-multi-record` task asks for both records \"in **separate** tool calls\" while\nstill grading only the count.\n\n## Why width belongs in acceptance\n\n- [Wide \u0026 Deep](https://arxiv.org/html/2602.07359v1) scales width (parallel calls per\n  reasoning step) against depth and finds width raises accuracy *and* lowers turn count —\n  62.2% with GPT-5-Medium on BrowseComp vs the 54.9% originally reported for GPT-5-High.\n  Width is a capability, and it is the capability that pays.\n- Provider support is genuinely uneven, so it is a real acceptance question rather than a\n  formality: some model families reject `parallel_tool_calls` outright while their\n  successors support it, meaning \"can this model batch at all\" is a routing-relevant fact\n  fak should know before it pins a model to a lane.\n- Volume alone misleads. A 1,440-run MCP benchmark found retry rate inflating cost 1.4-2.8x;\n  models at 75% first-attempt pass reach 96-98% only with up to 3 retries. A corpus that\n  rewards call count rewards the failing model.\n\n## Proposal\n\nAdd a width axis to the task schema and the fold, kept strictly separate from the existing\nvolume field:\n\n- `min_parallel_tool_calls` (or a `max_tool_turns` sibling) — a task can require that N\n  independent calls arrive in ONE assistant turn, not merely N calls overall.\n- Record per-run `tool_turns` alongside the existing `ToolCalls`, so the fold can derive\n  calls-per-turn using the **same denominator convention** as\n  `internal/agent/turnbatch.go` rather than inventing a third definition of the same ratio.\n- Report width per model in the acceptance report as a first-class dimension, so\n  \"better models batch more\" becomes a graded, reproducible claim about *this* corpus\n  instead of an inherited assumption.\n- Add at least one genuinely parallel-friendly task: two independent lookups with **no**\n  data dependency, where a one-turn solution is strictly better and the corpus can tell.\n\nKeep `MinToolCalls` as-is for the retry/recovery tasks where total count is the right\nquestion. The point is to stop one field carrying two meanings.\n\n## Acceptance criteria\n\n- A task can declare a per-turn width requirement, validated the way `MinToolCalls` is\n  (`:308` rejects a width requirement without `tool_required`).\n- The report carries a per-model width figure using `turnbatch`'s denominator convention.\n- A corpus task exists whose independent-calls-in-one-turn solution is distinguishable from\n  the serialized solution, and the fold distinguishes them (table test with both run shapes).\n- Existing corpora keep their current verdicts (the new field is additive and optional).\n\n## Related\n\nPart of the tool-call-width epic. Shares the width definition with the orphan-KPI child —\nthose two should agree on the denominator, or the fleet meter and the acceptance grade will\ndisagree about the same model.\n\nParent: #5796\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5802",
      "Number": 5802
    },
    {
      "ID": "issue-5929",
      "Split": "test",
      "Title": "region admission: a sub-lane passed to --lane resolves to no tree and collides with everything (regionadmit never adopted laneadmit's hierarchy)",
      "Body": "## Summary\n\n`internal/laneadmit` now understands hierarchical lane names (`gateway/server`,\n`gateway/server.go`), landed in d41f5605a8. `internal/regionadmit` — the *other* pure\nadmission twin, and the one every real surface actually calls — does not. The two twins\nhave diverged, and on the surface a user touches the divergence is not neutral: passing a\nsub-lane to `--lane` today is **strictly worse** than passing its declared root.\n\n## Reproduced from the code\n\n`fak loop region` (`cmd/fak/loop_region.go:83,93-94`), `fak loop drive`, and the dispatch\ntick all go through `regionadmit.Decide` / `regionadmit.ResolveTree`.\n\n**1. A sub-lane resolves to NO tree, so it collides with everything.**\n\n`regionadmit.ResolveTree` (`internal/regionadmit/regionadmit.go:94-102`) resolves a lane by\nexact map lookup:\n\n```go\nif req.Lane != \"\" {\n    return cleanGlobs(tax.Trees[req.Lane])   // :99\n}\n```\n\n`tax.Trees` is keyed by declared lane name only, so `--lane gateway/server` misses and\nreturns `nil`. Rung 4 (`:287`, `dispatchorder.TreesOverlap`) reads an empty tree as unknown\nblast radius and collides **conservatively against every live lease**. So the narrower name\na worker types to get *more* concurrency gets it *less*: `--lane gateway` is admitted\nagainst a disjoint peer, `--lane gateway/server` is refused by any peer at all.\n\n`laneadmit.Taxonomy.TreeFor` (`internal/laneadmit/lanetree.go:193`) is the fixed version —\nit walks to the nearest declared ancestor and derives `internal/gateway/server/**`.\n\n**2. Exclusivity does not inherit downward.**\n\nRung 1 (`internal/regionadmit/regionadmit.go:251`) is a direct lookup:\n\n```go\nif tax.Exclusive[req.Lane] \u0026\u0026 len(others) \u003e 0 {\n```\n\n`tax.Exclusive[\"abi/registry.go\"]` is false, so a sub-lane of an exclusive root does not\ntrip the runs-alone gate at rung 1. In practice defect 1 masks this (the empty tree refuses\nat rung 4 anyway, and with nothing live an exclusive request admits regardless), so this is\na latent hole rather than a live escape — but it becomes a real one the moment defect 1 is\nfixed without fixing this. `laneadmit.Taxonomy.IsExclusive`\n(`internal/laneadmit/lanetree.go:169`) walks ancestors for exactly this reason.\n\n**3. Same-lane comparison is string equality.**\n\n`:268` `if req.Lane != \"\" \u0026\u0026 lane == req.Lane` never fires for a parent/child pair, so\n`gateway` and `gateway/server` would not serialize against each other even though the\nparent may edit anywhere beneath the child. `laneadmit` reports this as its own rung,\n`ConflictLaneAncestry` (`internal/laneadmit/laneadmit.go:36,158`).\n\nNote the two twins also carry **different `Taxonomy` types** — `regionadmit.Taxonomy` has no\n`Loaded` field and its own `LoadTaxonomy` (`:317`), so this is not a one-line import.\n\n## What would fix it\n\nEither port the four helpers (`CanonicalLane`, `LaneContains`, `TreeFor`, `IsExclusive`) into\n`regionadmit`, or — better, and the reason the algebra was kept pure and dependency-light —\nhave `regionadmit` build a `laneadmit.Taxonomy` and delegate the lane-name dimension to it,\nleaving `regionadmit`'s narrowed-pair co-admit rung (`strictlyWithinLane`, `:149`) as the\ngeometry half. Prefer the second: two independent implementations of one contract is how\nthis drifted in the first place.\n\nThis changes the **live** admission path that `fak dispatch tick` and `fak loop drive`\nalready run, so it wants its own change, not a drive-by.\n\n## Acceptance bar\n\n- `--lane gateway/server` resolves to `internal/gateway/server/**`, not `nil`.\n- `--lane abi/registry.go` trips `exclusive_lane_requested`.\n- `gateway` vs `gateway/server` serialize; `gateway/server` vs `gateway/router` co-run.\n- Every existing `internal/regionadmit` test passes unchanged — all 543 declared lanes are a\n  single `[a-z0-9]+` segment, so flat verdicts must stay byte-identical (this is the property\n  `laneadmit`'s `TestDecideFlatVerdictsUnchanged` pins on its side).\n\n## Not this issue\n\nThis does **not** by itself raise fleet concurrency. No surface picks a sub-lane on its own —\n`fak dispatch wave` still routes an issue to a declared leaf — and #5854 is the blocker for\nthat (the lease record conflates admission geometry with authorization geometry, so narrowing\nonly the pricer yields phantom concurrency). This issue is the prerequisite: the decision has\nto understand a sub-lane before anything can usefully hand it one.\n\nDocs state the gap today: `docs/region-admission.md` (`Sub-lanes` + `Honest boundary`).\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5929",
      "Number": 5929
    },
    {
      "ID": "issue-5672",
      "Split": "test",
      "Title": "test(policy): enforce implication contracts between layered validators",
      "Body": "## Parent context\n\nPart of #4509.\n\n## Problem\n\nIf one validator is described as a stricter form of another, every input accepted by the stricter validator must also satisfy the prerequisite validator. Testing each validator independently does not prove that relationship.\n\nFak has layered advisory, admission, verification, and blocking policies. Parallel implementations can accidentally let a “strict” path accept records rejected by its base trust gate.\n\n## Proposed contract\n\nMake this distinction explicit at the project boundary, preserve the inputs needed to audit it, and fail closed when the distinction cannot be established.\n\n## Done when\n\n1. - Declare prerequisite relationships between validator levels.\n2. - Implement stricter validators by composing or reusing prerequisite validation.\n3. - Add a relational property test: acceptance by a stricter level implies acceptance by every prerequisite.\n4. - Exercise valid, absent, unstated, unknown, malformed, and internally inconsistent inputs.\n5. - Include a planted inversion that passes both validators’ isolated tests but fails the implication test.\n6. - Diagnostics identify the prerequisite layer that rejected the input.\n7. - Test output states the evaluated domain size.\n\n## Proof artifacts\n\n- A committed machine-readable receipt demonstrates the accepted case and the refusal case.\n- A committed known-positive fixture proves the signal can succeed.\n- A committed adversarial or boundary fixture proves the contract cannot pass vacuously.\n- Focused tests exercise the public result shape and every typed refusal used by the contract.\n\n## Provenance\n\nIdentified through private workspace review.\n\n## Work estimate\nEstimate: 3 points. Uncertainty: operator-supplied estimate; revise through the parent denominator when evidence changes.\n\n## Overall completion contribution\nContribution: 3/100 points.\n\n## Completion standard\nproduction\n\n## Target operating envelope\n- repository scope: \u003e= 1 repository\n\n## Witnessed operating envelope\n- repository scope: = 1 repository\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5672",
      "Number": 5672
    },
    {
      "ID": "issue-5154",
      "Split": "test",
      "Title": "feat(model): native DeepSeek V4 MTP (NextN depth-1) draft head — gated on measured external-engine acceptance",
      "Body": "Parent: #3006. Follow-on from #3020 (the MTP/speculative-decoding evaluation gate).\n\n## Why now\n#3020 landed the evaluation gate: the deepseekbench scorecard now carries a `speculative = off | mtp | dspark` axis and an `accepted_token_ratio` field, and `CompareSpeedup` refuses to credit a speculative speedup without verified quality parity. That gate is the precondition this native ticket must pass before it can claim a win. No native draft head lands until an external-engine mtp-vs-off run produces a real target.\n\n## Engine metric (named, per #3020 done-condition c)\nAccepted-token ratio == mean accepted draft length per verify pass.\n- In-kernel closure: `internal/polymodel/specdecode.go` `SpecDecodeRun.MeanAcceptanceLength`.\n- Scorecard field: `accepted_token_ratio` on the deepseekbench `Row` (added by #3020).\n\n## Target acceptance threshold (named, per #3020 done-condition c)\nMean acceptance length \u003e= X at `quality_parity = verified`, where X is taken from the measured external mtp-vs-off scorecard rows. Interim floor 1.5 (drafting must buy \u003e= 50% extra accepted tokens per verify pass) — replace with the measured X once an engine that exposes the acceptance counter is chosen.\n\n## In scope\n- Native DeepSeek-V4 MTP (NextN depth-1) draft head on the pure-fak decode path.\n- Wire draft-accept through `internal/modelroute/specbridge.go` (`SpecAccept`/`AcceptFunc`).\n- Emit `accepted_token_ratio` into the deepseekbench row from the live acceptance counter.\n\n## Out of scope\n- Any speedup claim not paired with verified quality parity (the #3020 gate refuses it).\n- DSpark-specific engine internals (separate lever).\n\n## Substrate already shipped\n- `polymodel.SpecDecode` live loop (#4877, closed).\n- Loader NextN/MTP skip: `internal/ggufload` `num_nextn_predict_layers` parse.\n- Eval plan: docs/notes/DEEPSEEK-V4-MTP-SPECULATIVE-EVAL-2026-07-08.md.\nRelated: #23 (generic native verify-accept), #3197 (EAGLE-3).\n\n## Done condition\n- [ ] Native V4 MTP depth-1 draft head produces drafts on the pure-fak decode path.\n- [ ] Live acceptance counter feeds `accepted_token_ratio`; a speedup row only prints when mean acceptance length \u003e= the named threshold AND quality_parity=verified.\n- [ ] One issue-bound commit with a fail-before/pass-after test and DOS witness.",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5154",
      "Number": 5154
    },
    {
      "ID": "issue-5245",
      "Split": "test",
      "Title": "feat(bench): coalescebench — net-tok/s(B) curve + coalescing ratio for SSD-offloaded MoE",
      "Body": "Epic: #5243. Design: `docs/notes/MOE-SSD-MULTI-AGENT-NET-TOKS-2026-07-18.md` §2. Depends on: A (`SimulateExpertCacheBatch`).\n\n## What\n\nA deterministic replay bench (new `cmd/coalescebench`, mirroring `cmd/batchbench`) that drives\nthe cross-agent coalescing simulator over synthetic (and later real, #H) GLM-5.2-shaped routing\ntraces at **B = 1, 4, 16, 64, 128** and emits the **net-tok/s(B) curve** plus the coalescing\nratio `C(B) = (B·K)/U(B)`, applying the §2 roofline:\n\n```\nt_agent(B) = max( SSD_term(B), RAM_term, FLOP_term )\nnet_toks(B) = B / t_agent(B)\nSSD_term(B) = (DistinctStreamed_per_step · expert_bytes) / (B · BW_ssd)\n```\n\nBench inputs (`BW_ssd`, `BW_ram`, `expert_bytes`, `NR`, K, N, L) are **flags with GLM-5.2\nheader-derived defaults** cited to `GLM52-DGX-THEORETICAL-CEILING`; every emitted row is labelled\n**PROJECTED** (roofline, not a served measurement) except a future `--trace` mode fed by #H.\n\n## Why\n\nTurns the epic's projected coalescing curve into a **witnessed, deterministic, zero-hardware**\nnumber — the table the demo (#I), the metric (#C), and the honest headline all rest on. Reports\nthe two baselines from §2.1 side by side so the 4–10×+ is never stated without its denominator:\n`× vs B un-coalesced streams` (the defensible headline) and `× vs single-agent latency` (labelled\naggregate/latency-tolerant only).\n\n## DoD\n\n- [ ] `cmd/coalescebench` prints a markdown table: `B | U(B) | C(B) | SSD_term | net_toks | ×vs-uncoalesced | ×vs-1agent | binding-roofline`.\n- [ ] A synthetic router with a tunable skew knob (uniform → Zipf) so `U(B)` growth is inspectable; default skew cites a source or is labelled a placeholder.\n- [ ] The regime transition (B* where `SSD_term` drops below `RAM_term` → the box goes effectively resident) is detected and printed.\n- [ ] Determinism test in `cmd/coalescebench` (or `internal/`): same seed/flags → identical table (no `Math.random`/time).\n- [ ] A `docs/benchmarks/` results note with the projected curve, every cell labelled, and the explicit statement that it is a roofline pending #H's real-trace witness.\n- [ ] New leaf `coalescebench` gets its architest tier row (`TestEveryPackageDeclaresTier`) and, if import-free at root, `pureRoot`.\n\n## Files\n\n`cmd/coalescebench/` (new), `docs/benchmarks/COALESCE-NET-TOKS-RESULTS.md` (new), architest tier\nrow. CPU-only, **no hardware gate**.\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5245",
      "Number": 5245
    },
    {
      "ID": "issue-5525",
      "Split": "test",
      "Title": "fix(gateway): a result-floor 502 on the streaming passthrough is never counted or printed, so the upstream-error metric silently under-reports",
      "Body": "## What\n\n`internal/gateway/messages_stream_passthrough.go:449-450` is the one arm of the streaming\npassthrough's terminal switch that returns **without counting or printing anything**:\n\n```go\ncase p.wroteError:\n    return true // a clean terminal HTTP error was already written\n```\n\nIts two siblings both observe and render. The mid-stream arm:\n\n```go\ns.renderTurnDebugError(reqTrace, \"anthropic_messages\", err, time.Since(began))\n```\n\nand the `default:` arm at :455-456:\n\n```go\ns.metrics.observeUpstreamError(err)\ns.renderTurnDebugError(reqTrace, \"anthropic_messages\", err, time.Since(began))\n```\n\nSo a turn that reaches the `p.wroteError` arm is invisible on `/metrics` and prints no `FAILED`\nline on the default debug output. The comment is accurate about what happened — a clean terminal\nHTTP error *was* written to the client — but \"the client was told\" and \"the operator was told\" are\ndifferent questions, and only the first is satisfied.\n\n## Why it matters now\n\nThe reachable producer of `wroteError = true` is the **result-floor** branch at :241-248:\n\n```go\nadms, aerr := p.s.admitInboundResults(p.r.Context(), p.req.Messages, p.req.Tools, p.reqTrace)\nif aerr != nil {\n    p.s.logf(\"gateway: result-floor error (messages stream): %v\", aerr)\n    writeErr(p.w, http.StatusBadGateway, \"upstream model error\")\n    p.wroteError = true\n    return errPassthroughResponded\n}\n```\n\nA result-floor rejection therefore returns a 502 to the caller and leaves **no operator-visible\ntrace except an `s.logf`**, which only reaches the `--log` stream — OFF by default. The\n`default:` arm's own comment says exactly why that is not enough: \"the `s.logf` calls below only\nreach the --log stream, OFF by default\". The same reasoning applies here and was not carried over.\n\nThe failure mode is the bad one: a class of 502s that never appears in the error metric, so the\n`/metrics` upstream-error count under-reports and the gap is silent. Nobody notices a number that\nis too small.\n\n## Scope note\n\nThis was found while working #5491. That fix removes the *in-band refusal* caller from this arm —\nbefore it, the pre-start in-band error path also set `wroteError` — so #5491 narrows what reaches\n:449 but does **not** close this. The result-floor branch at :247 still takes it, which is why this\nis filed separately rather than folded in.\n\n## Suggested shape\n\nMake the arm consistent with its siblings: `observeUpstreamError` + `renderTurnDebugError` before\nreturning, without re-writing to the client (the HTTP error is already sent — that part is\ncorrect). Care is needed not to double-count if any future caller observes before setting the flag;\ntoday's single producer at :247 does not, so a straight addition is currently exact.\n\nA witness would assert that a result-floor rejection increments the upstream-error metric exactly\nonce and emits a debug line, while still writing exactly one 502 body.\n\n## Confidence\n\n**Verified by reading**, at the current tip: the three switch arms and their differing calls, that\n:247 is the surviving producer of the flag, and that `writeErr` sends the body. Line numbers are\nfrom the working tree with #5491's change applied — the arm itself is untouched by that change.\n\n**Not reproduced:** I did not drive a live result-floor rejection and diff `/metrics` before and\nafter. That is the checkable next step and would confirm the under-count directly.\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5525",
      "Number": 5525
    },
    {
      "ID": "issue-5363",
      "Split": "test",
      "Title": "feat(accounts): fak guard finds no Claude login on macOS — read Claude Code's Keychain credentials",
      "Body": "On a Mac where the operator is logged into Claude Code (2.x), `fak guard` refuses to launch:\n\n    fak guard: no Claude subscription token found and no ANTHROPIC_API_KEY set, and stdin is not a terminal — refusing to spawn a headless agent ... login=needs_login can_serve=false\n\nRoot cause: every fak credential probe is disk-only — `hasClaudeCredentials` / `resolveAnthropicOAuthToken` / `readLiveAccessToken` / `credExpiresAt` / `credExpiry` read `\u003cconfig\u003e/.credentials.json` and `\u003cconfig\u003e/.oauth-token`. Claude Code on macOS stores the live subscription OAuth credential in the login Keychain instead (generic password, service `Claude Code-credentials`, account `$USER`; a non-default `CLAUDE_CONFIG_DIR` login appends `-\u003cfirst 8 hex of sha256(NFC(dir))\u003e` to the service). The plaintext file is only Claude Code's fallback store, so on a healthy Mac none of the probed files exist and every seat reads as `needs_login`.\n\nFix: one Keychain reader in `internal/accounts` (exec `/usr/bin/security find-generic-password -a \u003cuser\u003e -w -s \u003cservice\u003e`, darwin-only seam, short TTL cache, credential-safe), consulted as a fallback by:\n- `hasClaudeCredentials` (login posture / `guardSubscriptionLoginPresent`)\n- the guard OAuth token loader (between `.credentials.json` and `.oauth-token`)\n- `credExpiresAt` / `credExpiry` (headless StaleCred rung, park-for-relogin, TriggerRefresh)\n- `readLiveAccessToken` (account failover picker)\n\nNon-darwin behavior is byte-for-byte unchanged (the seam is nil). Verified end to end on a Mac with a live Keychain login: `fak guard status` reports ready/can_serve and a guarded session boots.",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5363",
      "Number": 5363
    },
    {
      "ID": "issue-5645",
      "Split": "test",
      "Title": "feat(closeout): verify range-scoped secondary obligations from repository evidence",
      "Body": "## Parent context\r\n#4636\r\n\r\n## Summary\r\n\r\nAdd a repository closeout verifier that derives secondary obligations from a commit range and the current candidate paths instead of trusting an agent checklist.\r\n\r\nfak already has specialized checks for documentation placement, generated indexes, release state, and witnessed completion. The missing layer is one bounded report that maps the actual change set to the follow-up artifacts it owes and returns repairable machine states.\r\n\r\n## Scope\r\n\r\n- Accept a commit range and, optionally, selected dirty paths.\r\n- Drive obligation discovery from a declarative change-kind-to-artifact map.\r\n- Emit one row per obligation with a closed state such as `paid`, `unpaid`, `unproven`, `no_candidates`, or `inherited`.\r\n- Include candidate and checked counts so an empty range or empty selector cannot look clean.\r\n- Render bounded repair missions without letting worker narration set the final verdict.\r\n- Keep any lifecycle-hook adapter advisory and bounded; the verifier must be rerun after repairs.\r\n\r\n## Done conditions\r\n\r\n- Invalid or unreadable ranges return a typed could-not-run result.\r\n- Generated-index, documentation, release, and tracker obligations can be registered without hard-coding them into the renderer.\r\n- JSON and human output describe the same obligation rows and counts.\r\n- A repair run cannot mark itself complete; only a fresh readback of the repository range can close the row.\r\n- Synthetic fixtures cover paid, unpaid, inherited, empty-domain, and checker-unavailable cases.\r\n\r\n## Proof requirement\r\n\r\nLand a failing-first fixture where a primary change omits one declared secondary artifact, show the verifier naming the exact unpaid row, apply the repair, and independently rerun the verifier to a paid result. Include a second fixture proving an empty or invalid range cannot return an ordinary pass.\r\n\r\n## Non-goals\r\n\r\nThis issue does not prescribe a release process or require every repository to enable the same obligation classes.\r\n\r\n## Provenance\r\n\r\nprivate workspace review\n\n## Work estimate\nEstimate: 3 points. Uncertainty: operator-supplied estimate; revise through the parent denominator when evidence changes.\n\n## Overall completion contribution\nContribution: 3/100 points.\n\n## Completion standard\nproduction\n\n## Target operating envelope\n- repositories: \u003e= 1 tree\n\n## Witnessed operating envelope\n- repositories: 1 tree\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5645",
      "Number": 5645
    },
    {
      "ID": "issue-5521",
      "Split": "test",
      "Title": "fix(dos): dos_commit_audit silently ignores its commit argument and audits HEAD, so a witness verdict can be attributed to the wrong sha",
      "Body": "## What happens\n\nThe `dos` MCP tool `dos_commit_audit` accepts a `commit` argument and **silently ignores it**, auditing `HEAD` instead. It reports the HEAD sha in its own `sha` field, so the response looks well-formed and authoritative — it is simply about a different commit than the one asked about.\n\nThree calls, three different `commit` values, one workspace (`C:\\work\\fak`):\n\n| `commit` passed | `sha` returned | `source_files` returned |\n|---|---|---|\n| `bc0988c11` | `b66a2dba5` | `internal/agent/stream_stall.go`, … |\n| `affbb1c70` | `b66a2dba5` | `internal/agent/stream_stall.go`, … |\n| `059d561a7` | `b66a2dba5` | `internal/agent/stream_stall.go`, … |\n\n`b66a2dba5` was `HEAD` at the time. The three requested commits are unrelated to it and touch `internal/toolprocgate/`, `internal/gateway/`, and `internal/superloop/` respectively — none touches `internal/agent/`.\n\nThe argument is *declared*: passing it raises no validation error. It is accepted and dropped.\n\n## Why this is worth fixing rather than documenting\n\nThe verdict is a **witness**. Its whole purpose is to answer \"does commit X's diff do the kind of thing commit X's message claims\". Auditing a different commit does not weaken that answer — it inverts it, because:\n\n- The failure is **silent and directional**. A caller auditing an old, genuinely-unwitnessed commit gets `verdict: OK` whenever HEAD happens to be well-formed. The tool fails toward \"witnessed\", which is the unsafe direction for a gate.\n- The response **self-reports a sha**, so a caller that logs the result records a true-looking statement about the wrong commit. Any downstream record built from this is mis-attributed at the source.\n- Anything that closes issues on a commit-audit verdict is affected. The close-resolved arm's comments cite a resolving commit and say the close was \"witnessed via `dos commit-audit` (verdict OK / diff-witnessed)\". If the audit graded HEAD rather than the cited commit, the citation is not evidence for what it appears to attest.\n\nFound while auditing a backlog of tickets that were fixed on trunk but never auto-closed, so the practical impact is not hypothetical — this is exactly the path that decides whether a fix is credited to its ticket.\n\n## Note on the CLI\n\nThe installed `dos` CLI does not accept `--commit` at all (`dos: error: unrecognized arguments: --commit`), so the CLI and the MCP surface disagree about whether targeting a specific commit is even expressible. Worth settling both ways at once.\n\n## Suggested resolution\n\nEither honour `commit`, or reject it explicitly. A hard error on an argument that cannot be honoured is strictly better than a confident answer about the wrong object. If the audit is intentionally HEAD-only, the parameter should be removed from the schema and the tool should say so in its description.\n\nUpstream note: the audit implementation lives in `dos-kernel`, so the fix likely belongs there; filing here because the close-resolved arm that consumes it operates on this repo's issues.\n",
      "URL": "https://github.com/anthony-chaudhary/fak/issues/5521",
      "Number": 5521
    }
  ]
}
