# Six-month release steward

Run `go run ./examples/release-steward`. The no-key demo advances one stable macro identity through three baseline sessions, two restarts, mailbox delivery, a delegated child, a micro-fleet operation, selective state promotion, and terminal export/retirement. Its JSON receipt makes the session/sub-agent/micro-operation boundaries inspectable and proves raw child history is not durable by default.
