# Example Output

Command:

```bash
go run ./examples/security-fleet-governance
```

Representative output:

```text
PASS security-fleet-governance: floor, attestation, canonical references, narrowing, read-back, and rollback
```
