---
name: bottleneck-map
description: Map the current "what is limiting us right now?" state across the running agent fleet and the live GitHub issue backlog. Runs fleet bottleneck detection plus issue triage, records the dominant system bottlenecks,...
metadata:
  generated-by: fak project-assets sync
  canonical: ../../../.claude/skills/bottleneck-map/SKILL.md
---

# Canonical project skill adapter

Load and follow [`../../../.claude/skills/bottleneck-map/SKILL.md`](../../../.claude/skills/bottleneck-map/SKILL.md). This generated discovery adapter contains no maintained workflow body.

## Portability contract

- The linked canonical `SKILL.md` is the single semantic workflow body for Claude, Codex, and fak-native loaders.
- This adapter changes discovery only; it must not fork, summarize, or translate the workflow.
- Harness-native invocation, permissions, hooks, model routing, and worker launch remain typed adapters outside the semantic body.
