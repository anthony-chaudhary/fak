---
name: guard-rsi-score
description: One repeatable pass that keeps the RSI loop for `fak guard` actually CLOSING on our own usage - the real, hash-chained decision journal `fak guard` writes - instead of a plan-mode scaffold that never closes. Runs...
metadata:
  generated-by: fak project-assets sync
  canonical: ../../../.claude/skills/guard-rsi-score/SKILL.md
---

# Canonical project skill adapter

Load and follow [`../../../.claude/skills/guard-rsi-score/SKILL.md`](../../../.claude/skills/guard-rsi-score/SKILL.md). This generated discovery adapter contains no maintained workflow body.

## Portability contract

- The linked canonical `SKILL.md` is the single semantic workflow body for Claude, Codex, and fak-native loaders.
- This adapter changes discovery only; it must not fork, summarize, or translate the workflow.
- Harness-native invocation, permissions, hooks, model routing, and worker launch remain typed adapters outside the semantic body.
