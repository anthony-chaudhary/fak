name: Feature request
description: Propose an improvement to fak or the DOS trust kernel.
title: "[feature] "
body:
  - type: markdown
    attributes:
      value: |
        A clear problem statement is the valuable part — a proposed design is
        optional. "This refusal was confusing" or "I wanted X and had to
        hand-roll it" are both great starts.
  - type: textarea
    id: problem
    attributes:
      label: The problem
      description: What are you trying to do, and where does it hurt today?
    validations:
      required: true  - type: dropdown
    id: primary_problem
    attributes:
      label: Primary fak problem
      description: Choose the main operator burden this removes; see docs/problems-we-solve.md.
      options:
        - P1 — Context should manage itself
        - P2 — Efficiency must be net-true
        - P3 — Fast adaptation without another standard
        - P4 — Agent operations should be one product, not a kit
    validations:
      required: true
  - type: textarea
    id: operator_value
    attributes:
      label: Today → better → witness
      description: Name the real next-best alternative, the observable operator step/decision/cost that disappears, and the artifact that would prove it.
      placeholder: "Today: …\nBetter because: …\nWitness: …"
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: Proposed solution (optional)
      description: If you have a shape in mind, sketch it. Alternatives welcome too.
    validations:
      required: false
