{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "python",
            "args": [
              "-c",
              "import os,subprocess,sys; root=os.environ.get('CLAUDE_PROJECT_DIR') or os.getcwd(); subprocess.call([sys.executable,os.path.join(root,'tools','dos_hook.py'),'posttool','--workspace',root]); sys.exit(0)"
            ],
            "type": "command"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "python",
            "args": [
              "-c",
              "import os,subprocess,sys; root=os.environ.get('CLAUDE_PROJECT_DIR') or os.getcwd(); d=os.path.join(root,'tools','.bin'); bins=[os.path.join(d,'repoguard.exe')]+([] if os.name=='nt' else [os.path.join(d,'repoguard')]); ex=[x for x in bins if os.path.exists(x)]; p=max(ex,key=os.path.getmtime) if ex else None; args=[p,'--hook'] if p else [sys.executable,os.path.join(root,'tools','repo_guard.py'),'--hook']; subprocess.call(args); sys.exit(0)"
            ],
            "type": "command"
          }
        ],
        "matcher": "Bash|Read|Write|Edit|MultiEdit|NotebookEdit"
      },
      {
        "hooks": [
          {
            "command": "python",
            "args": [
              "-c",
              "import os,subprocess,sys; root=os.environ.get('CLAUDE_PROJECT_DIR') or os.getcwd(); subprocess.call([sys.executable,os.path.join(root,'tools','dos_hook.py'),'pretool','--workspace',root]); sys.exit(0)"
            ],
            "type": "command"
          }
        ],
        "matcher": "Bash|Write|Edit|MultiEdit|NotebookEdit|mcp__.*dos.*"
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "command": "python",
            "args": [
              "-c",
              "import os,subprocess,sys; root=os.environ.get('CLAUDE_PROJECT_DIR') or os.getcwd(); subprocess.call([sys.executable,os.path.join(root,'tools','dos_hook.py'),'stop','--workspace',root]); sys.exit(0)"
            ],
            "type": "command"
          }
        ]
      }
    ]
  }
}
