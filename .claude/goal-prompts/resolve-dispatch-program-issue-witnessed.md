Read `.claude/goal-prompts/issue-owner-lifecycle.md` now and obey it as the binding `ISSUE_OWNER` / `LEAF_CHILD` lifecycle.

## Domain delta: dispatch program

- Select the first eligible, unclaimed item from the launcher's declared dispatch-program menu whose exact tree admits a lease; if none admit, report `LANDED: nothing` without bypassing a refusal.
- Read the selected issue and its named parity target before editing. New tooling is Go: pure logic in the internal leaf and only the required thin command shell.
- Preserve dispatch receipt schemas and prove the real command-level producer, not only a pure helper.
- Apply no lifecycle override: root implementation, executable work-shape classification, one-level admitted delegation, independent witness, park semantics, and exhaustive child/lease/intent closeout remain mandatory.
