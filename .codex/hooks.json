{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"${FAK_CODEX_RAW_RECOVERY:-}\" = \"break-glass\" ]; then echo 'fak codex guard: BREAK-GLASS raw Codex recovery active; fak capability floor and guard audit are not running' >&2; else fak sessions codex-loop-hook 2>/dev/null || true; fi",
            "commandWindows": "if ($env:FAK_CODEX_RAW_RECOVERY -eq 'break-glass') { [Console]::Error.WriteLine('fak codex guard: BREAK-GLASS raw Codex recovery active; fak capability floor and guard audit are not running') } else { fak sessions codex-loop-hook 2>$null; exit 0 }",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
