// Package harnessdiscover finds scoped harness declarations and proves where each came from.
//
// Tier: foundation (1). Discovery runs before product composition and imports only
// the standard library plus the foundation harnessselect contract.
package harnessdiscover
