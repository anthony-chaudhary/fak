// Package modelsetresolve deterministically binds harness roles to candidates
// from a validated, normalized model inventory.
//
// Tier: foundation-composite (2) - see internal/architest. This package may
// import only packages whose tier is <= 2; an upward import fails the gate.
package modelsetresolve
