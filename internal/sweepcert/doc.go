// Package sweepcert provides pure cross-layer sweep evidence validation and
// deterministic finding folds. Qwen3.8-native prefill helpers bind exact
// receipt identities and keep terminal maxima censored until range closure is
// explicitly proven; they do not synthesize measurements.
//
// Tier: primitive (1) - see internal/architest. This package may import only
// packages whose tier is <= 1; an upward import fails the architest gate.
// See AGENTS.md and internal/architest for the layering contract.
package sweepcert
