// Package harnessresolve compiles typed harness assets and component constraints into an immutable product lock.
//
// Tier: foundation-composite (2). It composes the foundation harness asset and
// generic dependency resolvers but does not load executable adapters.
package harnessresolve
