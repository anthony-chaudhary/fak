---
name: ctx-overlay
description: Compact the context window and prune the memory store when a session grows too long.
---

A context-management skill with no fak:ctxplan directive — an UNDECLARED context
surface (debt).
