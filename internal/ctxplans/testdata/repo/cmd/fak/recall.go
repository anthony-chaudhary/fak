package main

// A PARTIAL directive: it names the verb and two fields but omits warms=, so it is
// NOT a complete declaration — recall stays UNDECLARED (debt). This is the L7 teeth.
//
//fak:ctxplan verb=recall enters="a finished session as a core dump" pages="demand-paged back on recall"
func cmdRecall(argv []string) {}
