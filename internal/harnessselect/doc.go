// Package harnessselect resolves the context-dependent layers that choose a harness.
//
// Tier: foundation (1) - see internal/architest. This package imports only the
// standard library so selection can run before product adapters are loaded.
package harnessselect
