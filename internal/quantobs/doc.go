// Package quantobs emits bounded quantization route and residency telemetry.
//
// The contract reports artifact metadata, recipe declarations, runtime
// delegation, conversion records, and measured hardware residency as distinct
// evidence classes. It intentionally accepts no extensible labels.
package quantobs
