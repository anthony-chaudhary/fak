---
name: quality-report
description: Score the repository's code quality and emit a ranked table of findings. Read-only.
user-invocable: true
---

# /quality-report — a non-context skill (test fixture)

This skill mentions no context/memory management purpose, so the counter does
NOT classify it as a context knob. It proves the walker does not over-match.
